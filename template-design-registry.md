# Template Design Registry

Master log of every unique template generated with the Premium HTML Template Generator prompt. Consult this file before starting a new template — no layout, palette, hero composition, header, navigation, card structure, section order, footer, animation concept, or typography pairing may repeat.

---

## Template 01 — Bakery & Cake Shop

- **Brand name:** Bloom & Batter Cake Studio
- **Tagline:** "Handcrafted Cakes for Life's Sweetest Moments"
- **Business category:** Bakery / custom cake shop
- **Primary color:** Raspberry #C9184A (600)
- **Secondary color:** Caramel #F4A261 (500)
- **Neutral / background:** Cream #FFF8F0, Chocolate-brown dark palette #1F150E–#5A3E2C
- **Font combination:** Fraunces (display serif, headings) + Caveat (script accents/taglines) + Poppins (body)
- **Header type:** *(revised post-launch — see changelog below)* Logo-left, single unified nav row, controls-right — permanent glass background (light/dark) on every page, fixed position, no hide-on-scroll. Desktop nav + CTA appear at `xl` (1280px); hamburger drawer covers everything below that, including tablet landscape (1024–1279px).
- **Navigation style:** One flex row: logo (shrink-0) → all nav links in a single centered group (`flex-1`) → theme/RTL toggles + CTA + hamburger (shrink-0). 7 links: Home, Home 2, About, Menu, Gallery, FAQ, Contact.
- **Hero layout (Home):** Full-bleed photo hero with dual blurred "frosting blob" floating shapes, badge pill, large serif headline with script word accent, dual CTA buttons, animated scroll-down chevron, floating stats card overlapping hero bottom edge (CountUp numbers).
- **Hero layout (inner pages):** Compact (not full-height) banner with breadcrumb, varies per page: photo+gradient overlay (About, Contact, Gallery), solid raspberry with sprinkle pattern (Menu), gradient raspberry→choco with sprinkle pattern (Order), solid caramel with sprinkle + blob (FAQ). No two inner heroes are visually identical.
- **Section order (Home):** Header → Hero → Floating stats card → Why Choose Us (icon grid) → Cakes for Every Occasion (image category grid) → Today's Menu Highlights (product cards) → Gallery preview (asymmetric grid + lightbox) → Testimonials (Swiper carousel) → Process timeline (4-step, dark section) → CTA banner (gradient) → Footer.
- **Card style:** `cake-card` — rounded-3xl white/choco-800 cards, soft raspberry-tinted shadow on hover (`shadow-soft`), signature "scalloped fondant-edge" CSS motif (radial-gradient repeating bumps) used on section dividers and footer top edge.
- **Button style:** Pill / fully rounded. Primary = raspberry→pink gradient fill with lift + glow shadow on hover. Outline = 2px border, fills solid on hover.
- **Image style:** Full-bleed hero photography, image category cards with dark gradient overlay + hover zoom, masonry/columns gallery, flip-card (3D rotateY) team portraits on About.
- **Background style:** Cream base with a subtle repeating sprinkle dot-pattern texture (SVG radial-gradient) alternating with solid raspberry/caramel/choco full-bleed sections.
- **Animation style:** AOS scroll-reveal (fade-up/zoom-in) site-wide; CSS keyframe floating blobs in hero + 404; CountUp.js animated stat counters; Swiper.js autoplay testimonial carousel; GLightbox photo lightbox; 3D CSS flip-cards for team bios; smart-scroll header.
- **Footer layout:** Dark chocolate-brown, scalloped top edge, 4-column grid (brand+social / quick links / occasions / newsletter form with inline validation), bottom bar with copyright + address.
- **Mobile menu pattern:** Right-side slide-in drawer (translateX, RTL-aware direction flip) with backdrop, stacked links, theme+direction toggle buttons, and a CTA button pinned above contact details.
- **Special design features:** Dark/light mode toggle (localStorage), RTL/LTR direction toggle (localStorage, flips drawer + icons via Tailwind `rtl:` variants), custom cake enquiry form with pickup-date picker (min=today, past-date validation) and live character counter, occasion-based filter tabs (menu + gallery, shared JS), anchor-linked occasion sections, vertical milestone timeline (About), flip-card team bios, breadcrumb nav on every inner page, bakery-themed 404.
- **Pages built:** index.html (Home), home-1.html (Home 2), about.html, menu.html, gallery.html, order.html (custom cake enquiry), faq.html, contact.html, 404.html.
- **Libraries used:** Tailwind CSS (CDN, class dark mode), Font Awesome 6 (icons), Google Fonts (Fraunces/Caveat/Poppins), AOS 2.3.4 (scroll reveal), Swiper 11 (testimonials + Home 2's bestseller carousel), GLightbox (gallery lightbox), CountUp.js 2.8 (animated stats, Home only). No build step; no backend.

**Do not reuse for future templates:** logo-left unified-nav header, raspberry/caramel/cream/choco palette, Fraunces+Caveat+Poppins pairing, scalloped fondant-edge divider motif, floating-blob hero with overlapping stats card, sprinkle dot-pattern background, right-side slide-in drawer with this exact control layout, flip-card (rotateY) team bios, split-screen hero with inline stat trio (Home 2), circular category avatars (Home 2), zig-zag alternating process steps (Home 2), horizontal scroll-snap galleries (Home 2), testimonial-spotlight-with-avatar-switcher (Home 2).

### Header component changelog (applies to all pages in this template)
The header went through two corrective revisions after initial launch — noted here so future templates don't repeat the same mistakes:
1. **Grid → Flex:** original header used `grid-cols-3` (three *equal* thirds: left-nav / centered-logo / right-nav+controls). Equal-thirds tracks don't grow to fit content — the right column (nav + icons + CTA) needed far more than 1/3 of the header width at realistic desktop sizes, overflowing and visually overlapping the centered logo. Replaced with a single flex row (logo `shrink-0` + nav `flex-1` + controls `shrink-0`) sized by actual content, not arbitrary fractions.
2. **Breakpoint lg → xl:** even the corrected flex layout was razor-thin at 1024px (`lg`) with 6 nav links; adding a 7th link (Home 2) would have overflowed again. Desktop nav/CTA now gate on `xl` (1280px) instead of `lg` (1024px) — tablet-landscape widths (1024–1279px) now correctly get the hamburger drawer instead of a cramped nav row. `DESKTOP_BREAKPOINT` in `main.js` was updated to match (1280).
Also fixed: the header's transparent-over-hero state (home page only) was replaced with a permanent semi-opaque glass background on every page, since dark nav text over a busy/dark hero photo was unreadable; and the mobile drawer's RTL "closed" position (`translateX(-100%)`) was removed entirely because it wasn't matched by an equal flip of the drawer's physical docking side (`right-0`), which pulled the panel partially into view the instant RTL was toggled — the drawer now always docks and slides from the right regardless of direction.

---

## Home Page 1 — `index.html` (existing entry, unchanged) vs. Home Page 2 — `home-1.html`

`home-1.html` ("Home 2" in nav) is an alternate homepage for the same brand — same tokens (color/type/header/footer/buttons/cards), deliberately different layout, hero composition, and section presentation from `index.html` per the master prompt's "Home 1 vs Home 2 must not share section layouts" rule.

- **Hero layout:** Split-screen (not full-bleed). Text column left (eyebrow badge, serif headline with one script word, paragraph, dual CTAs, inline trio of stat callouts separated by vertical dividers — no floating card). Image column right: single rounded-corner photo with a small overlapping "review quote" card anchored to its bottom-left corner (hidden below `sm` to avoid crowding).
- **Section order:** Header → Hero (split-screen) → Cake Categories (circular avatar row, horizontally scrollable on mobile) → Best-Selling Cakes (Swiper carousel, peek-next-slide on mobile) → This Week's Fresh Bakes (dark icon band, 7-day highlight strip) → Custom Ordering Process (4-step zig-zag: alternating image-left/text-right and text-left/image-right, not the horizontal icon row used on Home) → From Our Gallery (horizontal scroll-snap strip, no lightbox) → Testimonials (single large spotlight quote + clickable initials-avatar switcher, not a multi-up carousel) → Seasonal Offers (2 gradient/dark promo cards, icon watermark, no photos) → CTA (image-left / dark-panel-right split, not a centered gradient banner) → Contact/Store info (compact 3-column icon strip linking to the full Contact page) → Footer (identical, shared component).
- **Image treatment:** Circular "rounded image" category avatars (new pattern for this template); horizontal scroll-snap "collage" strips for both the product carousel and the gallery preview (new interaction pattern); zig-zag alternating photo/text rows for the ordering process (vs. Home's numbered-icon-only row); no lightbox on this page (kept lightweight — full lightbox experience lives on `gallery.html`).
- **Animation:** Same AOS vocabulary as the rest of the site (fade-up/fade-left/fade-right/zoom-in), a second independent Swiper instance (`.bestseller-swiper`, peek-slide autoplay) distinct from Home's `.testimonial-swiper`, and a new lightweight vanilla-JS testimonial-switcher module (`data-testimonial-widget` / `data-testimonial-trigger` / `data-testimonial-panel` in `main.js`) — no new external library added for it.
- **Special features:** All category/CTA links route into `menu.html`'s hash-driven filter system (`menu.html#anniversary`, `#corporate`, etc.), so "shop by category" from this page filters the Menu grid exactly like it does from Home and from Menu's own category cards. Best-seller and gallery-strip content intentionally reuses the exact same products/photos already defined on `menu.html`/`gallery.html` (same name, price, and image per item) to keep pricing and imagery consistent across pages rather than inventing parallel data.
- **New nav entry:** "Home 2" added to both desktop nav and the mobile drawer on all 9 pages, linking here.

---

## Template 02 — Daycare & Kids Preschool

- **Brand name:** Little Sprouts Early Learning
- **Tagline:** "Nurturing Little Hearts with Loving Care" (Typed.js cycles: Loving Care / Early Learning / Bright Futures / Joyful Days)
- **Business category:** Daycare / kids preschool (infant through Pre-K, ages 6 weeks–5 years)
- **Primary color:** Coral #FF6B6B (500) — CTAs, accents
- **Secondary color:** Sky Blue #3DB4F2 (500) + Sunshine Yellow #FFC93C (500) + Leaf Green #06D6A0 (500) — playful 4-hue accent system (no single secondary; hue rotates by section/card)
- **Neutral / background:** Cream #FFFDF6, dark-mode Indigo/Navy #0D1321–#16121F (deliberately not brown/choco like Template 01)
- **Font combination:** Baloo 2 (rounded display, headings) + Quicksand (rounded sans, body) — no script font, 2-font pairing (different structure from T1's serif+script+sans triple)
- **Header type:** *(revised post-launch — see changelog below)* Two-level header — thin dark utility bar (phone/hours/enrollment link/social, hides on scroll-down) above a **logo-left, centered-nav** main row: logo (shrink-0, always far left, same on mobile) → nav links (`flex-1`, `justify-center`) → theme/RTL toggle + CTA (shrink-0, right) → hamburger (mobile only, `ml-auto` so it pins right regardless of what's hidden). Desktop nav gates at `xl` (1280px) per the lesson in T1's changelog.
- **Navigation style:** Logo-left with a centered nav group (not split either side of the logo — see changelog). 7 links total: **Home** (dropdown: Home 1 / Home 2), About, Programs, Gallery, Teachers, **Pricing**, Contact — FAQ reachable via footer + mobile drawer only (not crammed into the main desktop nav). The Home dropdown opens on hover *or* click (click never toggles closed — only outside-click/Escape/focus-out/mouseleave close it, avoiding a hover-then-click race condition), and mirrors as an inline accordion (chevron-button + collapsible sub-list) inside the mobile drawer.
- **Hero layout (Home):** Layered image composition — large organic **blob-mask** photo (CSS `clip-path: url(#id)` referencing an inline SVG `<clipPath clipPathUnits="objectBoundingBox">`, not a fixed-pixel `path()`, so it scales correctly at any size) with a smaller second blob-mask photo overlapping its bottom-left corner; floating SVG cloud/star decorative shapes (gentle bob keyframe, not blurred blobs); badge pill; Typed.js word-cycle headline; dual CTA; floating "State Licensed" card pinned to hero image corner (no CountUp stats card — that pattern is reserved for T1).
- **Hero layout (inner pages):** Compact banner with breadcrumb, varies per page: photo+gradient overlay (About, Contact), solid/gradient sky-blue with confetti-dot pattern (Programs), gradient coral with confetti (Gallery), gradient leaf-green with confetti (Teachers), gradient sunshine with confetti (Admissions), solid dark navy with confetti (FAQ). Every banner ends in a signature **wavy SVG divider** (this template's motif, replacing T1's scalloped edge) whose curve direction/color varies per section transition.
- **Section order (Home 1 — `index.html`):** Header → Hero (layered blob images) → Trust badges strip (4 icons) → Welcome snapshot (split image+text, floating stat badge) → Age-Group Programs (4-tab switcher: Infant/Toddler/Preschool/Pre-K, image+text panel) → Daily Schedule (vertical alternating timeline with dashed rail + colored icon nodes) → Why Parents Choose Us (6 sticker-card icon grid) → **Safety & Hygiene** (split photo-left / numbered-protocol-list-right, new section) → Facility Gallery preview (horizontal-scroll polaroid filmstrip, rotated photos, no lightbox) → Meet Our Teachers preview (Swiper circular-avatar carousel, click-to-modal) → Parent Testimonials (single spotlight quote, Swiper fade effect, no multi-card carousel) → **Upcoming Events** (3-card grid, "ticket stub" motif — date block + dashed-line divider + details, new section) → **Enrolment Process** (horizontal 4-step band, white circles on a solid sky-blue section connected by a dashed line, new section) → **FAQ preview** (single-open accordion, top 4 questions + "View All FAQs" link, new section) → Enrollment CTA (image-left / coral-panel-right split) → Footer.
- **Card style:** `sticker-card` — rounded-[1.75rem] white/navy cards, **thick 3px colored border** (color varies per card via `--card-accent`), slight independent rotation per card (`--rot`, corrects to 0° + lifts on hover), small rotated "tape" rectangle pinned at top-center. Completely different silhouette from T1's scalloped-edge `cake-card`.
- **Button style:** `btn-chunky` — chunky rounded-xl (not pill), solid fill with a **thick flat bottom-offset shadow** (`box-shadow: 0 5px 0 <darker-shade>`) that compresses on `:active` (translateY + shadow shrink) for a tactile "press" feel — mechanically distinct from T1's pill gradient lift+glow.
- **Image style:** Organic blob-mask hero photography (objectBoundingBox SVG clip-path), rotated polaroid-frame photos (gallery filmstrip + preview), circular cropped teacher/testimonial portraits, editorial CSS-grid gallery (asymmetric spans, not masonry columns) with hover-zoom + caption overlay. No flip-cards anywhere (reserved for T1).
- **Background style:** Alternating pastel color-block sections (cream / sky-tint / leaf-tint / coral-tint / sunshine-tint) separated by wavy SVG dividers; sparse confetti-dot pattern reserved for inner hero banners only (not a site-wide texture like T1's sprinkle background).
- **Animation style:** GSAP + ScrollTrigger scroll-reveals site-wide (fade-up + stagger) — a different engine from T1's AOS; Typed.js hero word-cycle; Swiper (teacher circular carousel + testimonial fade-spotlight); GLightbox gallery; GSAP-driven animated number roll-ups (stats, no CountUp.js — avoids reusing T1's library for the same visual concept); CSS keyframe floating cloud/star shapes; vanilla-JS accordion (FAQ + reusable for future schedule use), tabs (age-group programs, URL-hash aware), teacher-bio modal, gallery category filter, FAQ live search; smart-scroll header (hides utility bar only, main row stays fixed).
- **Footer layout:** **Light** pastel sky-blue footer (not dark like T1) with a wave top divider, 3-column grid (brand+social+hours / quick links + programs sub-list / visit-us info + inline "Schedule a Tour" CTA button), centered bottom bar.
- **Mobile menu pattern:** **Full-screen fade+scale overlay** (fixed inset-0, **right-aligned** stacked links — `items-end` + `text-right`, so it flips to visually-left automatically under `dir="rtl"` — with staggered per-link transition-delay entrance), large top-right close button, "Home" renders as an inline accordion (chevron-button + collapsible Home 1/Home 2 sub-list) rather than a plain link, theme toggle (icon-only circle) + RTL/LTR toggle (icon + short "RTL"/"LTR" label, `whitespace-nowrap` + `shrink-0` so it can never wrap to two lines) sit in one non-wrapping row, CTA button at the bottom — completely different mechanic from T1's right-side slide-in drawer.
- **Special design features:** Dark/light mode toggle (localStorage), RTL/LTR toggle (localStorage), age-group program tabs shared between Home 1 and `programs.html` (URL-hash aware, `#infant` / `#toddler` / `#preschool` / `#prek`), interactive vertical daily-schedule timeline, admission enquiry + campus-tour-request form with child-DOB → auto age-group-suggestion logic and a dynamic `data-min-today` / `data-max-today` date-limit system (sets `min`/`max` via JS on load instead of a hardcoded date, so the form never goes stale), teacher-bio modal popups (click any teacher card site-wide — shared modal, no dedicated bio pages), gallery category filter + GLightbox (every `.glightbox` link needs an explicit `; type: image` in its `data-glightbox` attribute — see changelog), FAQ accordion with live text search, graceful `onerror` image fallback (inline gradient SVG data-URI) on every photo via a shared `ph-img` class + `initImageFallback()`, breadcrumb nav on every inner page, balloon-illustration 404 with floating decorative shapes, desktop "Home" mega-lite dropdown (2-item panel, hover + click + keyboard, closes on outside-click/Escape/focus-out).
- **Pages built:** index.html (Home 1), **home-2.html** (Home 2, added post-launch), about.html, programs.html, gallery.html, teachers.html, **pricing.html** (added post-launch), admissions.html (enquiry/tour form), faq.html, contact.html, 404.html.
- **Libraries used:** Tailwind CSS (CDN, class dark mode), Font Awesome 6 (icons), Google Fonts (Baloo 2 / Quicksand), GSAP 3.12 + ScrollTrigger (scroll reveals, replaces T1's AOS), Typed.js 2.1 (hero word-cycle, Home 1 only), Swiper 11 (teacher carousel + testimonial spotlight, Home 1 + Home 2 hero collage does **not** use Swiper), GLightbox (gallery lightbox). CountUp is done via a GSAP tween (`data-reveal-number`), not CountUp.js — used on Home 1's welcome-snapshot badge and Home 2's stats band. No build step; no backend.

**Do not reuse for future templates:** logo-left-centered-nav header with two-level utility bar and a hover/click "Home" mega-lite dropdown, coral/sky/sunshine/leaf multi-hue playful palette, Baloo 2 + Quicksand pairing, wavy-SVG-divider motif, layered dual blob-mask hero composition, objectBoundingBox SVG clip-path blob technique, sticker-card (thick-border + rotation + tape-pin) component, chunky flat-bottom-shadow "press" button mechanic, polaroid-filmstrip gallery preview, light pastel footer with wave-top, full-screen fade+scale mobile overlay menu (right-aligned), GSAP+ScrollTrigger as the site-wide reveal engine, vertical alternating dashed-rail timeline (Home 1 daily schedule), age-group tab switcher pattern, teacher-bio click-to-modal pattern, asymmetric photo-collage hero (Home 2), comparison-table "us vs typical daycare" section (Home 2), ticket-stub event cards (Home 1) / stacked date-badge event list (Home 2), horizontal dashed-line connected-circle process steps (Home 1) / sticker-card numbered process cards (Home 2), FAQ accordion preview (Home 1) / FAQ topic-icon grid (Home 2), pricing sticker-cards with a "Most Popular" ribbon + centered gradient final-CTA band (Pricing page).

### Home Page 1 (`index.html`) vs. Home Page 2 (`home-2.html`)
Added after initial launch, once the brief called for a second homepage. Same tokens (palette/type/header/footer/buttons/cards), deliberately different hero composition, section order, and component choices per page — no section on Home 2 reuses Home 1's exact layout for the same content type.

- **Hero layout:** Home 1 = layered organic blob-mask photo composition (image right, text left), Typed.js word-cycle headline, floating cloud/star shapes. Home 2 = **asymmetric photo collage grid** (one tall photo + two stacked smaller photos, plain rounded-2xl corners, no blob-mask), image **left**, text **right** (mirrored from Home 1), star-rating + licensing trust line instead of a word-cycle.
- **Section order (Home 2):** Header → Hero (photo collage) → **Quick Stats band** (dark full-bleed strip, 4 GSAP count-up numbers — new position for this device; Home 1 only uses a single count-up badge) → **Age Groups** (static 4-card photo grid with gradient-overlay hover-zoom links into `programs.html#...`, not the thabbed switcher Home 1 uses) → **Learning Programs / Curriculum Pillars** (4-icon band on a solid leaf-green confetti section — STEM / Literacy / Arts / Social-Emotional — a concept Home 1 doesn't have at all) → **Daily Activities** (horizontal scroll-snap photo strip with a time-of-day caption baked into each photo's gradient overlay, vs. Home 1's vertical dashed-rail timeline) → **Why Parents Choose Us** (2-column "Typical Daycare vs. Little Sprouts" comparison table with check/x rows, replacing Home 1's sticker-card icon grid entirely) → **Safety & Hygiene** (5-icon horizontal badge strip, no photo — vs. Home 1's photo+numbered-list split) → Meet Our Teachers (static 4-card grid linking to `teachers.html`, not a Swiper carousel) → Parent Testimonials (3-card grid, all visible at once, not a single-spotlight Swiper) → Upcoming Events (stacked list widget with square date chips + chevron, vs. Home 1's 3-card ticket-stub grid) → Enrolment Process (2×2 grid of sticker-cards with giant numerals, vs. Home 1's horizontal dashed-line connected circles) → FAQ (4-icon topic-card grid linking into `faq.html`, vs. Home 1's accordion preview) → Final CTA (full-bleed photo background with a dark gradient overlay and centered text, vs. Home 1's image-left/color-right split panel) → Footer (identical, shared component).
- **New nav entry:** "Home" became a dropdown (Home 1 / Home 2) in both the desktop nav and the mobile drawer, on all 11 pages.

### Pricing Page (`pricing.html`)
Added after initial launch as a dedicated main-nav page.

- **Hero:** New gradient combination — leaf-green→sky-blue diagonal gradient + confetti-dot pattern + a coin/leaf float-shape pair (not reused from any other inner-page hero gradient).
- **Section order:** Header → Hero → Pricing Plans (6 sticker-cards in a 3-col grid: Half-Day Daycare, Full-Day Preschool *"Most Popular"* with a ribbon badge + `-translate-y-3` lift, Full-Day Infant/Toddler, After-School Care, Optional Activities add-on, Drop-In/Hourly) → What's Included (6-item icon+text card grid) → Flexible Care Options + Sibling Discounts (2-column colored panels, leaf-green and coral) → Admission Process (numbered list beside a sticker-card CTA panel — lighter-weight than either homepage's process treatment) → Pricing FAQs (single-open accordion, 6 tuition-specific questions) → Final CTA (**centered gradient band**, sunshine→coral→coral, no photo, no split panel — a third distinct CTA treatment alongside Home 1's split-panel and Home 2's full-bleed-photo CTAs) → Footer.
- **Card style:** Reuses the site's `sticker-card` component (consistent with every other page) but adds a "Most Popular" ribbon + vertical lift for the featured plan — the only page-specific card variant, not a new component.

### Gallery Page redesign (`gallery.html`)
Redesigned after initial launch — replaced the original asymmetric "editorial" grid (one `col-span-2 row-span-2` featured tile + one `col-span-2 aspect-video` tile among plain squares) with a **uniform grid**: every tile is the same `aspect-[4/5]`, so the CSS grid auto-flow can never leave a gap or an incomplete row, in any filtered state, at any breakpoint. The asymmetric layout was the root cause of the original imbalance — with irregular tile spans, categories couldn't be filtered to an even, gap-free layout no matter how many photos each had.
- **Photo count:** Every category now holds exactly **4 photos** (16 total, was 4/3/3/2 unevenly). 4 new content-verified images were sourced via Unsplash search (not guessed IDs) to fill the gaps: one playground photo, one art-activity photo, two meal-time photos.
- **Filter tabs:** Each tab now shows a live photo count badge (`data-filter-count`, computed from the DOM at runtime, not hardcoded) — e.g. "Meals ⸱" — plus a category icon. Active tab styled via a new `.gallery-filter-btn[aria-selected="true"]` state (coral fill + bottom-shadow, distinct from the `tab-btn` class used for the age-group tabs elsewhere).
- **Category badge chips:** Each tile carries a small always-visible color-coded pill top-left (sky=Classroom, leaf=Playground, coral=Activity, sunshine=Meal Time) so the category reads at a glance even before hovering — new pattern, not used elsewhere in the template.
- **Load More pagination:** `data-gallery-grid` + `data-batch-size="8"` drive a JS batching system (`initGalleryFilter` in `main.js`, rewritten): "All Photos" shows 8 initially with a "Load More Photos" button revealing the rest; switching to any single-category filter shows all 4 of that category directly (no pagination needed for a set that small) and hides the Load More button; returning to "All Photos" resets the batch. A live `data-gallery-count` label ("Showing 8 of 16 photos") tracks the current state.
- **Reveal animation:** New `.gallery-tile` / `.gallery-tile-visible` CSS pair (opacity + translateY + scale transition) replaces the old instant `display:none` toggle — items entering view (via filter change or Load More) now fade/rise in smoothly instead of popping. Deliberately *not* wired through GSAP ScrollTrigger's `data-reveal` system, since ScrollTrigger initializes against an element's layout box at scan time and this grid's items are frequently `display:none` at load (everything past batch 1) — mixing the two would have produced unreliable trigger positions.

**Gallery page content expansion** (second pass, same file): the page went from "hero + filter grid + one-line CTA" to a full campus-life showcase. New section order after the grid: **Gallery Overview** (intro copy, added directly under the hero, plus a 4-icon stat strip — Photos / Classrooms / Playground / Years) → filter grid (existing) → **Classroom Highlights** (image-left split, floating stat badge, 2×2 checklist) → **Outdoor Play Area** (mirrored text-left/image-right split, inline stat trio instead of a checklist — deliberately not identical to Classroom Highlights) → **Arts & Crafts** (clustered, independently-rotated `.polaroid` pair standing in for a "gallery wall," text beside it) → **Learning Activities** (text-only 2×2 icon grid, no photos at all — an intentional pacing break between photo-heavy sections) → **Meals & Nutrition** (photo + 3-column "menu card" band: Breakfast/Lunch/Snacks) → **Parent Testimonials** (horizontal scroll-snap quote-card strip — a fourth distinct testimonial treatment alongside Home 1's spotlight swiper, Home 2's static 3-grid, and none of them reused) → **Gallery FAQ** (two independent `data-accordion="single"` groups side by side, so opening a question in one column never closes the other — distinct from every other page's single-column accordion) → **Enrolment CTA** (solid dark `bg-ink` band with a GSAP count-up stat trio + dual buttons — a fourth distinct CTA treatment alongside Home 1's split-panel, Home 2's full-bleed-photo, and Pricing's centered gradient).

### Programs, Teachers & About content expansion (third pass — `programs.html`, `teachers.html`, `about.html`)
All three pages went from a single lean section to a full, unique page — content only, no new page-level layout system (each already had a distinct header/hero treatment from initial launch).

- **`programs.html`:** added a "Curriculum Philosophy" intro band (3 icon pillars) right after the ratio strip. Each of the 4 existing age-group split sections gained a shared but per-program-colored sub-block below the checklist: a "A Typical Day" mini time-stamped schedule (4–5 entries) + a "Key Learning Outcomes" chip row (4 tags) — the same *template* repeated once per program is intentional here (users compare programs apples-to-apples), the *content* is unique per age group. Closed with a new "Which Program Is Right for Your Child?" comparison table (Age / Ratio / Hours / Focus × 4 programs) before the existing CTA. The Pre-K photo was replaced (see image-verification note below).
- **`teachers.html`:** added a "Teaching Methodology" 4-pillar icon band after the credentials strip. The flat 8-card teacher grid was reorganized into three labeled groups — **Leadership Team** (Director, Assistant Director) → **Classroom Teachers** (one lead per age group) → **Specialist Teachers** (Music, Art) — for real visual hierarchy instead of one undifferentiated grid. Every teacher card gained `data-years` / `data-qualification` / `data-specialties` / `data-certifications` / `data-quote`, and the shared teacher-bio modal (`#teacher-modal`, used here **and** on `index.html`'s swiper preview — both updated identically) was rebuilt to render a stat row (years + qualification, icon-led), specialty tags, a pull-quote blockquote, the existing bio paragraph, and a certifications row — `initTeacherModal()` in `main.js` populates all of it via a `fillTags()` helper and hides any row whose data attribute is absent, so cards that only set the original `data-name/role/img/bio` (there are none left, but the mechanism stays graceful) still render correctly. Added "Classrooms in Action" (3 teacher-name + signature-activity spotlight cards, e.g. "Ms. Carter's Story Circle"), a "Teacher Achievements & Growth" dark stat band (training hours, credentials earned, mentorship program, tenure — deliberately a *growth/development* angle, distinct from the top credentials strip's static aggregate numbers), and "What Parents Say About Our Teachers" (2×2 grid of quote cards paired with a colored initials-avatar and an explicit "on Ms./Mr. ___" teacher attribution — ties the testimonial content specifically to this page's people-first identity, and is the 5th distinct testimonial layout on the site alongside Home 1's spotlight swiper, Home 2's static 3-grid, Gallery's scroll-snap strip, and About's asymmetric masonry below).
- **`about.html`:** the largest expansion. New section order: Hero → Our Story (existing) → **Mission & Vision** (twin white cards, top-bordered coral/sky, side by side) → **Core Values** (existing 3-icon band, now with its own eyebrow+heading instead of sharing implied meaning with Mission) → **Our Milestones** (horizontal scroll-snap year-card timeline, 2011–2026 — a *company-history* timeline, deliberately distinct from Home 1's *time-of-day* daily-schedule timeline both conceptually and in orientation: horizontal cards here vs. vertical alternating rail there) → Impact Stats (existing dark band) → **Learning Approach** (image-left split + a numbered 3-step "how we teach" list, distinct from Home 2's 4-icon curriculum-pillar grid) → **Facilities** (asymmetric photo collage: one tall `row-span-2` classroom photo + a 2×2 grid of smaller facility photos, each with a gradient-overlay caption, linking to `gallery.html`) → Director spotlight (existing) → Accreditation & Safety (existing sticker-card grid) → **Awards & Recognition** (horizontal wrapped row of pill-shaped badges with a colored icon roundel — visually distinct from the accreditation grid directly above it on the same page) → **Community Involvement** (plain 3-column icon+text row, no cards/borders — the lightest-weight section on the page, a deliberate pacing break) → **Parent Testimonials** (asymmetric masonry: one large featured quote card + two smaller stacked cards beside it — the site's 6th distinct testimonial layout) → **FAQs** (static, non-interactive Q/A pairs with colored "Q"/"A" role avatars — deliberately *not* an accordion, since accordions are already used on `faq.html`, Home 1's preview, Pricing, and Gallery; this is the only FAQ presentation on the site with no click-to-expand interaction) → CTA (existing, now with an added inline trust-badge row above the button).

**Image-verification lesson applied immediately (see changelog item 9 below):** the Pre-K photo swapped into `programs.html` and the classroom photo added to `about.html`'s new Learning Approach section were both sourced via Unsplash-search-then-`WebFetch`-then-`Read` (not recalled from memory), and one already-mismatched image discovered *during this pass* was caught and replaced using the same technique.

### Header, mobile-menu & lightbox changelog (fixes applied after initial launch)
Three corrective revisions, recorded so future templates don't repeat the same mistakes:
1. **Split-nav → logo-left:** the original "centered-logo, split-nav-either-side" header was replaced with a conventional logo-left / centered-nav-links / right-controls layout, at explicit user request. Logo position is now identical on mobile and desktop (far left) rather than centering only on desktop and left-aligning only on mobile.
2. **Nav-link centering:** the centered nav-links group was initially left-aligned immediately after the logo (`flex-1` container, links packed to its start); changed to `justify-center` so the links center within the space between the logo and the right-side controls, per explicit request.
3. **GLightbox blank popup:** gallery images hosted on Unsplash have no file extension in the URL (`?auto=format&fit=crop&w=1400&q=80`), so GLightbox couldn't auto-detect them as images and rendered an empty iframe on click. Fix: every `.glightbox` anchor's `data-glightbox` attribute must include an explicit `; type: image` (e.g. `data-glightbox="title: Nursery Room; type: image"`) whenever the image URL lacks a recognizable extension.
4. **RTL skip-link scroll-width explosion:** the accessibility skip-link used the classic `left: -9999px` off-screen-hiding technique. Under `dir="rtl"`, the browser's overflow/scrollWidth calculation folds that huge negative offset into `document.scrollWidth` (observed: exactly `9999 + viewport-width`), which pushed the entire visible viewport off to a blank stretch of the inflated canvas — the whole page (not just the skip-link) appeared to render blank whenever RTL was toggled with the mobile menu open. Fix: hide the skip-link with `transform: translateY(-100%)` instead of a large negative `left` offset — no large offsets anywhere means nothing for RTL's overflow recalculation to inflate. **Lesson for future templates: never use `left: -9999px` (or any large fixed-pixel physical offset) for visually-hidden-until-focused elements on a template that supports RTL — use a transform-based hide instead.**
5. **Dropdown click-vs-hover race:** an early version of the desktop "Home" dropdown toggled open/closed on click while *also* opening on `mouseenter` / closing on `mouseleave`. A mouse user hovering (opens it) then clicking (toggle → closes it) got a dropdown that closed on click. Fix: click now always calls `open()` (never toggles), and closing is delegated entirely to `mouseleave`, outside-click, `focusout`, and Escape — click and hover no longer fight each other.
6. **Hero decorative shapes bleeding through the translucent header:** two of `index.html`'s floating cloud/star SVGs sat at `top-16`/`top-24` relative to the hero section — but since the header is `position: fixed` (removed from document flow), the hero section's own top edge is at document y=0, not below the header. At `lg`+ widths those shapes rendered underneath the header's `bg-white/95 backdrop-blur-md`, faintly bleeding through as a ghost artifact only visible on close inspection. Fix: pushed both shapes further down (`top-16`→`top-32`, `top-24`→`top-36`) to clear the header's ~92–104px height with margin. **Lesson: any absolutely-positioned decorative element inside the first section after a `position: fixed` header needs a `top` offset comfortably larger than the header's total height, since the section's own coordinate origin starts at the true document top, not visually below the header.**
7. **Mobile drawer content pinned to one edge at tablet widths:** per an explicit requirement the overlay itself must stay 100% width at every breakpoint, but the nav content inside it was `items-end`/`text-right` unconditionally — great on a 320–425px phone, but at 768–1279px (tablet, still below the `xl` hamburger cutoff) it left the content pinned to the far right of a mostly-empty full-width canvas. Fix: `items-end sm:items-center` + `text-right sm:text-center` (nav, the Home-submenu wrapper, and its panel) — right-aligned on phone widths, centered from `sm` (640px) up, while the overlay itself never stops being 100% width. Both requirements ("100% width" and "properly aligned on tablet") are satisfied simultaneously because they're about content alignment vs. container width — not the same property.
8. **`gallery.html` missing GSAP/ScrollTrigger includes:** the original gallery page never used scroll-reveal animation, so it never loaded the GSAP/ScrollTrigger `<script>` tags every other page includes. When later content sections were added using the site's standard `data-reveal` / `data-reveal-number` attributes, `initScrollReveals()` silently no-op'd (`typeof gsap === 'undefined'` guard) — the new sections just appeared statically (harmless) but the animated stat counters stayed frozen at their literal `0` fallback text forever (not harmless — a visibly broken counter). Fix: added the two missing script tags. **Lesson: `data-reveal*` attributes are inert markup, not a self-contained feature — always confirm the GSAP/ScrollTrigger scripts are actually present on a page before using them, especially when extending a page that predates the site's animation system.**
9. **Confirmed a genuine image/caption mismatch, and found a reliable way to catch more:** `about.html`'s new Learning Approach section used `photo-1497633762265-9d179a990aa6` captioned "Teacher guiding preschoolers through a hands-on classroom lesson" — an ID picked from memory very early in this project, before the site adopted Unsplash-search-then-verify sourcing. The rendered photo was actually a stack of colored books. Caught it by eye in a full-page review screenshot, not by any automated check (HTTP 200 + a WebFetch text description of the *search results page* both said nothing about the *actual photo's contents*). **Working technique discovered while fixing this: `WebFetch` can't describe an image URL directly (it only sees encoded binary and says so) — but it silently saves that binary to a local `.jpg` in the tool-results folder every time, and the `Read` tool *can* view that saved file. So `WebFetch` on the raw `images.unsplash.com/photo-…` URL (any prompt) → `Read` on the path it reports saving to, is a real way to visually verify a specific candidate image before committing to it, not just trust its alt-text search description.** Replaced with `photo-1761208662734-fb46f1398551`, verified this way before use. Apply this fetch-then-Read check to any image sourced from memory or from a search-results description rather than viewed directly — search descriptions describe the search snippet, not necessarily what's in the full-resolution photo.

10. **LTR mobile/tablet hamburger-left fix regressed RTL:** to move the hamburger to the left / logo to the right on mobile/tablet in LTR, the previous fix removed the hamburger's old unconditional `ml-auto` and replaced it with `ltr:order-1` / `ltr:order-2 ltr:ml-auto` (scoped only to `dir="ltr"`). This fixed LTR but left RTL with *no* edge-anchoring at all below `xl`: both elements defaulted to `order: 0` and sat packed together at the flex-start side (visually the right, since RTL mirrors `flex-direction: row`), leaving a large dead gap on the opposite side — exactly the bug in the reported screenshot. **Fix: added `justify-between` to the header's main flex row** (`<div class="flex items-center gap-4">` → `...justify-between gap-4`). This is direction-agnostic — `justify-content: space-between` always anchors the first and last flex-order items to the start/end edges, and "start/end" already flip correctly with `dir` on their own. Combined with the existing `ltr:order-1`/`ltr:order-2` swap (which only needs to override *which* element is first/last, not push anything to an edge), both directions now anchor correctly with no extra `ltr:`/`rtl:` duplication needed for the edge-pinning itself. At `xl`+ this has no effect, since the `flex-1` nav already consumes all free space regardless of `justify-content`. **Lesson: when reversing a two-element edge-pinned layout for only one text direction, prefer `justify-between` (inherently bidi-correct) over an `auto`-margin scoped to a single `dir:` variant — a scoped auto-margin fixes the direction you tested and silently strips the other direction's spacing if it was relying on that same margin before.**

### Lessons applied from Template 01's changelog
Two mistakes recorded in T1's header changelog were deliberately avoided here rather than re-learned: the centered-logo header used plain flex (`flex-1` siblings + `shrink-0` logo) from the very first draft instead of `grid-cols-3` equal-thirds, and desktop nav was gated at `xl` (1280px) from the start given a 6-link split nav needs at least as much room as T1's single-row 7-link version did.

### Content-accuracy note (image sourcing)
An initial pass populated photo placeholders with Unsplash `photo-<id>` URLs recalled from memory and verified only by HTTP 200 status — several turned out to depict unrelated subjects once rendered (a library interior captioned "Pre-K Classroom," a magnifying glass on a laptop captioned "Story Time," a street-photography shot in a gallery grid). HTTP 200 confirms a URL *resolves*, not that its *content* matches the caption. The fix: use `WebFetch` on Unsplash search-results pages (e.g. `unsplash.com/s/photos/preschool-classroom`) to extract real `images.unsplash.com/photo-…` URLs paired with their actual alt-text descriptions, then verify those specific URLs return 200 — only then treat an image as content-verified. Every photo in this template was re-sourced this way. Apply this method from the start on future templates rather than as a corrective pass.

### Cross-page image de-duplication pass
A full-site audit found the same photo reused across 2–5 *different* pages for entirely different sections — e.g. one "art studio" photo appeared on `index.html`, `home-2.html`, `about.html`, `gallery.html` (twice — once in the grid, once in its own Classroom Highlights section), and `teachers.html` simultaneously. Root cause: early sections were built by copying an existing card/section as a starting template and only the caption text was changed, not the image.
- **Policy adopted:** `gallery.html`'s filterable grid is the canonical home for every "facility scene" photo (classrooms, playground, art, meals, reading, etc.) since it's the dedicated photo-library page. `programs.html` is canonical for its own age-specific program photos. Every other page's use of the same photo was replaced with a fresh, independently-sourced one (search → verify with the `WebFetch`-then-`Read` technique above → confirm it doesn't collide with any of the ~50 IDs already on the site → apply).
- **Teacher/staff headshots are the deliberate exception** — the same person's photo is *supposed* to recur across their bio card, the shared teacher-modal, and any homepage preview swiper; that's correct identity consistency, not a duplication bug, and was left untouched.
- **When a caption no longer matched its new photo, the caption was rewritten, not left stale** — e.g. `about.html`'s Facilities collage had a "Dining Room" tile; the replacement photo (verified, unique, but not a meal scene) was captioned "Play & Discovery" instead of leaving a mismatched label. Swapping an `src` without checking the surrounding caption/label text is exactly the kind of thing that produces a *second* content-accuracy bug while fixing the first.
- **Scope of this pass:** fixed every photo that had been duplicated across 3 or more pages (the highest-severity groups: art, reading, nursery/classroom, dining, playground — each brought down to a single canonical page). A smaller set of 2-page duplicates was intentionally left for a follow-up pass given the sourcing effort involved (each replacement requires a search, a CDN-URL resolve, a collision check against every ID already on the site, and a visual fetch-then-Read verification): the "prek classroom" photo (`1777056491418`, on `index.html` + `programs.html`, gallery.html kept as canonical), "music/movement" (`1779398422654`, on `home-2.html` + `teachers.html`), "healthy snacks" (`1777056481869`, on `home-2.html`), "play kitchen" (`1763310225537`, on `home-2.html`), "toddler room" (`1601339434203`, on `home-2.html`), and the `index.html` age-tab panel images that mirror `programs.html`'s infant/toddler/Pre-K photos (`1560307815`, `1558576997`, `1709301264789`) — `programs.html` was treated as canonical owner for those three since it's the more detailed, dedicated page. The Infant tab photo was later swapped twice more per follow-up requests (final: `1593793373220`, a baby on a colorful sensory play mat) — each swap re-verified via the fetch-then-Read technique and re-checked against every ID on the site before landing.

### Horizontal scroll strips unusable with a desktop mouse
Several sections (`index.html`'s facility gallery preview, `home-2.html`'s daily-activities strip, `about.html`'s milestones timeline, `gallery.html`'s testimonial strip) use `overflow-x-auto no-scrollbar` — deliberately hiding the scrollbar for a cleaner look. This left desktop mouse users with **no way to reach the overflow content at all**: a mouse only produces a vertical wheel delta, which just scrolls the *page* past the section, and there's no visible scrollbar to drag since it's hidden by design. Touch and trackpad users never noticed since horizontal swipe gestures work natively. Confirmed via a Playwright test *before* fixing: `scrollWidth` (1424px) genuinely exceeded `clientWidth` (1280px) — extra content existed and was permanently unreachable by mouse.
- **Fix, centralized in `main.js`/`style.css` rather than per-page** (`initHorizontalScrollStrips()`, applied via the generic `.overflow-x-auto.no-scrollbar` selector so it automatically covers every current and future strip using this pattern, no per-page wiring needed): a `wheel` listener redirects vertical wheel delta into `scrollLeft` and calls `preventDefault()` so the page itself stops scrolling while hovering a strip with room left to scroll; a `mousedown`/`mousemove`/`mouseup` set adds click-and-drag scrolling (`cursor: grab` → `grabbing` via a `.is-dragging` class, which also sets `pointer-events: none` on children mid-drag so a card's own hover/link states don't fight the drag); a `moved` flag suppresses the trailing `click` event after an actual drag so dragging a strip never accidentally activates a link/card underneath the cursor, while a plain (non-drag) click still navigates normally.
- **Lesson: `overflow-x-auto` + `no-scrollbar` is a mobile-first pattern that silently fails for desktop mouse users** — if a horizontal-scroll strip needs a hidden scrollbar for visual cleanliness, it needs the wheel-redirect + drag-to-scroll pair (or explicit prev/next buttons) from the start, not just on mobile where native swipe already works. Apply `initHorizontalScrollStrips()`'s selector-based approach on future templates too, since it fixes every matching strip site-wide with zero per-instance markup changes.

---

## Template 07 — Local Gym & Fitness Center

- **Brand name:** Momentum Fitness Club
- **Tagline:** "Your Streak Starts Today." (Home 1) / "Built on Consistency." (Home 2)
- **Business category:** Neighborhood gym & fitness center (Hawthorne District, Austin, TX) — public marketing site **plus** the registry's first authenticated **member dashboard** (group-class booking, membership renewal countdown, attendance/check-in streak tracking) — no backend, state persisted entirely in `localStorage`
- **Primary color:** Volt `#c9fc3c` (500) — CTAs, active states, progress bars, streak-ring fill
- **Secondary color:** Deep Ink `#14151a` — headlines, dark sections, header/footer/mobile-menu base (doubles as the brand's near-black, not a tinted brown/navy/charcoal like any prior template)
- **Accent:** Ember `#ff5a36` — streak/flame iconography, urgency badges ("Live Now", spots-left counters)
- **Neutral / background:** Chalk `#f6f6f2` (light), Graphite `#0e0f13`→`#1b1d24` (dark) — a plain neon-on-monochrome system, deliberately distinct from every prior template's tinted-neutral base (T1 cream/choco, T2 cream/indigo, T3 kraft/forest, T5 warm-neutral/near-black, T6 paper-cream/aubergine)
- **Font combination:** Unbounded (bold geometric-black display, headings/buttons) + Manrope (body) — a "heavy impact display + clean grotesque body" pairing not used by any prior template (T1 serif+script+sans, T2 rounded Baloo+Quicksand, T3 angular Space Grotesk+Inter, T5 Sora+Inter, T6 Bricolage Grotesque+Karla — Unbounded's blocky geometric weight is a different register from all of them)
- **Header type:** **Floating capsule/pill nav** — a fully detached, rounded-full bar with margin on every side (`position:fixed`, `border-radius:999px`, `backdrop-filter:blur`), not touching the viewport edges; compacts its top offset/shadow on scroll via an `is-docked` class. A structural first for this registry (T1 = unified single row flush to the top edge, T2 = two-level utility-bar, T3 = three-tier marquee+scroll-spy chips, T5 = right-aligned glass bar flush to the edge, T6 = centered-logo split-nav with a utility strip — none are a detached floating shape). Desktop nav gates at `xl` (1280px) per the site-wide lesson, applied from the start.
- **Navigation style:** Logo (shrink-0) → centered nav group (`flex:1`, `justify-center`) → theme/RTL toggles + Login link + "Join Now" CTA + hamburger (shrink-0). Only "Home" carries a dropdown (Home 1 / Home 2, click-always-opens / mouseleave-or-outside-click-or-Escape-closes, per T1–T6's shared click-vs-hover lesson). 7 links: Home, About, Classes, Trainers, Membership, Gallery, Contact — FAQ reachable via footer + mobile menu only.
- **Hero layout (Home 1):** **Diagonal-split hero** — solid-ink text panel (left) beside a photo panel clipped by a CSS `clip-path` diagonal edge (right, RTL-mirrored polygon), with a **floating circular "streak-ring" card** (conic-gradient progress ring + flame stat) overlapping the diagonal seam — a functional teaser of the dashboard's streak feature baked directly into the marketing hero, not a stats-counter card (T1), a licensed-badge card (T2), a product-spotlight card (T3), a glass price-match card (T5), or a pickup-wait stat card (T6). A stat-trio with vertical dividers sits below the CTAs.
- **Hero layout (Home 2):** **Editorial full-bleed** — a single HD photo fills the section edge-to-edge with a dual gradient scrim, an oversized uppercase display headline with one `<em>` (volt-colored, non-italic) word, and a **stat ticker marquee** (auto-scroll bidi-aware strip) directly beneath the hero rather than inside it — the site's second distinct hero mechanic (photo-clip-path-diagonal vs. full-bleed-editorial).
- **Hero layout (inner pages):** Compact banners, no two identical — photo+dual-gradient overlay (About, Trainers, Contact), solid ink + diagonal hairline-stripe pattern (Classes, Gallery), solid volt (Membership, FAQ).
- **Section order (Home 1):** Header → Hero (diagonal split + streak-ring) → Trust icon strip → Why Momentum (6 `card-surge` icon cards) → **Today's Class Highlights** (live "Happening Now / Next Up" schedule widget, 4 cards, no photos — functional not decorative) → Membership preview (3-card pricing) → Trainers strip (horizontal scroll-snap) → Facility Gallery preview (asymmetric grid) → Testimonials (dark section, scroll-snap quote cards, colored-initial avatars — no photographed avatars anywhere on the site) → **Dashboard/App teaser** (live mock dashboard panel: streak badge, renewal progress bar, attendance heatmap, a booked-class row — CTA to Create Free Account) → Final CTA (solid volt band) → Footer.
- **Section order (Home 2):** Header → Hero (editorial full-bleed) → Stat ticker marquee → Facility Features (image+checklist split) → **Class Categories bento** (asymmetric grid: 1 large 2-row tile + 5 standard tiles, gradient-overlay captions, links into `classes.html#anchor`) → **Membership comparison table** (Basic vs. Premium checklist rows — a structurally different device from Home 1's 3-card pricing) → **Trainer Spotlight** (single large bio + credential badges, not a strip) → **Community Wall** (4-photo grid, high-fives/coaching/circle-huddle/pickup-basketball) → Testimonials (video-quote-style cards: photo + centered play-icon overlay + pull-quote — a third distinct testimonial treatment alongside Home 1's scroll-snap strip and the site's plain quote cards) → **Streak Leaderboard teaser** (gamification: ranked mini-table tying directly into the dashboard's streak concept) → Final CTA (diagonal volt gradient band) → Footer.
- **Card style:** `.card-surge` — a **clip-path angular cut corner** (top-right notch, RTL-mirrored to top-left) with a 4px colored top accent bar and a `box-shadow` **pulse-ring outline** that fades in on hover (not a blur/glow) — sharp/angular geometry, mechanically and visually distinct from every prior card motif (T1 scalloped-edge, T2 thick-border+tape-pin sticker, T3 die-cut price-tag notch, T5 soft rounded glow-lift, T6 folded-paper-ribbon corner).
- **Button style:** `.btn-surge` — angular beveled-corner clip-path (small cut top-left/bottom-right, echoing the card's cut-corner language) with a **bottom-to-top fill rise** on hover (`::before` `translateY(101%→0)`, text color inverts) — a vertical-rise mechanic distinct from T1's gradient lift+glow, T2's flat-bottom-shadow press, T3's slide-arrow/wipe-fill, T5's diagonal shine-sweep, T6's paper-fold-unfurl.
- **Image style:** Diagonal clip-path hero photo (Home 1), full-bleed editorial photo (Home 2), asymmetric bento class-category grid, horizontal scroll-snap trainer/testimonial/milestone strips, uniform non-spanning `aspect-square` gallery grid with hover-caption scrims (GLightbox), video-quote-style testimonial cards with a play-icon overlay. No masonry, no blob-masks, no polaroid frames, no flip-cards, no die-cut price tags.
- **Background style:** Plain chalk/graphite neutral canvas, no repeating site-wide texture motif (no dot-grid, no confetti, no pegboard, no grain overlay) — texture is reserved entirely for the `page-banner-stripe` diagonal hairline pattern on select inner-page banners, keeping the "athletic HUD" identity clean rather than decorative.
- **Animation style:** **Native CSS scroll-driven animations** (`animation-timeline:view()`, `@supports`-gated) as the primary reveal engine, with a JS `IntersectionObserver` fallback (`reveal` → `reveal-io`/`is-visible` swap) for browsers lacking support — a reveal mechanism never used by any prior template (T1 = AOS, T2 = GSAP+ScrollTrigger, T3 = vanilla IntersectionObserver as primary, T5 = ScrollReveal.js, T6 = vanilla IntersectionObserver as primary). Paired with **Lenis** for inertial smooth-scroll (first use on this site). A CSS `conic-gradient` streak-ring (no JS charting library), a bidi-aware CSS `@keyframes` marquee ticker, a hand-rolled `requestAnimationFrame` count-up (`data-count-to`, distinct instance from T3's and T6's own hand-rolled counters), and `initHorizontalScrollStrips()` (wheel-redirect + drag-to-scroll, reused verbatim per the T1/T2 lesson) on every scroll-snap strip. GLightbox on the gallery only, all anchors tagged `type: image` per the site-wide lesson.
- **Footer layout:** `.footer-track` — dark ink panel with a **repeating diagonal chevron "track-line" top edge** (`repeating-linear-gradient`, volt-colored), evoking running-track lane markings — a top-edge motif distinct from every prior template (T1 scallop, T2 wave, T3 straight diagonal-stripe, T5 rounded corners, T6 perforated ticket-tear). Standard 4-column grid (brand+social / explore / membership / visit-us+hours).
- **Mobile menu pattern:** **Radial circle-reveal** — a full-screen menu whose `clip-path: circle()` grows from the hamburger button's exact screen coordinates (JS sets `--origin-x`/`--origin-y` from the button's `getBoundingClientRect()` on open), rather than sliding or fading in — a geometry-driven mechanic not used by any prior template (T1 right slide-in drawer, T2 full-screen fade+scale, T3 bottom-sheet, T5 right glass-backdrop drawer, T6 left logical-property drawer). "Home" renders as a native `<details>` accordion inside it.
- **Special design features (incl. the site's first member dashboard):** Dark/light mode + RTL/LTR toggles (localStorage, `data-theme` + `dir`); **`dashboard.html`** — single-page authenticated app view (guarded by a `momentum_session` localStorage flag, redirects to `login.html` if absent) with: a **conic-gradient streak ring** + current/best streak + an "at risk" nudge badge, a **membership countdown** (days-to-renewal + billing-cycle progress bar computed from a stored `renewalDate`), a **6-week attendance heatmap** (CSS grid of `data-level`-shaded cells built from a check-in date array), a **"Check In Today"** button that appends today's date and live-recomputes the streak, a **class-booking grid** (13 real weekly slots, live capacity = simulated baseline + the user's own booking, toggle-to-book/cancel) shared verbatim with `classes.html`'s public timetable data, and an **upcoming-bookings** list + **check-in history table** rendered from the same `localStorage` records; `login.html` includes a one-click **"Use Demo Account"** button that seeds a realistic 12-day-streak member so the dashboard can be explored without registering; `register.html` reads a `?plan=` query param (linked from every pricing card) to pre-select the membership dropdown; a shared `WEEKLY_CLASSES` dataset keeps the marketing timetable (`classes.html`) and the bookable dashboard grid numerically consistent; class category filter (`classes.html`, 7 chips) and gallery category filter (`gallery.html`, 4 chips, uniform non-spanning grid per T6's "never leaves a gap" lesson) share one filter pattern; FAQ live search across 4 topic-grouped accordions; full custom form validation (contact/login/register) with password-visibility toggles and `data-min-today` date-floor helper; graceful `onerror`→`ph-img` image fallback site-wide; a chevron/flame-themed 404 with a text-outline "404" numeral.
- **Pages built:** index.html (Home 1), home-2.html (Home 2), about.html, classes.html (filterable class cards + full weekly timetable), trainers.html, pricing.html (3 plans + annual + comparison-adjacent FAQ), gallery.html (12-photo filterable GLightbox grid), faq.html (topic-grouped + live search), contact.html (form + map + hours), login.html, register.html, dashboard.html (member app), 404.html. No dedicated `service-details`/`portfolio`-style pages — not relevant to this business.
- **Libraries used:** Tailwind CSS (CDN, class dark-mode strategy), Font Awesome 6 (icons), Google Fonts (Unbounded/Manrope), **Lenis** (smooth scroll — first use on this site), native CSS scroll-driven animations with a vanilla IntersectionObserver fallback (no AOS/GSAP/ScrollReveal — first template with zero JS scroll-reveal library dependency for browsers that support the CSS feature), GLightbox (gallery only). No Swiper, no Typed.js, no CountUp.js — carousels are custom scroll-snap strips and stat counters are a hand-rolled `requestAnimationFrame` tween. No build step; no backend (member data is 100% `localStorage`, clearly a front-end simulation).

**Do not reuse for future templates:** floating detached capsule/pill header, volt-lime/ink/ember neon-on-monochrome palette, Unbounded+Manrope pairing, diagonal clip-path split-hero with an overlapping conic-gradient streak-ring card (Home 1), full-bleed editorial hero with a sub-hero stat ticker marquee (Home 2), `.card-surge` angular cut-corner + pulse-ring-outline component, `.btn-surge` bottom-to-top fill-rise button mechanic, chevron/speed-stripe `.footer-track` top edge, radial `clip-path: circle()` mobile-menu reveal anchored to the hamburger button's coordinates, native CSS `animation-timeline: view()` as a site's primary reveal engine, Lenis smooth-scroll, conic-gradient streak-ring stat device, 6-week CSS-grid attendance heatmap, live capacity class-booking grid pattern, membership renewal countdown + billing-cycle progress bar, "Use Demo Account" one-click seed-login pattern, `?plan=` query-param pricing→registration handoff, class-category bento grid with anchor deep-links (Home 2), streak-leaderboard gamification teaser (Home 2), video-quote-style testimonial cards with a play-icon overlay (Home 2).

### Image sourcing & duplication audit (applied from the start, per T1–T6's registry lessons)
Every photo was sourced via `WebFetch` on Unsplash search-results pages to obtain real long-form `images.unsplash.com/photo-<id>` URLs (never `plus.unsplash.com`, never a short page-slug), paired with actual alt-text. All hero images, all 6 trainer portraits, and every image reused across more than one page were additionally verified with the fetch-then-Read technique (`WebFetch` the raw CDN URL — it saves the binary to a local `tool-results/*.jpg` even though it can't describe it — then `Read` that file to see the actual photo before committing to it).
- **Three candidates were caught and rejected during verification, not after:** a "group gym class" photo whose participants' shirts and equipment tags repeatedly read "GOLD'S GYM NEPAL" (a real, named competing gym brand — same class of defect as T3's "Riverside Market"/"Mercadona" storefront lesson, just on apparel instead of signage); a "coaching consultation" photo whose subject wore a clinic polo embroidered "Vivir sin dolor" with a logo (a real physical-therapy business identity); and a TRX-training photo with "TRX" repeated across multiple straps plus a visible Nike shoe logo. All three were swapped for clean, brand-free replacements before the pages were considered done.
- **Cross-page reuse policy (continuing T2/T3's precedent):** a small set of facility photos legitimately recurs on exactly 2 pages where the same subject belongs in both (e.g. a class-type photo shared between `classes.html` and the matching Home 2 bento tile; a facility photo shared between `gallery.html` and a homepage preview grid). A post-build `grep`-based sweep confirmed **zero photo IDs appear on more than 2 pages** — the first pass of `index.html`'s gallery-preview grid actually failed this check (3 IDs shared across `index.html` + `home-2.html` + `gallery.html`), and those 5 tiles were re-sourced and re-verified rather than left as a 3-page violation. 35 unique verified photo IDs across the site, all HTTP-servable `images.unsplash.com` URLs, zero `plus.unsplash.com`, zero placeholder/lorem text (confirmed via `grep`).
- **Trainer portrait sourcing note:** many "fitness trainer portrait" search results returned bra-top/shirtless bodybuilder-pose stock photography unsuitable for a professional "meet the team" bio grid; the 6 trainer photos actually used were deliberately selected for being fully clothed, professionally lit, and smiling/approachable rather than physique-focused, to match the neighborhood-gym (not bodybuilding-gym) brand tone.

### Verification performed before sign-off
`node -c assets/js/main.js` (syntax valid); a `grep`-based sweep of internal `href="*.html"` targets confirmed every link resolves to one of the 13 shipped pages with zero dead links; `<div>`/`<section>`/`<a>`/`<header>`/`<main>`/`<footer>` open/close tag counts balanced on all 13 pages; `<!DOCTYPE html>` and a single `</html>` confirmed present on every page; no `TODO`/lorem-ipsum/placeholder text found site-wide. **Note on this template's verification scope:** unlike several prior templates in this registry, this build was not interaction-tested in a live headless-Chrome/Puppeteer browser session (no dev server or browser-automation tool was invoked this pass) — responsiveness, the radial mobile-menu reveal, the localStorage-driven dashboard (booking/streak/countdown), and dark-mode/RTL toggling were verified by careful code review against the same patterns and lessons validated by T1–T6's own browser-tested builds (global `overflow-x:hidden`, `xl`-gated desktop nav, transform-based skip-link, `justify-between` bidi-safe header edge-pinning, pointer-capture excluded from filter-chip containers), not by an actual rendered-browser pass. A live visual/interaction test pass is recommended before this template is presented as fully sign-off-verified.

### Revision pass — dark-mode color-token bug, button redesign, RTL polish, equal-count grids
A direct follow-up requested three things: refine the theme colors (keep the volt/ink/ember identity), replace the angular cut-corner buttons with a rounded pill shape, and make sure `classes.html`/`gallery.html` show an equal number of cards in every filter category. Auditing the color request surfaced a real pre-existing bug worth recording.

1. **`--ink`/`--chalk` dark-mode contrast bug (found during the color audit, not reported by the user):** both tokens were dual-purposed and collided under `data-theme="dark"`. `--ink` was used everywhere as a *permanent* dark surface color (footer, hero-split, ticker, page-banner-ink, mobile menu, dash-side, brand-mark, `.card-surge-dark`, `.btn-surge-dark`) — but the dark-mode block also redefined `--ink:#f6f6f2` (near-white), so every one of those "always-dark" bands would flip to a *light* background in dark mode. Simultaneously `--chalk` was used both as the adaptive page-background token (correct) **and** as the "light text sitting on a dark band" color in those same components — but `--chalk` itself flips to near-black in dark mode, so the text would go dark-on-dark at the same time the background went light-on-light. Net effect: the footer, the diagonal-split hero, the radial mobile menu, the stat ticker, `dashboard.html`'s sidebar, and six inline `background:var(--ink);color:var(--chalk)` section bands (About/FAQ/Home 2/Home 1/Pricing) would all have rendered as low/zero-contrast in dark mode. **Fix:** added a new permanent token `--brand-light:#f6f6f2` (never redefined in either dark-mode block, alongside `--ink` which simply had its two dark-mode redefinitions deleted so it stays `#14151a` in both themes); every `color:var(--chalk)` usage that meant "light text on a permanently-dark surface" was repointed to `color:var(--brand-light)` (9 CSS rules + the 6 inline HTML bands). `--chalk` itself was left fully reactive for its one legitimate use (`body{background:var(--chalk)}` and the streak-ring-card's page-matching border). Also fixed `.badge-ember`'s dark-mode contrast (`--ember-dark` text was too low-contrast on its own translucent chip in dark mode; added a `[data-theme="dark"]` override to a brighter `--ember`). **Lesson for future templates: a color token that's reused for two different *semantic* purposes (an adaptive one and a fixed one) is a latent dark-mode bug even if it looks correct in light mode — grep every `var(--token)` usage site-wide before assuming a token is safe to redefine per-theme.**
2. **Button redesign:** `.btn`/`.btn-surge`/`.btn-surge-dark`/`.btn-outline` were rebuilt from the angular clip-path + bottom-rise `::before` fill (documented in the original entry above) to a fully rounded pill (`border-radius:999px`) with a lift-on-hover (`translateY(-3px)`) plus a colored glow `box-shadow` and a `filter:brightness()` tweak — no background/color/border swap on hover at all. This was a deliberate choice over swapping `background`/`color` in the `:hover` rule: several `.btn-outline` instances across the site carry an inline `style="color:...;border-color:..."` override (for outline buttons sitting on dark hero photos), and inline styles always beat an external stylesheet's `:hover` rule for the same property — a hover effect built from `transform`/`box-shadow`/`filter` instead is immune to that conflict and looks correct everywhere without auditing every inline override individually. The card component's own angular cut-corner (`.card-surge`) was intentionally left unchanged — only buttons moved to the rounded pill language, so the site now deliberately contrasts sharp cards against soft pill CTAs rather than matching them.
3. **Filter-chip active-state bug (found while touching button styles):** `classes.html`'s and `gallery.html`'s category filter chips only ever toggled an `.is-active` class in `main.js`, but no CSS rule styled `.is-active` — so clicking a different chip never visually indicated which filter was selected (only the initial "All" chip, hardcoded with `.btn-surge`, ever looked active). Fixed in `initClassFilter`/`initGalleryFilter` by swapping the actual `.btn-surge`/`.btn-outline` classes alongside `.is-active` on every click, reusing the two button variants that already exist rather than adding a third `.is-active` style.
4. **RTL audit (code-review level, no live browser available this pass):** confirmed the existing bidi handling holds up — `justify-between` header edge-pinning, logical properties (`inset-inline-*`, `border-inline-start`) on the dash sidebar/stat-divider/pw-toggle, the explicit `[dir="rtl"]` overrides on `.hero-split-media`'s clip-path, `.card-surge`'s clip-path, and `.streak-ring-card`'s absolute position, the ticker's separate RTL keyframe, and the radial mobile-menu's coordinate-based (not CSS-direction-based) origin all check out without changes, since CSS Grid/Flexbox track order already inverts automatically under `dir="rtl"` per spec. Two real gaps were found and fixed: breadcrumb `fa-chevron-right` icons (7 pages) and two standalone "view all" arrow links (`dashboard.html`, `pricing.html`'s FAQ link) had no mirroring at all — added `[dir="rtl"] .breadcrumb i` and a new `.rtl-flip` utility class (`transform:scaleX(-1)`), applied to those two links. The day-of-week timetable's left-to-right column order was deliberately left unmirrored under RTL — reversing a calendar's day sequence is a genuine convention choice (real RTL sites are split on it), not a bug.
5. **Equal-count filter grids:** `classes.html` had an uneven 2/1/1/1/1/2 split across Yoga/Dance/HIIT/Cycling/Strength/Recovery (8 cards); `gallery.html` had an uneven 4/2/4/2 split across Training/Studios/Amenities/Community (12 photos). Rather than trimming down to the thinnest category (which would have cut real content), both were brought up to the richest existing count: **4 new class cards** (Tabata Sprint/HIIT, Latin Cardio Jam/Dance, Endurance Ride/Cycling, Kettlebell Power/Strength) bring `classes.html` to a clean 2-per-category × 6 = 12, and **4 new gallery photos** (2 Studios: Reformer Pilates Studio, Mirror Studio; 2 Community: Game Night High-Five, Friends Catching Up) bring `gallery.html` to 4-per-category × 4 = 16. The 4 new classes were also added to `main.js`'s shared `WEEKLY_CLASSES` dataset (so they're bookable from `dashboard.html`, not just decorative cards) and to the weekly timetable widget. The timetable itself was restructured from a flat 7-column CSS Grid (which assumed exactly one row per class slot per day and couldn't cleanly absorb the new, unevenly-distributed session times) to **7 independent per-day flex-column cells inside the outer grid** — each day now stacks its own classes in chronological order regardless of how many it has, which is what let Wednesday/Thursday/Saturday/Sunday grow to 3, 3, 3 and 2 sessions respectively without any empty placeholder cells or misordered rows.
6. **New images (8), sourced and visually verified via the established `WebFetch`-raw-image → `Read`-saved-file technique before use, each checked against the full site-wide photo-ID ledger:** a kettlebell mid-swing in chalk dust; an aerial shot of sprinters on a track; a moody single-bike studio product shot (Endurance Ride); a K-pop-style dance-rehearsal stretch shot (Latin Cardio Jam); a reformer-Pilates studio; a mirrored group-fitness studio with exercise balls; four older men high-fiving after badminton; two women high-fiving and laughing outdoors after a workout. **Rejected during this pass** (same discipline as the original build, all caught via the fetch-then-Read technique rather than trusted from alt-text): a "group gym class" photo with "GOLD'S GYM NEPAL" repeated across shirts and equipment tags; a coaching photo whose subject wore a shirt embroidered with a real physical-therapy clinic's name and logo; a TRX-training photo with "TRX" printed multiple times plus a visible Nike logo; a dance-crew photo with a "JUST DO IT" Nike hoodie and Adidas three-stripe pants; a group-of-men gym photo with visible Adidas branding; a rock-climbing high-five photo with a legible "The North Face" logo; and an inappropriately-framed stationary-bike photo. **A second-order lesson from this pass:** the very first draft of `index.html`'s gallery-preview grid (built in the original session) had actually reused 3 gallery photos that were also used on `home-2.html`, putting them on 3 pages at once — caught only by re-running the same `grep`-based duplication sweep documented in the original build, not by memory of what was already placed; that sweep is worth re-running after *any* image-touching edit, not just treated as a one-time launch check.

---

## Template 03 — Local Grocery Store

- **Brand name:** GreenCart Local Market
- **Tagline:** "Fresh Groceries, Local Roots."
- **Business category:** Local/neighborhood grocery store (produce, dairy, bakery, pantry, beverages, household — no full e-commerce checkout, no dashboard)
- **Primary color:** Basil Green #16653E / #2E8F57 (500) — CTAs, brand
- **Secondary color:** Heirloom Tomato #E8542A (500) — discount ribbons, sale accents
- **Accent:** Golden Wheat #E09E33 — star ratings, badge highlights, footer top divider
- **Neutral / background:** Kraft cream #FBF8F2 (light), Forest charcoal #0F1712–#1B281F (dark) — deliberately not brown/choco (T1) or navy/indigo (T2)
- **Font combination:** Space Grotesk (geometric grotesk, headings/display) + Inter (body) — a 2-font pairing, but structurally distinct from T2's Baloo 2 + Quicksand (both rounded): Space Grotesk is angular/technical, giving a "clean, practical" retail feel per the brief rather than playful or ornate.
- **Header type:** **Three-tier header**, a new structural pattern for this site: (1) a non-sticky top strip combining a CSS `@keyframes` marquee ticker (scrolling offers/announcements, pauses on hover, disabled under `prefers-reduced-motion`) with live store-hours/phone info — scrolls away with the page, not fixed; (2) a smart-scroll (hide-on-scroll-down/show-on-scroll-up) main row: logo-left, centered nav (`flex-1`, gated at `xl` per T1/T2's shared lesson), a **click-to-expand inline search overlay** that swaps the nav row for a full-width search input + live-filtered results dropdown (not a separate page or modal), theme/RTL toggles, a "My List" icon with a localStorage item-count badge, hamburger; (3) a **sticky secondary category-chip bar** (Produce/Dairy/Bakery/Pantry/Beverages/Household/Offers) with IntersectionObserver scroll-spy active-states — this bar *is* the site's primary product navigation (grocery categories matter more than page links for this vertical), distinct from both T1's single unified row and T2's two-level utility-bar-plus-nav.
- **Navigation style:** Centered nav group (Home dropdown: Home 1/Home 2, About, Categories, Offers, Contact, FAQ) + the category chip bar described above as a second, parallel navigation system. Contact and Offers stay directly visible per the master prompt's rule.
- **Hero layout (Home 1):** Split layout — left: eyebrow badge, headline, inline quick-search bar (not just a CTA), dual buttons, three inline stat pill-badges (icon + count-up number, no vertical dividers — deliberately not T1 Home 2's stat-trio-with-dividers pattern). Right: **2×2 photo collage grid** (not a single full-bleed photo, not a blob-mask) with a rotated "Save up to 25%" sticker badge and a floating "Today's Pick" product-spotlight card (image+name+price+add-to-list) overlapping the bottom-left corner — a commerce widget, not a stats counter (T1's floating card reserved for CountUp stats).
- **Hero layout (Home 2):** Full-bleed photo banner + dark gradient overlay, centered text, a prominent white search bar embedded directly in the hero, and a **quick-category "dial"** — a rounded card of 5 circular category icons overlapping the hero's bottom edge — the site's third distinct hero mechanic.
- **Hero layout (inner pages):** Compact banners (h-56 to h-72) with breadcrumb, each using a different verified photo (aisle gondola, checkout counter, supermarket aisle, produce display) with a dark gradient overlay — no repeated crop/treatment across pages.
- **Section order (Home 1):** Header (3-tier) → Hero (collage) → Trust/USP icon strip → Shop by Category (6 uniform image cards) → This Week's Offers (countdown timer + Swiper carousel of price-tag cards) → Fresh Produce Spotlight (horizontal scroll-snap strip) → Why Shop With Us (image+stat-badge / checklist split) → Store Hours & Location (Google Maps iframe embed + live-updating hours table + address card) → Testimonials (static 3-card grid, star ratings — no carousel) → Newsletter + Bulk-Order promo (2-panel split) → Footer.
- **Section order (Home 2):** Header → Hero (full-bleed + category dial) → Today's Highlights pill band (static, not a marquee — distinct from the header's marquee) → Featured Categories (asymmetric bento grid: 1 tall + 2×2) → Deal of the Day (single spotlight card with its own countdown, not a multi-card carousel) → Browse by Aisle (numbered "01–06" horizontal store-directory cards, `aisle-card` component with a bidi-aware colored border) → Meet Our Partner Farmers (3-card bios, dark section) → Bulk & Business Orders promo (checklist + image split) → Store Info compact 3-column strip → Testimonials (Swiper single-spotlight fade — the site's second distinct testimonial treatment) → Final CTA (centered gradient band) → Footer.
- **Card style:** `tag-card` — rounded-xl, thin 1px border, a CSS `clip-path` **die-cut price-tag notch** on one top corner with a small circular "hole" punched into it (mirrors for RTL), flat `translateY(-6px)` + shadow lift on hover (no glow, no rotation, no thick border — mechanically distinct from T1's scalloped `cake-card` and T2's rotated/tape-pinned `sticker-card`), diagonal `tag-ribbon` discount banner for sale items.
- **Button style:** `btn-fresh` — solid rounded-lg (not pill, not chunky-flat-shadow) fill; on hover the background darkens and a trailing icon slides via `translateX` (mirrors for RTL, `rotate(180deg)` on the arrow glyph). Outline variant uses a `::before` pseudo-element that wipes in as a solid fill from the leading edge on hover (`scaleX`, `transform-origin` flips for RTL) — a third distinct button mechanic alongside T1's gradient lift+glow and T2's flat-bottom-shadow press.
- **Image style:** 2×2 photo-collage hero (Home 1), full-bleed + overlapping category-dial hero (Home 2), uniform image category cards with gradient overlay + hover-zoom, horizontal scroll-snap produce/aisle/farmer strips, price-tag notch product cards. No masonry gallery, no polaroid frames, no blob-masks, no flip-cards, no lightbox (kept deliberately light for a "clean, practical" grocery site per the brief).
- **Background style:** Kraft-cream base with a subtle fine-line **graph-paper/dot-grid texture** (`texture-grid`, linear-gradient hairlines) — a third distinct light-mode texture concept, structurally different from T1's radial sprinkle-dot pattern and T2's confetti-dot pattern (lines vs. dots in both cases). Dark mode uses solid forest-charcoal panels, no texture.
- **Animation style:** **Vanilla IntersectionObserver** scroll-reveal (`data-reveal` / `data-reveal="left|right|scale"`, no external library) — a third distinct reveal engine alongside T1's AOS and T2's GSAP+ScrollTrigger; CSS `@keyframes` marquee ticker (bidi-aware, reduced-motion safe); a hand-rolled `requestAnimationFrame` count-up for stats (not CountUp.js, not a GSAP tween — a third distinct counter technique); a real-time weekly-offers countdown timer (`setInterval`, resets to the coming Sunday); a live "Open Now / Closed" store-status indicator computed from the actual client clock against a stored weekly-hours table; Swiper 11 used narrowly (offers carousel + Home 2 testimonial fade only — not the site's primary animation engine). `initHorizontalScrollStrips()` (wheel-redirect + drag-to-scroll for mouse users) reused from the T1/T2 lesson on every `.scroll-strip.no-scrollbar` instance site-wide.
- **Footer layout:** Dark forest-green, **straight-edge top divider** (a repeating gold/tomato diagonal-stripe bar, `.footer-divider`) — deliberately not scalloped (T1) or wave-curved (T2). 4-column grid: brand+live-status-badge+social / shop categories / customer care / store info+directions button. Bottom bar: copyright + payment-method icons.
- **Mobile menu pattern:** **Bottom-sheet drawer** — slides up from the bottom (`translateY`, rounded-t-3xl, drag-handle bar), containing search input, nav links (Home as a native `<details>` accordion, no JS needed), a 3×2 category-icon grid, theme/RTL toggle row, and a bulk-order CTA. A fourth distinct mobile-menu mechanic alongside T1's right slide-in drawer and T2's full-screen fade+scale overlay.
- **Special design features:** Dark/light mode toggle (localStorage), RTL/LTR toggle (localStorage), live open/closed status badge (real-time clock vs. a JS weekly-hours table, reused in the top strip, Store Hours section, Home 2's info strip, and the footer), real-time countdown timers (offers banner + Home 2's Deal of the Day, both resolving to the same coming-Sunday deadline), sticky category chip-bar with IntersectionObserver scroll-spy (shared between the header nav and `categories.html`'s own in-page anchors), click-to-expand header search overlay with a small client-side product index and live-filtered results dropdown, quantity steppers + a persisted "My List" badge count (`localStorage`, `data-add-to-list` / `data-stepper`) used across the offers carousel and every category product card, a **dynamic Bulk & Special Order form** (`contact.html#bulk-order`) with add/remove item rows, per-row quantity + a live estimated-subtotal calculator, and a `min=tomorrow` delivery-date field set via JS on load; a Google Maps `output=embed` iframe (no API key) on both Home 1 and Contact; a general contact form with full custom validation (name/email/phone/message + live character counter); an FAQ page with five independent single-open accordion groups by topic; graceful `onerror` image fallback (`ph-img` + `initImageFallback()`, same technique as T2) on every photo; a grocery-themed 404 ("This aisle is empty," cart icon + question-mark badge, live product/page search box).
- **Pages built:** index.html (Home 1), home-2.html (Home 2), about.html, categories.html (full 6-aisle product directory with steppers/add-to-list), offers.html (countdown + category-filterable discount grid), contact.html (map + hours + general form + bulk-order form), faq.html, 404.html.
- **Libraries used:** Tailwind CSS (CDN, custom `[data-theme="dark"]` selector strategy), Font Awesome 6 (icons), Google Fonts (Space Grotesk + Inter), Swiper 11 (offers carousel on Home 1 + testimonial fade on Home 2 only — no AOS, no GSAP, no GLightbox, no Typed.js, no CountUp.js). No build step; no backend.

**Do not reuse for future templates:** three-tier header (marquee-info strip + smart-scroll main row + sticky scroll-spy category-chip bar), click-to-expand inline header search overlay with a live results dropdown, basil-green/tomato/gold/kraft-cream palette, Space Grotesk + Inter pairing, 2×2 photo-collage hero with a floating product-spotlight card (Home 1), full-bleed hero + overlapping circular category-dial (Home 2), fine-line graph-paper background texture, price-tag die-cut notch `tag-card` component, wipe-fill `btn-outline` mechanic + slide-arrow `btn-fresh` mechanic, bottom-sheet mobile menu, straight-edge diagonal-stripe footer divider, vanilla-IntersectionObserver scroll-reveal as the site-wide animation engine, hand-rolled `requestAnimationFrame` count-up technique, live open/closed store-status pattern, numbered "01–06" `aisle-card` store-directory strip (Home 2), asymmetric bento featured-categories grid (Home 2), single-spotlight Deal-of-the-Day card with its own countdown (Home 2), dynamic multi-row bulk-order form with live subtotal + JS-set `min=tomorrow` date field.

### Image sourcing & verification (applied from the start, per T1/T2's registry lessons)
Every photo was sourced via `WebFetch` on Unsplash search-results pages to get real `images.unsplash.com/photo-…` URLs paired with actual alt-text, not recalled from memory. A representative sample of the most prominent images (hero collage tiles, storefront/interior banners, team photos) was further verified using the fetch-then-Read technique documented in T2's registry (`WebFetch` the raw CDN URL — it silently saves the binary to a `tool-results/*.jpg` file even though it can't describe it — then `Read` that saved file to see the actual photo before committing to it). All spot-checked images matched their captions.
- **Caught and fixed during this pass, not after:** two storefront/exterior photos initially chosen for the About and Contact page heroes turned out to prominently display a *different*, real business's signage ("Riverside Market," "Mercadona") baked into the photo. Since the brand in this template is "GreenCart Market," a hero photo with a competing store name reads as a visible content-accuracy defect, not just a stylistic mismatch. Both were swapped — About's hero to an unbranded aisle/produce-display photo, Contact's hero to an unbranded checkout-counter photo — before the pages were ever marked complete. **Lesson for future templates: real estate/storefront stock photography frequently has another business's actual name or logo legible in the shot — treat any photo with readable third-party signage as unusable for a hero/banner on a fictional-brand page, the same way a caption/content mismatch would be treated, and check for it specifically since it won't show up in a simple "does this photo depict a grocery store" content check.**
- **Cross-page reuse policy:** most photos are used on exactly one page; a small set of category-representative photos (e.g. the produce-market wide shot, the aisle-interior shots) intentionally recur across 2 pages at most (e.g. once as a Home 1 hero-collage tile and once as a `categories.html` product/category image) where the same subject legitimately belongs in both places — consistent with T2's registry precedent that 2-page reuse of a genuinely on-topic photo is acceptable while 3+ page reuse is not.

### Testing & functional-bug changelog (found and fixed via headless-Chrome + Puppeteer interaction testing before sign-off)
Unlike T1/T2 (whose changelogs record fixes made after user reports), this template was interaction-tested end-to-end — dark/light toggle, RTL/LTR toggle, header search, mobile bottom-sheet, category filters, steppers, all three forms, FAQ accordion, and horizontal-overflow checks at 320/375/425/768/1024/1280/1440px on every page — **before** being marked complete, using headless Chrome (`--headless=new --screenshot`) for visual review and a Puppeteer-core driver (pointed at the system Chrome install) for scripted interaction. Three real bugs were caught this way and are recorded here so future templates can watch for the same classes of mistake:
1. **`data-reveal="left"/"right"` scroll-reveal transforms caused permanent horizontal overflow on every page.** The reveal system's initial (pre-intersection) state applies `transform: translateX(±32px)` so elements slide into place. Since this transform is applied to *every* matching element on page load — including ones far below the fold — any element whose untransformed position was already near the left/right edge of the viewport pushed the document's scrollable area past the viewport width from the very first paint, before any scrolling or JS observer had fired. This produced a consistent 8–19px `scrollWidth > clientWidth` overflow at *every* breakpoint on *every* page, confirmed via `document.documentElement.scrollWidth - clientWidth` in a scripted check. **Fix: `overflow-x: hidden` on `html, body`** — this is the standard, defensive counter-measure for exactly this class of bug (any decorative/animation transform that might transiently or permanently extend layout bounds) and was verified to bring overflow to exactly 0px at all 7 tested widths across all 8 pages afterward. **Lesson: any template using transform-based entrance animations (translateX/Y reveal, floating decorative shapes, etc.) needs a global `overflow-x: hidden` safety net, not just careful placement of individual elements — CSS transforms on far-below-fold elements can silently extend `document.scrollWidth` before the user ever scrolls there or the animation resolves.**
2. **Header search overlay had no results container in the markup.** The JS (`initHeaderSearch` → `renderResults`) was written against a `[data-search-results]` selector that was never actually added to the header HTML on any page — the search input and close button existed, but typing produced no visible output at all (a silently-dead feature, not a crash). Caught by a scripted test that typed "milk" and asserted at least one result rendered; it returned zero. **Fix:** added an absolutely-positioned `[data-search-results]` dropdown panel (rounded, bordered, scrollable) as a sibling of the input inside the now-`relative` search-overlay container, on all 7 pages that include the header search. **Lesson: when JS is written against a `data-*` hook, grep the HTML to confirm that hook actually exists before considering the feature done — a missing container fails silently (no console error) rather than loudly.**
3. **Success messages were nested inside the `<form>` they belonged to, so hiding the form on submit also hid its own success message.** The submit handlers in `main.js` (`initForms`, `initBulkOrderForm`) add a `hidden` class to the `<form>` element itself and simultaneously try to reveal a `[data-form-success]` message — but on the general contact form and the bulk-order form, that success `<p>` was a *child* of the form being hidden, so `display: none` on the parent made the success message unconditionally invisible regardless of its own class state. (The two newsletter forms were unaffected — their success message happened to already be written as a sibling *after* `</form>`, which is what exposed the inconsistency.) Caught by a scripted test that submitted valid data and asserted the success message's computed visibility, not just its class list. **Fix:** restructured both affected forms so `[data-form-success]` is a sibling of `<form>` (wrapped in a shared parent `<div>` where needed to preserve grid layout), and updated `main.js` to look up the success element via `form.parentElement.querySelector('[data-form-success]')` instead of `form.querySelector(...)`. **Lesson: never place a "success" confirmation message as a descendant of the form element you plan to hide on success — verify the fix by checking actual rendered visibility (e.g. `!el.classList.contains('hidden')` *and* the element is reachable), not just that the class-toggle code ran without throwing.**

### Premium redesign pass (Home, Categories, Offers, Contact, FAQ)
After initial launch, the client asked for a substantially more premium/ThemeForest-quality treatment of five pages, explicitly rejecting "simple card-only" layouts. Rather than a new template, this was a *design-system extension* of T3: new shared components were added to `style.css`/`main.js` and reused across all five pages, keeping the established palette/type/header/footer/RTL/dark-mode system intact.

**New shared components added (available site-wide, not just on the redesigned pages):**
- `.bento-card` — image tile with gradient overlay, hover lift + zoom + reveal-description panel, rotating arrow button (bidi-aware). Used for premium category grids (Home bento scroll strip, Categories' 8-card showcase, Offers' featured-deals bento).
- `.blob` decorative gradient-blur shapes + `.texture-grid` — layered behind hero sections for depth, replacing flat single-color hero backgrounds on Categories/Contact/FAQ.
- `.promo-banner` (radial-gradient + dot-pattern overlay) — used for Offers' hero and FAQ's support-CTA band; visually distinct from the plain `bg-primary-dark` solid sections used elsewhere.
- `.coupon-ticket` / `.copy-code-btn` — a dashed-perforation "ticket" component with a punched-hole notch and a working copy-to-clipboard button (`data-copy-code`, `initCopyCode()` in `main.js`) — new interaction pattern, distinct from the existing `tag-card` price-tag notch.
- `.quote-premium` / `.avatar-ring` — testimonial cards with a large background quote-mark glyph and a gradient-ring avatar frame, replacing the site's original plain-bordered testimonial cards on Home.
- `.field-float` — floating-label form fields (label animates up/shrinks on focus or filled state) for the newsletter and general-contact/bulk-order static fields; deliberately *not* applied to the bulk-order form's dynamically-JS-generated item rows or the native `<input type="date">` field (floating labels don't play well with native date-picker placeholder rendering).
- `.accordion-premium` — icon-in-a-box accordion trigger (rotates 180° + inverts to filled color on expand) used on FAQ and Contact's FAQ-preview, replacing the plain-bordered accordion rows from the original build.
- `.faq-search-wrap` + `initFaqSearch()` — live client-side FAQ search across every `[data-faq-item]`, with a result-count readout and a "no results" empty state; matching items inside `data-accordion="single"` groups auto-expand while non-matches auto-collapse.
- `data-tilt` + `initTiltCards()` — lightweight pointer-driven 3D tilt on hover, gated to `(pointer: fine)` and `prefers-reduced-motion: no-preference` so it never activates on touch devices or for motion-sensitive users.
- `data-word-cycle` + `initWordCycle()` — vanilla cross-fade word rotator for the Home hero headline (`Local Roots. / Real Flavor. / Farm Direct. / Picked This Morning.`), avoiding a new external dependency (no Typed.js, keeping with T3's vanilla-first animation policy).
- `.logo-marquee-track` — a second marquee instance (trust/certification badges under the Home hero) reusing the existing marquee keyframes but as a distinct content lane from the header's offers ticker.
- `.ribbon-corner` — a CSS-triangle corner ribbon (bidi-aware) for the seasonal-offer banner on Offers, distinct from the diagonal `tag-ribbon` used on product cards.

**Page-level changes:**
- **Home (`index.html`):** hero rebuilt with blob backdrop + word-cycle headline + tilt photo collage + a trust/certification marquee strip beneath it (new). "Shop by Category" changed from a uniform 6-card grid to a horizontally-scrollable bento strip with badges (Best Seller / Daily / New). "This Week's Offers" wrapped in a `.promo-banner` with a coupon-ticket teaser pair added beneath the existing carousel. Testimonials upgraded from plain cards to `.quote-premium`. Newsletter field upgraded to `.field-float`.
- **Categories (`categories.html`):** added a new premium hero section — an 8-tile bento showcase (6 real aisles + "This Week's Bestsellers" linking to Offers + "Meet Our Local Farms" linking to About) sits above the existing, unchanged 6-aisle priced product directory (kept for real functionality: steppers, add-to-list, chip-bar anchors). A `.divider-glow` + heading separates "the full directory" from the showcase so both purposes read clearly on one page.
- **Offers (`offers.html`):** fully rebuilt per spec — `.promo-banner` hero with countdown, a "Featured Offers" asymmetric bento (Deal of the Week + two stacked cards), a dark "Flash Deals" horizontal strip, a 4-card coupon-code grid, a seasonal "Autumn Harvest Sale" ribbon-corner banner, the existing filterable "All Discounts" grid (kept, restyled with `data-tilt`), and a final `.promo-banner` newsletter CTA. This page does not load Swiper (never did) — the new "featured/flash" carousels use the existing `.scroll-strip` drag/wheel mechanism instead, kept consistent with the rest of the site.
- **Contact (`contact.html`):** hero rebuilt with blob backdrop + 3 quick-contact `info-card-premium` tiles (Call / Email / Bulk Orders), a new trust-indicator strip, hours/location cards upgraded to `.info-card-premium`, general-contact fields upgraded to `.field-float`, and a new FAQ-preview section (4 `.accordion-premium` items in a `data-accordion="multi"` group) added before the footer. The bulk-order form's structure, IDs and `data-*` hooks were preserved exactly (only its static top-level fields got `.field-float`; the dynamically-generated item rows keep their original plain-bordered inputs since they're built as raw HTML strings in `main.js` and retrofitting floating labels there wasn't worth the risk to the working add/remove/subtotal logic).
- **FAQ (`faq.html`):** fully rebuilt — blob-backdrop hero with a live search bar, a "Popular Questions" quick-jump grid (anchor links into the categorized list below), the same 5 topic groups as before but restyled as `.accordion-premium` inside `data-faq-group` wrappers (required for the new search feature to scope/filter correctly), a "no results" empty state, and a `.promo-banner` support CTA at the end.

**Scope decision — category taxonomy:** the brief's "only 8 category cards, not all categories" was interpreted as *presentation*, not a request to expand the store's real product taxonomy. Expanding to 8 literal grocery aisles would have required touching the chip-bar, footer links, mobile-sheet aisle grid, and search index on all 8 pages, plus building two entirely new priced product sections. Instead, Categories' new 8-tile showcase uses the existing 6 real aisles plus two editorial tiles ("This Week's Bestsellers" → `offers.html`, "Meet Our Local Farms" → `about.html`) — satisfying the "8 cards, premium, badged, not a plain grid" requirement without fragmenting the site's information architecture that every other page already depends on.

**Bugs caught during this pass (scripted-test + visual-review, before sign-off):**
1. **FAQ search had a one-way "ratchet" bug that permanently mis-expanded accordion items.** `page.type()`-style character-by-character input fires an `input` event per keystroke; the original handler set `aria-expanded="true"` on any item matching the *current partial* query but never set it back to `false` when a later, more specific keystroke stopped matching it. Typing "delivery" letter-by-letter (d → de → del → …) transiently matched nearly every FAQ item (most contain the letter "d" somewhere) and force-expanded all of them; by the time the full word landed only 3 items were *visible*, but all ~13 remained `aria-expanded="true"` underneath — confirmed by a scripted check (`TRUE_AFTER_DELIVERY_SEARCH: 13` before the fix). **Fix:** the expand/collapse line changed from `if (match && q) { …aria-expanded, 'true' }` (write-only) to `if (q) { …aria-expanded, match ? 'true' : 'false' }` (always reconciles both directions whenever a query is active). **Lesson: any "auto-expand on search match" feature that fires per-keystroke must also explicitly *un*-expand non-matches on every keystroke, not just expand matches — otherwise state from an earlier, broader partial-query accumulates and never clears.**
2. **One Unsplash photo ID (`1682092016074-b136e1acb26e`, a farmer-in-field shot) started 404ing** — used on both `home-2.html`'s "Halden Family Farm" card and the new Categories 8-tile showcase's "Meet Our Local Farms" tile. Caught visually (a bento tile rendering as bare alt-text on a blank background instead of a photo) rather than by the earlier link-check pass, since the URL had returned 200 at original sourcing time and only broke later. **Fix:** replaced both usages with a freshly-sourced, fetch-then-Read-verified photo (`1777113317559-fb3e17e8b60c`, farmers bagging harvested potatoes). While investigating, a full sweep of all 48 unique image URLs on the site was run (`xargs -P 10 curl -o /dev/null -w '%{http_code}'`) confirming this was the only broken one. **Lesson: Unsplash CDN URLs are not permanently guaranteed to stay live — a photo verified at sourcing time can 404 later (removed by photographer, moderation, etc.) — so a template that's been sitting for a while (or is being revisited for a redesign pass) is worth a cheap bulk HTTP-status sweep of every image URL, not just a one-time check when each image was first added.**
3. **A "premium" (`plus.unsplash.com`) photo candidate had a tiled "Unsplash+" watermark baked into the image itself**, only visible by actually viewing the rendered photo (HTTP 200, normal file size, no error) — the fetch-then-Read technique caught it while sourcing a replacement for bug #2 above, before it was ever committed to a page. Not all `premium_photo-*` IDs are watermarked (four *other* `plus.unsplash.com` photos already used elsewhere on the site were re-verified in the same pass and are clean), so the `premium_photo-` naming prefix alone is not a reliable signal either way. **Lesson: treat every `plus.unsplash.com` candidate as requiring visual (fetch-then-Read) verification before use, specifically checking for a repeated diagonal watermark — HTTP 200 and a plausible file size are not sufficient signals that a premium-tier photo is actually free to render cleanly.**

### Global policy update — strict image uniqueness, and second premium redesign pass (Home 1, Home 2, Categories, About)
The client issued a standing, template-wide requirement after the first premium-redesign pass: **no image may ever be reused anywhere on the site, even for the same product/category appearing on multiple pages.** This is stricter than the "2-page reuse of a genuinely on-topic photo is acceptable" precedent recorded earlier in this file (T2's registry) — that precedent is superseded *for this template going forward*. Apply the strict no-reuse rule from the start on any future template revision, rather than as a retrofit.

**Audit method (repeatable for future passes):** extract every `images.unsplash.com` / `plus.unsplash.com` URL from every HTML file with a `grep -ohE` sweep, strip query-string noise (`?w=`, `&q=`), `sort | uniq -c`, and any count greater than 1 is a duplicate needing a replacement for every occurrence past the first. This template had **94 total image usages across 8 pages, of which 27 distinct photo IDs were each reused 2–5 times — 46 individual occurrences needed a fresh, unique replacement.** For each duplicate group, one occurrence was designated the "keeper" (kept unchanged) and every other occurrence got a newly-sourced photo, with accurate new `alt` text written to match what the replacement image actually shows (not left describing the old photo). After all replacements, the same audit command must return zero output, and a bulk `xargs -P N curl -o /dev/null -w '%{http_code}'` sweep over every unique URL must show 100% `200`.

**New rejects found while sourcing ~40 replacement photos** (beyond the watermark/404 lessons already recorded above):
- Several "clean-sounding" bakery/pantry candidates turned out to have a competing real bakery's **branded packaging, signage, or a name/logo baked into the shot** once actually viewed (a "Josey Baker Bread"-branded paper bag; a wall-sized "Rocco Princi" bakery mural with "STARBUCKS RESERVE" watermarked into the corner; a hand-lettered German-language chalkboard sign with zero food visible). None of these were catchable from alt-text alone ("a bunch of breads that are on a rack" gave no hint of the branding baked into the frame) — only the fetch-then-Read visual check caught them, reinforcing the standing lesson that alt-text/search-snippet descriptions describe the search result, not full photo content.
- A bakery display-case candidate had **visible foreign-currency (€) price tags** baked into the shot ("Croissant 1.00€") — not a competing brand, but a content-accuracy mismatch for a USD-priced Ontario store; discarded for the same reason a mismatched caption would be.
- **Lesson reinforced at larger scale:** when sourcing many replacement photos quickly under time pressure, the failure mode shifts from "wrong subject" (the earlier T1/T2 lesson) to "right subject, wrong background details" — a photo can accurately show bread, croissants, or pantry jars and *still* be unusable because of an incidental price tag, chalkboard, or logo elsewhere in the frame. Bakery and prepared-food-display photography is disproportionately likely to include this kind of incidental real-world signage compared to plain produce/product-on-a-surface shots — budget extra verification passes specifically for that category.

**Second redesign pass — index.html, home-2.html, categories.html, about.html:**
- **Home 1 (`index.html`):** no structural changes in this pass; all 15 of its duplicate image occurrences (hero collage, category bento tiles, offers carousel, produce spotlight strip, "Why Shop With Us") were swapped for unique photos with corrected alt text.
- **Offers (`offers.html`):** heaviest concentration of duplicates (21 occurrences) since it visually repeats products already featured on Home 1 and Categories across its Featured/Flash/All-Discounts sections — all replaced.
- **Categories (`categories.html`):** the 8-tile premium showcase from the first redesign pass was reusing the *same* photos as the detailed product cards further down the *same page* (e.g. the "Produce" showcase tile and the "Fresh Garden Vegetable Mix" product card were identical) — six showcase tiles got fresh photography distinct from their corresponding product-card image below. Additionally, `data-tilt` was added to all 24 product-directory `tag-card` elements (previously only the 8 showcase tiles had it) as the requested "polish the full directory" pass — a lighter touch than restructuring the grid, since a scannable, comparable grid is the correct UX pattern for a priced product listing and the user's original complaint (page opened with "just a plain grid, no premium hero") was about the page's *first impression*, already solved by the showcase.
- **Home 2 (`home-2.html`):** brought up to the same premium-component standard as Home 1 (which got the full treatment in the first pass) — Featured Categories grid converted from plain `rounded-2xl` image tiles to the shared `.bento-card` component with `badge-glow` labels and `data-tilt`; Deal-of-the-Day image and Meet-Our-Farmers cards gained `data-tilt`/`bento-card` styling; the testimonial swiper was wrapped in `.quote-premium` with `.avatar-ring` initials to match Home 1's testimonial treatment conceptually while keeping its own single-spotlight-fade mechanic (still structurally distinct from Home 1's static 3-card `.quote-premium` grid); the final CTA band changed from a plain two-color gradient to the shared `.promo-banner` (dot-pattern overlay) used elsewhere on the site. **Caught and fixed one duplicate introduced by this very pass**: an early edit accidentally set Home 2's Deal-of-the-Day image to the same citrus-board photo just assigned to Offers' hero — caught immediately by re-running the site-wide duplicate audit after every batch of edits (not just once at the end), which is the operating lesson here: **run the cheap `uniq -c` duplicate check after each file's edits during a large image-swapping pass, not only as a final gate — it's fast enough to run dozens of times and catches copy-paste mistakes (reusing a just-assigned replacement URL) immediately instead of at the end of a long session.**
- **About (`about.html`):** rebuilt beyond the light touch-up scope of similar pages — new sections added that didn't exist in the original build: a dedicated **Mission & Vision** twin-card section (distinct from the existing Core Values 3-card band, per an explicit brief requirement to have both), a **promo-banner "By the Numbers" achievements band** consolidating stats that had previously been scattered across the page into one premium count-up section, and a **Testimonials** section (About had none previously) using an asymmetric layout — one large `.quote-premium` spotlight plus two stacked smaller ones in a 3:2 column split — the site's *third* distinct testimonial treatment alongside Home 1's static 3-card grid and Home 2's single-spotlight Swiper fade, with entirely new customer names/quotes not reused from either homepage. The compact photo hero banner (storefront/aisle photography) was replaced with the same blob-backdrop + texture-grid text-only hero pattern already used on Categories/Contact/FAQ, freeing that page's hero photo slot entirely (no image needed there now) and keeping About visually consistent with the other "premium hero" inner pages introduced in the first redesign pass.

### Third redesign pass — full palette replacement + ground-up rebuild of both homepages
The client rejected the *layout* of both homepages outright ("does not look modern or premium... do not reuse the existing homepage layout or sections") and separately requested a full color-theme replacement plus a site-wide carousel/UX bug sweep. This pass touched the shared design tokens (`style.css` `:root` — cascades to all 8 pages) and completely replaced `index.html` and `home-2.html`'s `<main>` content with two new, structurally distinct concepts, while leaving the shared header/chip-bar/mobile-sheet/footer chrome and all 6 other pages' own content untouched.

**New palette (replaces the Basil Green / Heirloom Tomato / Golden Wheat / Kraft-cream palette used since initial launch):**
- Primary: Deep Teal `14 107 103` (light) / `63 189 179` (dark) — brand, CTAs, header
- Accent: Burnt Terracotta `217 98 43` (light) / `240 121 74` (dark) — sale/discount emphasis, secondary CTAs
- Gold: Warm Amber `217 164 65` — ratings, badges, countdown numerals
- Background: Warm Ivory `251 246 238` (light) / deep charcoal-teal `16 24 23` (dark)
Applied via the existing CSS-custom-property system (`rgb(var(--color-x))`), so the swap required editing only the `:root`, `:root[data-theme="dark"]`, and `@media (prefers-color-scheme: dark)` blocks — every page, component, and the dark-mode/RTL systems re-themed automatically with zero per-page edits. This is the value of T3's original "everything references CSS variables, nothing hardcodes a hex" decision paying off three redesign passes later.

**Home 1 (`index.html`) — "Editorial Split" concept, replaces the 2×2-collage-hero/bento-scroll layout entirely:**
Header/chip-bar/footer unchanged. New section order: diagonal-clip-path split hero (`.diagonal-clip-left`, bidi-mirrored via `[dir="rtl"]`, disabled below `lg` in favor of a stacked layout) → bold `.ribbon-marquee` value-prop ticker (a second, visually heavier marquee variant distinct from the header's thin top-strip ticker) → "Shop by Craving" `.mood-card` grid (breakfast/dinner/snacks/pantry-restock framing, full-bleed photo + hover-zoom + icon badge, replacing the old bento category-scroll strip) → a Swiper "Trending This Week" carousel with custom prev/next buttons wired via new `data-swiper-prev`/`data-swiper-next` id-targeting in `initSwipers()` → a local-sourcing image+checklist promo split → a single Weekly-Deal `.coupon-ticket` spotlight with its own countdown → a 5-card `.quote-premium` testimonial Swiper (upgraded from the prior static 3-card grid to a carousel, since a 5-card grid doesn't fit cleanly) → a 6-photo "Fresh Picks" gallery grid → a newsletter + bulk-order 2-panel split → footer. Structurally shares no section with either the pre-redesign or the first-two-redesign-pass version of this page.

**Home 2 (`home-2.html`) — "Interactive Aisle Explorer" concept, replaces the full-bleed/category-dial/bento layout entirely:**
New section order: hero with a small circular `.spin-badge-wrap` (rotating SVG `textPath` ring, 8.5rem, positioned as a decorative badge overlapping the corner of a large circular hero photo — **not** a wrapper around the whole photo, see bug #1 below) → `.ribbon-marquee` deal ticker → a fully-interactive **aisle tab switcher** (`.aisle-tab`/`.aisle-panel`, `data-aisle-tabs` wrapper, `initAisleTabs()`) letting visitors click between Produce/Dairy/Bakery/Pantry/Beverages and see that aisle's actual priced items inline, replacing the old static "Browse by Aisle" numbered-card strip → a 3-card "This Week's Meal Inspiration" recipe-tie-in section (new — links each recipe's ingredients back to `categories.html`) → a 5-card Meet-the-Farmers Swiper carousel → a `.stripe-divider` + count-up stats band → a bulk/business promo split → a **manual-nav testimonial** section (`data-testi-manual`, prev/next buttons, `.testi-dot` indicators, `initManualTestimonial()`) — deliberately autoplay-free and user-driven, the site's *fourth* distinct testimonial mechanic (alongside Home 1's carousel, About's asymmetric spotlight, and the original static grid pattern) → a final `.promo-banner` CTA → footer.

**New shared components added to `style.css`/`main.js` for this pass:** `.diagonal-clip-left`, `.ribbon-marquee`, `.mood-card` + `.mood-icon-badge`, `.spin-badge-wrap`/`.spin-badge` (CSS `@keyframes` rotation, reduced-motion safe), `.aisle-tab`/`.aisle-panel` + `initAisleTabs()`, `.testi-nav-btn`/`.testi-slide`/`.testi-dot` + `initManualTestimonial()`, `.stripe-divider`. `initSwipers()` extended to accept optional per-instance `navigation` wiring via `data-swiper-prev="<id>"`/`data-swiper-next="<id>"` attributes on external buttons (previously Swiper instances on this site only ever used pagination dots, never custom nav buttons).

**Bugs caught during this pass (scripted-test + visual-review, before sign-off):**
1. **Tailwind's CDN-injected utility stylesheet silently defeated a custom display-toggle class due to cascade order, not specificity.** `.aisle-panel { display: none } .aisle-panel.is-active { display: grid }` looked correct and the `initAisleTabs()` JS was toggling `is-active` correctly (confirmed by inspecting `classList` directly) — but every panel remained visibly `display: grid` regardless, because each panel `<div>` also carried Tailwind's own `.grid` utility class for its internal layout, and the Tailwind CDN script injects its generated `<style>` block into `<head>` *after* `style.css` has already been parsed (it runs synchronously and appends at the point the script tag was declared, while `style.css` is a `<link>` earlier in the same `<head>`), so `.grid` — same specificity as `.aisle-panel`, later in source order — won the cascade. Caught by a Puppeteer test that read `getComputedStyle(panel).display` before any tab was ever clicked and found it was already `"grid"` for a non-active panel. **Fix:** raised the hidden-state selector's specificity instead of reordering markup or adding `!important` — `.aisle-panel:not(.is-active) { display: none }` (two-part specificity, `0,2,0`) reliably beats `.grid` (`0,1,0`) regardless of load order. **Lesson: any custom CSS class that toggles `display` on an element which *also* carries a Tailwind display-utility class (`.grid`, `.flex`, `.block`, etc.) for its internal layout is racing the Tailwind CDN script's injection timing — don't rely on source order or equal-specificity single-class selectors to win; use `:not(.state-class)` or an equivalent higher-specificity selector, not `!important`, on the hidden-state rule.** A `getComputedStyle` check on the *unclicked* default state (not just the post-interaction state) is now the standard test for any new show/hide toggle feature going forward.
2. **Passing `navigation: undefined` to the Swiper constructor threw `Cannot read properties of undefined (reading 'enabled')` on every page load**, even though the intent ("only enable custom navigation for swiper instances that actually have matching buttons") was reasonable. Swiper 11's internal param-merging does not tolerate an explicitly-`undefined` value the same way it tolerates the key being absent entirely. Caught by a scripted `pageerror` listener (not visible without one — the carousels still rendered and functioned, autoplay included, so this was a silent-but-real console error, not a UX-visible bug). **Fix:** build the Swiper config object conditionally — only assign `config.navigation = {...}` when both button elements exist, never assign the key to `undefined`. **Lesson: when a library's config object is being built conditionally, omit optional keys entirely rather than assigning them a falsy/`undefined` placeholder — many libraries (Swiper included) distinguish "key absent" from "key present but undefined" internally and only the former is safe.**
3. **Two Swiper carousels' autoplay silently never advanced because they had exactly as many slides as the carousel's largest `slidesPerView` breakpoint, leaving nothing to scroll to** — the *same root cause* as the `loop`-missing bug fixed earlier in this template's history, but manifesting differently: `.testimonial-cards-swiper` (3 slides) and `.farmers-swiper` (4 slides) both sit under the shared `.offers-swiper` config, whose widest breakpoint shows 4 slides at once (`1280: { slidesPerView: 4 }`). With slide count ≤ `slidesPerView`, every slide is already visible simultaneously and there is nothing for `activeIndex` to advance to, regardless of `loop`/`autoplay` settings — confirmed via the same multi-sample `realIndex`-over-8-seconds technique used for the original bug, which returned `0,0,0,0` for both. **Fix:** added one additional testimonial and one additional farmer card to each (bringing both to 5, matching the `loop: slideCount > 4` threshold already established elsewhere in `initSwipers()`), re-verified with the same sampling technique returning genuine progression (`0,1,1,2`). **Lesson: any carousel sharing a common Swiper config with other instances needs its own slide count checked against *that config's maximum `slidesPerView` breakpoint*, not just checked for "more than one slide" — a carousel can have multiple slides and a working `loop` setting and *still* never move if the viewport-appropriate `slidesPerView` happens to equal or exceed the slide count.**
4. **Six replacement Unsplash photo IDs sourced for this pass returned HTTP 404 in production despite the search-result page listing them as live results**, three of them additionally surfacing as `net::ERR_BLOCKED_BY_ORB` in Chrome (Opaque Response Blocking triggers when a cross-origin `<img>` request's response isn't actually image content — consistent with the CDN returning an HTML/JSON error body for a dead photo ID). This is the same "Unsplash CDN URLs are not permanently guaranteed to stay live" lesson recorded earlier in this file, but caught proactively this time: a full bulk `xargs -P N curl` HTTP-status sweep of every new image URL was run as a matter of course before considering the two new homepages done (not triggered by a visual glitch), which is now the standing practice rather than a reactive fix. All 6 were replaced with fresh, verified (HTTP 200 + dedup-checked against all 110+ other site-wide IDs) photos before sign-off; a final full-site sweep confirmed 115 unique image URLs, zero duplicates, 100% HTTP 200.
5. **A Puppeteer `fullPage: true` screenshot rendered the mobile bottom-sheet menu content inline mid-page with a large dark void beneath it, initially read as a severe layout bug.** Investigation (`getComputedStyle` + `getBoundingClientRect` at a normal fixed viewport) showed the live site was actually correct — `.bottom-sheet` computed `position: fixed`, translated fully below the viewport (`rectTop: 1000` at a 1000px-tall viewport), and every `<main>` section had sane, sequential `top`/`height` values summing to the reported `scrollHeight`. The artifact only appeared when Puppeteer's `fullPage` option resized the viewport to the full document height before capturing, which changes how `position: fixed` elements (sized/positioned relative to viewport dimensions, e.g. `max-height: 80vh`) are laid out relative to the temporarily-huge viewport. **Not a template bug — a known Puppeteer/Chrome interaction between `fullPage` screenshots and `position: fixed` elements.** **Lesson: before trusting a `fullPage: true` screenshot that shows something structurally alarming (especially involving a `position: fixed` element like a mobile drawer, sticky header, or modal), cross-check with `getComputedStyle`/`getBoundingClientRect` at the actual intended viewport size, or re-capture by manually setting the viewport height to the page's `scrollHeight` and taking a normal (non-`fullPage`) screenshot — the latter was adopted as this template's standard method for full-page visual review going forward.**

### Fourth redesign pass — Home 1 hero rebuild, chip-bar removal, and a template-wide container/alignment audit
The client asked for three specific changes to `index.html` plus a general consistency pass: remove the category chip-bar entirely from the Home 1 header, completely redesign the hero (called out as not looking "modern or premium" even after the third pass), and verify container/spacing/alignment consistency across the whole template.

**Chip-bar removal (`index.html` only):** the `<nav class="chip-bar">` block was deleted from Home 1's `<header>`, leaving the three-tier header as a two-tier header on this page only — `.site-header`'s own `border-bottom` naturally becomes the header's bottom edge with no CSS changes needed. The chip-bar remains unchanged on all 7 other pages (it's still each of those pages' primary in-page/cross-page category navigation, per the original T3 header design). `initChipScrollSpy()` and the shared `[data-chip]` styling in `style.css` were left untouched since they're driven entirely by markup presence, not a page-count assumption.

**Hero rebuild (`index.html`):** replaced the diagonal-clip split-screen hero (dark left panel + clipped image right panel) with a **full-bleed image hero + glass content panel** — a fourth distinct hero mechanic for this template (alongside the original 2×2-collage Home 1 hero, the full-bleed+category-dial Home 2 hero, and the third-pass diagonal-split hero it now replaces). Structure: a single full-bleed photo with a bidi-aware two-layer gradient (`ltr:from-black/85` / `rtl:from-black/55`, swapped, plus a bottom-up scrim for the floating cards) sits behind a `max-w-[1440px] mx-auto px-4 sm:px-6` content column (fixing the alignment bug described below); a `data-word-cycle` headline cross-fades through four closing phrases (`somewhere real. / never mass-produced. / picked yesterday. / still local.`) reusing the existing `initWordCycle()` engine rather than adding a new dependency; a frosted-glass (`bg-white/10 backdrop-blur-md`) stat strip with divider rules replaces the old plain-text stat row; two `data-tilt` floating badge cards (discount + free-delivery) sit over the photo at `lg:` breakpoint only; a bounce-animated "Scroll" cue anchors down to the next section. New hero photo (`images.unsplash.com/photo-1516594798947-e65505dbb29d`, a grocery cooler shelf of leafy greens) was sourced fresh, fetch-then-Read visually verified (an out-of-focus price sign is visible in one corner but is fully illegible/blurred, not a content-accuracy issue per the established signage lesson), confirmed HTTP 200, and confirmed non-duplicate against all other site-wide image IDs before use.

**Container/alignment audit (template-wide, before sign-off):** rather than guessing at "inconsistent" sections from a source-code read-through, the audit was done by measuring actual rendered geometry with Puppeteer across all 8 pages at 375/1024/1440/1920px:
- Confirmed **zero horizontal overflow** on every page at every tested width (already true going into this pass — no regression).
- Confirmed every element carrying `max-w-[1440px]` uses **identical padding** (`px-4 sm:px-6`) with no stray alternate container width anywhere in the template (`max-w-[1440px]` is used 84 times across the 8 pages; no other pixel-based container width exists).
- At a 1920px (ultra-wide) viewport specifically — the width at which a hero *not* wrapped in the standard container would visibly drift from the header logo's left edge — compared each page's first heading's left edge against the header logo's left edge. Six of 8 pages matched exactly (`diff=0`); the other two (About, FAQ) and 404 use an intentional narrower **centered** hero (`max-w-3xl mx-auto text-center`, the established "inner page" hero pattern), which legitimately differs and is not a bug. **The one real hit: Home 1's pre-redesign hero had no `max-w-[1440px]` wrapper around its text content at all** (only `px-4 sm:px-6 lg:px-16` padding directly on the flex column), so on viewports wider than ~1490px the hero headline drifted further from the true content edge than the header logo above it — this is precisely the defect the hero rebuild above fixes structurally, not just visually, by wrapping the new hero's content in the same `max-w-[1440px] mx-auto px-4 sm:px-6` container used everywhere else.
- **Lesson: a full-bleed hero (edge-to-edge background image/color) must still wrap its *text content* in the page's standard container — the background and the content container are independent concerns, and it's easy to accidentally constrain only with padding (`px-16`) instead of the real container, which silently drifts out of alignment with the header above it on wide viewports where `padding ≠ (viewport − max-width) / 2`.** Any future hero redesign in this template should default to `<section>` (full-bleed bg) → inner `<div class="max-w-[1440px] mx-auto px-4 sm:px-6">` (content) as the standard skeleton, the same pattern already used correctly by every non-hero section in the template.

**Incidental bug found and fixed during the audit (unrelated to the container check itself):** `home-2.html`'s hero had two `<div class="blob blob-1"></div>` / `<div class="blob blob-2"></div>` decorative elements referencing `.blob-1`/`.blob-2` classes that **do not exist anywhere in `style.css`** — every other page's `.blob` usage pairs the base class with explicit Tailwind size/position/color utilities (`w-96 h-96 -top-24 ltr:-right-20 bg-[rgb(var(--color-primary))]`, etc.) directly on the element, since `.blob` alone only sets `position/border-radius/filter/opacity`, not size or color. The `blob-1`/`blob-2` variant was a one-off naming choice from the third redesign pass that was never actually defined, so both decorative blobs on Home 2's hero have been rendering as invisible 0×0 elements since that pass. **Fix:** replaced both with the same explicit-utility pattern used on every other page's `.blob` instances. **Lesson: when a decorative/utility CSS class is applied with a modifier suffix (`.blob-1`) instead of the established explicit-Tailwind-utility pattern used everywhere else for that same base class, grep the stylesheet to confirm the modifier is actually defined — an undefined modifier fails completely silently (no console error, no visual glitch beyond "the decoration just isn't there"), the same failure mode recorded earlier in this file for the missing `[data-search-results]` container.**

### Fifth redesign pass — content simplification (Categories, Offers), chip-bar removal template-wide, and a full Home 2 rebuild
The client's core complaint this round was density, not aesthetics: `categories.html` (a hero + an 8-tile bento showcase + a full 24-item priced product directory across 6 aisles) and `offers.html` (hero + featured bento + a flash-deals strip + 4 coupon tickets + a seasonal banner + a 14-item filterable "all discounts" grid + a newsletter CTA — 6 sections, ~40 product cards combined) had grown well past "clean, practical" through four prior redesign passes that each *added* premium sections without ever removing anything. The brief this time was explicit: simplify, remove what isn't essential, keep strong CTAs, and make sure every page still communicates the business at a glance. The client also asked for the header's category chip-bar removed everywhere it still appeared (it had already been removed from `index.html` in the prior pass) and for `home-2.html` to be rebuilt again with a structurally distinct concept.

**Chip-bar removed from `about.html`, `contact.html`, `faq.html`, and `home-2.html`** (categories.html and offers.html lost theirs as part of their full rewrite below) — every page now uses the plain two-tier header (top-strip + main row only). The chip-bar's own CSS/JS (`initChipScrollSpy()`, `.chip-bar`, `.category-chip`) was left in `style.css`/`main.js` since it's still harmless, guarded, unused markup elsewhere is a no-op — no page references it anymore site-wide.

**`categories.html` — cut from 691 lines / ~6 sections / 32 product cards down to 4 sections:** compact breadcrumb hero → a single clean 6-card category grid (Produce/Dairy/Bakery/Pantry/Beverages/Household, `.bento-card` component, one HD photo + name + one-line description each, no per-item pricing or steppers) → a 3-icon trust strip (Fresh every day / 18 local partners / Bulk orders welcome) → a single CTA band linking to Offers and Bulk Order. The 8-tile bento showcase (which duplicated the 6-card grid's own subject matter with two editorial tiles) and the entire 24-card priced product directory were removed outright — per-item pricing/browsing now lives only on `offers.html` (for deals) and is otherwise implied by the category cards, consistent with the original master brief's "clean, practical layout showcasing store categories" rather than a full e-commerce catalog. **The category `id="produce"` / `id="dairy"` / etc. anchors were kept on the new cards** specifically so the footer's existing `categories.html#produce`-style deep links (present on all 8 pages) continue to resolve without needing a site-wide footer edit.

**`offers.html` — cut from 595 lines / 6 sections / ~24 product cards down to 4 sections:** the countdown-timer promo-banner hero (kept, it's the page's strongest CTA element) → "Featured Offers" simplified from an asymmetric 2-large/2-small bento layout to a single consistent 3-card grid (Citrus Grove Bundle, 2-for-1 Sourdough, Free-Range Eggs) → the 4-ticket promo-code section (kept, high CTA value, low visual weight) → the newsletter CTA band (kept). Removed entirely: the dark "Flash Deals" horizontal-scroll strip (5 cards), the gradient "Autumn Harvest Sale" seasonal banner, and the 14-card filterable "All Discounts, by aisle" grid — all three were substantially redundant with Featured Offers and, combined, accounted for the bulk of the page's length. `initCategoryFilter()` in `main.js` is now unused on every page (it was only ever wired to the removed filter grid) but was left in place since it's a generic, self-guarding function (`if (!filterBtns.length || !items.length) return;`) — harmless dead code, not a half-finished feature.

**`home-2.html` — full rebuild, fifth distinct concept for this slot:** replaced the "Interactive Aisle Explorer" (tabbed category/product browser, meal-inspiration cards, farmers Swiper, manual-nav testimonial) with an **"Editorial" concept** that is typography-led rather than card-grid-led, and deliberately drops Swiper entirely (no carousel dependency on this page at all — the CDN `<link>`/`<script>` tags were removed from the page, not just left unused). New section order: a solid-color (not photo) hero with a large `data-word-cycle` headline and a small offset two-photo collage (`data-tilt`) → an early stats band (15+ years / 18 farms / 180+ products / 4.9 rating — trust signals moved to the top of the page rather than buried mid-scroll) → a single full-width immersive farm-field photo banner with overlay text (no card grid at all in this section — a deliberate large visual break) → a 4-icon "What Makes Us Different" feature list (pure icon+text, no photos, the page's only all-typography section) → a single combined weekly-highlight-and-CTA band linking to Offers and Bulk Order. No dedicated testimonial section was added, since Home 1, About, and the original Home 2 already each have their own distinct testimonial treatment — a fourth would be redundant given this pass's "remove what isn't essential" directive.

**Image economy:** this pass *reduced* the site's total unique image count from 115 to 40 (zero duplicates, all verified HTTP 200) simply by removing the ~40 individual product-card photos that no longer exist on the simplified Categories/Offers pages and by sourcing only 4 new photos for the rebuilt Home 2 (2 hero-collage images, 1 farm banner, later +1 replacement below).

**Bugs caught during this pass (scripted-test + visual-review, before sign-off):**
1. **A real, competing third-party brand label was hiding in plain sight on `categories.html`'s Beverages card.** The photo (`photo-1650547001420-…`, "person holding a bottle of fresh juice") had passed visual review twice in earlier passes because it was always used as a small ~150px bento tile where the label was too small to read. Simplifying Categories to 6 large, prominent 256px-tall cards made the same photo's label — a real "Neviri Juice" bottle logo — clearly legible for the first time. Caught by re-running the fetch-then-Read visual-verification technique specifically on every reused image at its *new, larger* display size (not just at first-sourcing time), which surfaced the branding immediately. Two follow-up candidate replacements were also visually rejected before landing on a clean one: a "3D rendering" (not a real photograph, tagged as such in its own metadata, inconsistent with the site's photography-only aesthetic) and a real "Kinley" (Coca-Cola-owned) branded water bottle. **Fix:** replaced with a clean, unbranded "orange juice splashing into a glass" shot, verified HTTP 200 and non-duplicate. **Lesson: a photo's branding risk is a function of its *display size*, not just its subject matter — an image that visually cleared review as a small thumbnail must be re-verified before being reused at a significantly larger size, since small legible text/logos are frequently invisible at thumbnail scale and only become a real problem once the crop is enlarged.** Every other reused/prominent image in this pass (produce, dairy, bakery, pantry, household category cards; the Home 2 hero collage and farm banner) was proactively re-checked at full display size using the same technique and confirmed clean before sign-off.
2. **The shared `initWordCycle()` hero-headline rotator (in production since the third redesign pass, used on both Home 1 and now Home 2) silently went permanently blank after its very first rotation** — a deterministic JavaScript closure bug, not a timing/environment fluke: the interval callback declared `var next = (idx + 1) % spans.length`, performed the crossfade using `idx`/`next`, scheduled a 500ms `setTimeout` to clean up the *old* span via `spans[idx]`, and only *then* executed `idx = next`. Because `idx` is a single mutable variable shared by every closure (not a fresh per-call binding), by the time that `setTimeout` actually fired 500ms later, `idx` had already been reassigned to `next`'s value — so the cleanup callback ran against the *new current* span instead of the *old departing* one, permanently stacking a leftover `is-waiting` class (`opacity: 0`) onto the span that was simultaneously carrying `is-current` (`opacity: 1`). Since `.is-waiting` is declared after `.is-current` in the stylesheet with equal specificity, it won the cascade — so after transition #1, the headline's active word rendered at `opacity: 0` forever, on **every page that used this component**, not just the newly-built one. This had been silently broken since the feature was introduced and simply hadn't been caught because prior screenshot-based visual QA happened to sample the hero before the first ~2.6s rotation completed. Caught this time only because a scripted test explicitly sampled span opacity/classList every 500ms across multiple rotation cycles (6+ seconds) rather than taking a single screenshot. **Fix:** capture the pre-transition index in a fresh per-invocation variable (`var prev = idx;`) at the top of the interval callback and reference `spans[prev]` in the `setTimeout` cleanup instead of the shared, later-mutated `idx`. Re-verified with the same multi-sample technique across 12 samples / 6 seconds / 3 full rotations on both Home 1 and Home 2: exactly one span visible at all times, correct word at each expected interval. **Lesson: any `setTimeout`/`setInterval` callback that closes over a shared, later-reassigned loop/state variable (`idx`) rather than a value captured fresh at invocation time is a latent bug even when it "looks fine" in a quick visual check — this class of bug is specifically inclined to hide from screenshot-based QA (which naturally tends to sample near page-load, before enough animation cycles have elapsed to surface it) and needs an explicit multi-sample-over-time test, not a single screenshot, to catch.**
3. **A product label starting with a digit ("2-for-1 Sourdough Loaves") visually flipped to "for-1 Sourdough Loaves-2" under the RTL toggle** — caught during the light/dark/RTL/mobile screenshot matrix that's now standard practice for every rebuilt page before sign-off. This is standard Unicode bidi-algorithm behavior (a leading numeral in an otherwise-LTR string embedded in an RTL paragraph gets reordered to the visual end) rather than a template bug, but it reads as broken to a viewer regardless of the underlying cause. **Fix:** reworded the copy to "Buy One, Get One Sourdough" — sidestepping the issue rather than patching around it with `<bdi>`/`dir="ltr"` markup, since the copy reads just as naturally without a leading digit. **Lesson: avoid leading a translatable/mirrorable text string with a raw numeral (prices are fine since they're always wrapped in their own span/element with a fixed LTR context) — check any new digit-led product name or label specifically under the RTL toggle, since this class of issue is invisible in LTR and only surfaces exactly where the site's RTL support is supposed to be a selling point.**

### Sixth pass — template-wide container/header alignment re-audit, and a full "Trending This Week" rebuild (Home 1)
The client asked for two things: a precise, re-stated container/alignment spec applied template-wide (content must align exactly from the header logo's left edge to the last header button's right edge, everywhere except intentionally full-bleed sections), and a full redesign of Home 1's "Trending This Week" section specifically away from the Swiper-card layout used for it since the fourth pass.

**Container/alignment re-audit — a testing-methodology lesson, not a template bug:** the first automated pass of this audit falsely flagged every content container on every page as misaligned (`leftMismatch`/`rightMismatch` > 0 everywhere). Root cause was in the *test script*, not the site: it compared each `.max-w-\[1440px\]` container's own unpadded `getBoundingClientRect().left` (which is legitimately `0` at the viewport's exact max-width, since the container itself has no left offset before its own `px-4`/`px-6` padding is applied) against the header **logo's** rendered position (which is *already* inset by the header's identical container's padding). This is an apples-to-oranges comparison — a padded child position vs. an unpadded parent-container position — and would flag a false mismatch on a perfectly correct page. **Corrected methodology:** compute each container's *padded content edge* (`rect.left + computedPaddingLeft`, `rect.right - computedPaddingRight`) and compare that to the logo/last-button positions, since both sides of the comparison are now "where content visually starts/ends," not a mix of container-edge and content-edge measurements. Re-run with the fix across all 8 pages at 1024/1440/1920px: **zero mismatches everywhere** — the container system was already correct going into this pass; no site changes were needed here. **Lesson: when an automated geometry audit reports every single page as broken (100% failure rate on a system that passed manual/visual review repeatedly in prior passes), suspect the test's comparison logic before the site — a measurement bug that compares two different "kinds" of edge (padded vs. unpadded, content-box vs. border-box) produces exactly this kind of uniform false-positive across every page, whereas a real template regression would typically affect specific pages or sections inconsistently.**

**"Trending This Week" rebuild (`index.html`):** replaced the Swiper carousel (5 uniform `tag-card` slides + custom prev/next nav) with a **spotlight-and-ranked-list layout** — a single large `#1 This Week` HD image card (`.trend-spotlight`, `data-tilt`, gradient-overlay caption with name/price/CTA) beside a vertical stack of four compact `.trend-row` items (rank number, thumbnail, name/category, price, and a small pill-shaped quick-add button that expands in place to show "Added" via the existing generic `data-add-to-list` handler — no new JS needed). Deliberately drops Swiper for this section entirely (no carousel, no autoplay, no slide-count-vs-`slidesPerView` class of bug possible here) while the page's testimonial section keeps its own separate Swiper instance unaffected. All 5 products/images are the same ones the old carousel used (no new images sourced, no dedup risk). New CSS added: `.trend-spotlight`, `.trend-row` (with a `translateX` hover lift, mirrored for RTL), `.trend-rank`, `.trend-thumb`, `.trend-add`.

**Bugs caught and fixed while building it (scripted-test + visual-review, before sign-off):**
1. **Nested a `<button>` inside an `<a>` in an early draft of the spotlight card** (to make the whole card a link to `categories.html#produce` *and* keep the "Add to list" button inside it). This is invalid HTML — a `<button>` is not permitted as a descendant of an `<a>`, and browsers silently "fix" the malformed markup via the HTML parser's adoption-agency algorithm, which can rearrange the DOM in ways that break event handling unpredictably. Caught before ever loading the page, by re-reading the markup just written rather than only trusting the visual screenshot. **Fix:** changed the spotlight's outer element from `<a>` to a plain `<div>`, matching the established `tag-card` pattern used by every other product card site-wide (a non-link container with the `data-add-to-list` button as its real call-to-action) — consistent with the rest of the template rather than a one-off exception.
2. **A CSS Grid `min-width: auto` overflow caused real content clipping on mobile, masked by the site's own defensive `overflow-x: hidden`.** Neither grid child (the spotlight, the ranked-list column) had `min-width: 0`, so — per the CSS Grid/Flexbox spec's default `min-width: auto` behavior for grid/flex items — the browser sized the implicit grid track to the *content's intrinsic minimum width* (measured at 427px) rather than shrinking to the actual 358px available inside the mobile container. Because `overflow-x: hidden` on `html`/`body` (added in an earlier pass specifically to prevent this class of bug from producing a page-wide horizontal scrollbar) was doing its job, `document.documentElement.scrollWidth` reported **zero** overflow even while the spotlight card and list rows were visibly clipped mid-content on screen (the "Add to list" button and a price figure cut off at the viewport edge) — a real, visible defect that a naive `scrollWidth`-only overflow check would have completely missed. Caught by a scripted test that measured individual elements' `getBoundingClientRect()` against the viewport width, not just the document-level scroll metrics. **Fix:** added `min-w-0` to both direct grid children. Re-verified: grid track now correctly sizes to 358px (matching its container) at 375/390px widths, with zero overflow at both the element level and the document level, and correct behavior confirmed right at the `lg:` (1024px) breakpoint boundary from both sides (1023px and 1024px). **Lesson: `document.documentElement.scrollWidth <= clientWidth` is *not* sufficient proof that nothing is visually overflowing once a page has a global `overflow-x: hidden` safety net — that net converts a page-wide scrollbar bug into a silent content-clipping bug instead, which is arguably worse (invisible in the one check most likely to be run). Any new CSS Grid or Flexbox layout with fixed-size children (thumbnails, icons, buttons) sitting alongside a `flex-1`/shrinking text element needs `min-w-0` on the shrinking container(s) as a matter of course, and should be verified with an element-level `getBoundingClientRect()` check at the narrowest supported viewport, not just a document-level scrollbar check.**

### Seventh pass — Home 1 hero + testimonials redesign, and a filterable 8-card Categories rebuild
Three targeted requests: replace Home 1's hero background photo (client called it "dull") and strengthen its typography/CTAs/animation; completely redesign Home 1's testimonials section away from its Swiper-card layout; and rebuild `categories.html` again with a filter bar and exactly 8 cards instead of 6, since the client wanted the two editorial tiles (Bestsellers, Local Farms) back but with interactive filtering this time rather than a static grid.

**Hero (`index.html`):** replaced the background photo with a bright, vivid, crystal-clear studio-shot vegetable arrangement (`photo-1597362925123`, white background, high color saturation) — a deliberate contrast from the previous dim grocery-cooler photo, with the left-side gradient overlay strengthened slightly (`black/60` → was `/55`) to keep white text legible against the new photo's much brighter background. Added a slow 18-second Ken Burns zoom (`.hero-kenburns`, `scale(1)→scale(1.08)`, `prefers-reduced-motion`-safe) as the "modern animation" ask. Added a small trust-signal row (3-avatar stack + star rating + "Loved by 200+ neighbors") between the intro paragraph and the CTA buttons, reusing the same customer-photo-as-social-proof pattern the testimonials redesign below also uses.

**Testimonials (`index.html`):** replaced the 5-card Swiper carousel with a **featured-quote-plus-bento-grid** layout — one large dark `.testi-featured` card (bigger type, avatar-ring, "Verified customer" label) beside a 2×2 grid of four standard `.quote-premium` cards, with a `4.9 · from 200+ reviews` rating readout in the section header. Both card types got a new hover lift (`translateY` + shadow). This is the site's *fifth* distinct testimonial treatment (after Home 1's original 3-card static grid, Home 2's Swiper fade, About's asymmetric spotlight, and the removed Home 2 manual-nav slider) and — like the Trending rebuild two passes ago — deliberately drops Swiper for this section. With no section on `index.html` using Swiper any longer (Trending was rebuilt away from it in the sixth pass), the Swiper CDN `<link>`/`<script>` tags were removed from `index.html` entirely rather than left in as dead weight.

**`categories.html` — filterable 8-card rebuild:** added a filter bar (`All` + the 6 real aisles) above the grid, reusing the existing `data-filter-btn`/`data-filter-item` mechanism from `initCategoryFilter()` (previously wired to a filter grid on `offers.html` that was removed during the fifth pass's simplification — the JS function had been sitting unused ever since). Grid expanded from 6 to 8 cards (6 aisles + the "This Week's Bestsellers" and "Meet Our Local Farms" editorial tiles from the original launch concept, both tagged `data-filter-item="all"` so they appear only under the "All" filter, not under a specific aisle). Each card gained an explicit `.card-cta` text-plus-arrow call-to-action (e.g. "Shop Produce →") layered over the existing full-card link and hover-zoom image, directly answering the "clear CTA buttons" requirement. Grid remains exactly 2 rows at desktop (`lg:grid-cols-4`) to keep the "avoid long scrolling" requirement.

**Bugs found and fixed during this pass (before sign-off):**
1. **`initCategoryFilter()` had been silently broken since it was first written** (in an earlier pass, for the now-removed `offers.html` filter grid) and nobody had ever visually caught it because that feature was removed before any scripted interaction test covered it. The click handler only ever set the `aria-selected` *attribute* on filter buttons; the actual visual "active" styling is driven entirely by an `.is-active` *class* (`.category-chip.is-active { background: rgb(var(--color-primary)); ... }`). Since nothing in the click handler ever touched `.is-active`, clicking a filter button would correctly re-filter the cards but the *visual* active state would never move off the initially-hardcoded button — a real, dormant bug that had simply never been exercised end-to-end before. Caught by re-reading the JS function before reusing it for the new Categories filter bar (on the theory that anything about to be reused for the first time in a new context deserves a fresh read, not just a name-match assumption that it works), then confirmed with a scripted click-through test asserting `classList.contains('is-active')` after each filter click, not just checking that cards were hidden/shown correctly. **Fix:** the click handler now toggles `.is-active` alongside `aria-selected` on every button. **Lesson: a JS function that was written for a feature later removed before ever being visually exercised is not "known-working" just because it shipped without errors — re-verify it as if new the next time it's reused, especially checking that every piece of state the CSS depends on (classes) is actually being set, not just the accessibility attributes.**
2. **Two of three newly-sourced images for the Categories page's editorial tiles had real disqualifying content**, caught only by the fetch-then-Read visual-verification step (not by alt-text or search-result descriptions, which described both as clean): a "grocery haul" photo intended for "This Week's Bestsellers" had *five* different real, legible product brands baked into the packaging (Maple-brand syrup, Almond Breeze, Edgell corn, Milka chocolate, a "Higher Living" tea box); a farmer portrait intended for "Meet Our Local Farms" had a full tiled "Unsplash+" watermark repeated across the entire frame (a premium/Unsplash+ photo, despite the search listing not flagging it as such in the alt text). A third candidate for the same "Bestsellers" tile was also rejected after visual check for **visible foreign-currency crate price tags** ("330 K", "80 K" — matching the standing lesson about incidental signage in market-stall photography). **Fix:** all three were replaced with fetch-then-Read-verified clean alternatives before being committed to the page. **Lesson (reinforced, not new): this template's image-sourcing failure rate for "clean, unbranded, unwatermarked" candidates has consistently run at roughly 2-in-3 rejected across every pass that required fresh photos — budget for multiple rounds of visual verification as the default expectation, not the exception, especially for categories (grocery hauls, farmer portraits, market-stall shots) that have repeatedly produced contaminated results throughout this project's history.**
3. **The new hero trust-badge and testimonials-section avatar images were initially set to reuse the same 5 named-customer photos already used in the testimonial cards below** (e.g. Renata's and Priya's photos appearing a second time each as generic "social proof" avatars), a direct violation of the template's standing "never reuse the same image anywhere" policy. Caught by re-running the site-wide `uniq -c` duplicate audit immediately after drafting the hero and testimonials sections, before moving on to Categories — consistent with the established practice of auditing after each section rather than only at the very end. **Fix:** removed the redundant avatar-stack from the testimonials section header entirely (kept just the numeric rating, since a second "sample of customers" visual so close to the named testimonial cards below it was redundant content, not just a duplicate-image problem) and sourced 3 fresh, unique customer portraits specifically for the hero's trust badge. **Lesson: decorative "social proof" avatars (trust badges, summary stacks) are exactly where accidental image reuse creeps in, because it's tempting to reach for "a customer photo" generically without checking whether that specific photo is already committed to a named testimonial elsewhere on the same page — treat every avatar/portrait placement as needing its own dedup check, not just product/category photography.**

### Eighth pass — categories.html rebuilt again as a full filterable 72-product catalog
The client's direction reversed course from the fifth pass's "simplify to 6-8 cards" request: this time `categories.html` was asked to become a proper filterable product catalog — a category filter bar (All + the 6 real aisles), exactly 12 items visible at a time in a 4×3 grid, and each item now needing a title, description, HD image, star rating, price, an optional New/Popular/Best-Seller badge, and an explicit CTA button. Read literally, this meant every one of the 6 aisle filters needed its own bank of 12 priced products (72 unique products total), with "All" showing a curated 12-item cross-section rather than everything at once — reconciling "avoid long, cluttered layouts" with "12 items per filter" by ensuring only one 4×3 grid's worth is ever visible at a time, swapped by the filter bar rather than stacked.

**Image sourcing at scale:** rather than sourcing all 72 images from scratch, the ~40 individual product photos removed from `categories.html` and `offers.html` during the fifth pass's simplification (and still sitting unreferenced anywhere on the live site) were re-identified from this session's own history, re-verified as still unused and still HTTP 200, and reused as a 39-image head start — cutting the fresh-sourcing requirement to 33 new photos across the 6 categories. All 72 final image URLs were bulk-verified for HTTP 200 and zero site-wide duplication before being committed to the page.

**Bugs found and fixed during this pass (before sign-off):**
1. **The "All" filter showed all 72 products instead of a curated 12**, because `initCategoryFilter()`'s matching logic was `value === 'all' || cats.indexOf(value) !== -1` — a hard-coded special case that always treats `"all"` as "show everything, unconditionally," a holdover from every earlier use of this function where "All" genuinely meant "no filtering." That assumption broke as soon as "All" needed to mean "show this specific curated subset" instead. Caught immediately by a scripted click-through test that asserted the visible count after every filter button, not just spot-checking one or two. **Fix:** removed the special case entirely — `data-filter-item="produce all"` on 2 items per category makes `"all"` a literal tag like any other, matched the same way as `"produce"` or `"dairy"`. Verified: `all` → 12, and each of the 6 aisle filters → 12, with the 4-column grid confirmed via `getComputedStyle`. **Lesson: a shared filter utility's "match everything" special case is only safe as long as every consumer actually wants that behavior — the moment any one usage needs "all" to mean a specific subset rather than "no filter," the special case must be removed for every consumer, not special-cased further, since a per-page override would silently reintroduce the same bug the next time the function is reused.**
2. **A recycled "Vine-Ripened Tomatoes" photo — already vetted and in production since a much earlier pass — turned out to have a legible, mismatched price sign baked into the shot** ("ORGANIC FRESH CHERRY TOMATOES $4.00 BOX," a different product name and price than the card's own "Vine-Ripened Tomatoes, $2.79/lb"). This photo had passed review before specifically because it was displayed small in a dense 24-card directory; promoted to the #1 "Best Seller" position in a premium 4-column, larger-card catalog, the sign became clearly legible. This is the same "branding risk scales with display size" lesson recorded in the seventh pass, now confirmed as a recurring, not one-off, failure mode for this template — **any image being reused from an earlier, smaller placement must be re-verified at its new size before reuse, every time, not just the first time this lesson was learned.** Replaced with a close-up vine-tomato photo with no signage, re-verified clean, unique, and HTTP 200.
3. Two further candidates were pre-emptively rejected during initial sourcing before ever reaching the page: an olive oil bottle shelf with a real, legible Spanish brand ("Rafael Salgado — Packed in Spain") repeated across every bottle in frame, and an artisan candle with legible product-label text ("Poured by Fi — Calmness") — a small/independent brand rather than a national one, but still real, legible product branding, treated as disqualifying under the same standard regardless of the brand's size or recognizability. Both were swapped for clean alternatives (a spice-spoon shot for the pantry slot, a plain unlit taper candle for the household slot) before either was ever committed to a page, consistent with the project's now-standard fetch-then-Read verification step running *before* an image is written into HTML, not as a post-hoc audit.

### Ninth pass — `categories.html`: "Add" buttons with a toast, richer per-card content, and two more sections
Three changes: replace every product card's "View Details" link (which navigated to `offers.html`) with an "Add" button that behaves like a real add-to-list action — text flips to "Added," stays that way, shows a toast in the top corner, never navigates or reloads; add more per-card substance (an availability line, on top of the rating/price/badge already added in the eighth pass); and bring back page-level content the client felt was missing.

**"Add" button + toast:** all 72 `<a href="offers.html">View Details</a>` links (textually identical, replaced in one `replace_all` pass) became `<button data-add-to-list data-persist>Add</button>`, reusing the same site-wide add-to-list mechanism already powering the Trending section and Offers cards rather than inventing a parallel one. Two small, backward-compatible additions were made to the *shared* `initListAndSteppers()` handler in `main.js`: a new `showToast()` helper (creates a `.toast-root` container lazily on first use, stacks multiple toasts, auto-dismisses each after ~2.9s, mirrors to the top-left under `dir="rtl"`) fires on every `data-add-to-list` click site-wide; and a new opt-in `data-persist` attribute skips the existing 1.4s revert-to-original-icon behavior for buttons that have it. Verified both paths explicitly: a `data-persist` button on `categories.html` stays "Added" past the old 1.4s window, while a plain `data-add-to-list` button on `index.html`'s Trending section (no `data-persist`) still reverts on schedule — confirming the change is additive and didn't regress the carousel/list buttons built in earlier passes.

**Richer cards and page:** added a color-coded availability line to every card (`.availability.in-stock` / `.low-stock` / `.restocked`), varied by badge — Best Seller → "Only a few left," New → "Just restocked," Popular → "Selling fast," everything else → "In Stock" — set programmatically (a Node one-off script matching each badge line to its availability line 10 lines below it, since all 72 cards share the exact same template structure) rather than hand-edited 18 times. Restored the "Why Shop With Us" stat strip (now with animated `data-count-to` counters) and the Offers/Bulk-Order CTA band that existed in the sixth-pass version of this page but were dropped when the page was rebuilt as a 72-product catalog in the eighth pass — their absence, plus the page ending abruptly after the product grid, was almost certainly what read as "too empty." Also added a live `[data-filter-count]` readout under the filter bar ("Showing 12 of 72 products across all aisles" / "Showing all 12 Produce products"), updated by `initCategoryFilter()` on every click, giving the page a small but genuine piece of dynamic, data-driven content rather than static copy.

**No new images this pass** — every change was structural/behavioral (buttons, JS, two restored sections built entirely from existing icon components), so the site-wide image count, dedup, and HTTP-status sweep were re-run only to confirm zero regressions, not because new photography was sourced. **Lesson: not every "make the page richer" request means more photography — data-driven UI (live counts, count-up stats, varied availability states generated from existing badge data) adds genuine content density without touching the image budget or reopening the branding/watermark review surface at all.**

### Tenth pass — `index.html` Shop by Craving + Fresh Picks Gallery redesign, `home-2.html` hero redesign, and a site-wide watermark fix
Three redesign requests plus one significant defect discovered and fixed proactively along the way.

**`?cat=` deep-linking (prerequisite, `main.js`):** `categories.html`'s filter bar previously had no way to be linked into from another page in a pre-filtered state — the old `categories.html#produce`-style anchor links used by the original "Shop by Craving" section were already silently broken (the `id="produce"` etc. anchors were removed from the markup during the eighth-pass 72-product rebuild and never noticed, since a dead in-page anchor fails silently — it just scrolls to the top of the page instead of erroring). `initCategoryFilter()` was refactored to extract a shared `applyFilter(btn, opts)` helper and add `URLSearchParams` support: `categories.html?cat=dairy` now finds and clicks the matching `[data-filter-btn]` on load. The query value is validated against `/^[a-z]+$/` before being interpolated into a `querySelector` attribute-selector string — a raw, unsanitized value could otherwise break out of the selector (a `"` in the query string) and throw an uncaught `DOMException` that would abort the rest of the filter bar's setup. **Lesson: a dead in-page anchor link is a silent failure (no console error, no broken-link tool catches it) — before wiring a new "link into a filtered/scrolled state on another page" feature, explicitly grep the target page for the anchor `id`s the new links assume exist, rather than trusting that a plausible-looking `#hash` still resolves to something.**

**Shop by Craving → asymmetric bento grid:** replaced the 4-card uniform `mood-card` grid (which grouped products by *meal occasion* — "Breakfast Table," "Weeknight Dinner" — only loosely mapped to `categories.html`'s actual 6 aisles) with a new `.craving-grid` CSS-grid-template-areas layout showing all 6 real aisles (Produce, Dairy & Eggs, Bakery, Pantry, Beverages, Household) at genuinely different sizes — one large 2×2 feature tile, two medium tiles, and three small tiles, not a repeated card size — each linking via the new `?cat=` mechanism straight to its pre-filtered `categories.html` view. Reused the existing `.bento-card`/`.bento-overlay`/`.bento-desc`/`.card-cta` component family (previously used only for About's team-photo grid) but added a visible always-on `.card-cta` "Shop [category] →" line plus a hover-revealed `.bento-desc` sentence, since the brief explicitly asked for a "strong CTA button," not just an icon.

**Fresh Picks Gallery → editorial masonry:** replaced the flat 6-column uniform `aspect-square` grid with a second, distinct `.gallery-editorial` CSS-grid-template-areas layout (one wide feature tile, two tall tiles, two more tall tiles, two small tiles — a different area map than the craving grid, so the two redesigned sections don't visually rhyme) with a new `.gallery-tile`/`.gallery-overlay`/`.gallery-caption` component: an always-visible category label + product name, plus a caption sentence that expands in on hover (`max-height` transition). Each tile links into `categories.html?cat=…` as well, giving the gallery a real navigational purpose it didn't have before (it was purely decorative).

**`home-2.html` hero → centered editorial + rotating badge + photo strip:** the brief required this hero to be structurally, not just cosmetically, distinct from `index.html`'s hero. Where Home 1's hero is a full-bleed dark photo background, left-aligned content, a 2-photo offset collage, and a `data-word-cycle` animated headline, the new Home 2 hero is a **light** `--color-bg-alt` background with decorative blobs, **centered** text, a **static** headline (no word-cycle — a deliberately different animation surface, not a repeat of the same mechanic with different words), and a 4-tile horizontal photo strip below the fold-line CTA row with alternating tile heights (`lg:mt-10` on the 1st/4th tiles only) rather than a 2-photo overlapping collage. The previously-built-but-never-used `.spin-badge`/`.spin-badge-wrap` component (a `<textPath>` SVG rotating circular badge, sitting dormant in `style.css` since an earlier, replaced Home 2 concept) was finally put to use here as a "FRESH DAILY · LOCAL ROOTS · EST. 2011 ·" rotating seal next to the CTA buttons — a decorative element that exists nowhere else on the site.
- **Theme bug caught during QA, not by the user:** the photo-strip's caption pill (`.hero-strip-label`) was styled with a fixed white background but `color: rgb(var(--color-ink))` for the text — correct in light mode, but `--color-ink` is a theme variable that flips to a near-white value under `:root[data-theme="dark"]` *and* under a bare `@media (prefers-color-scheme: dark)` match with no `data-theme` attribute present at all (confirmed via a fresh, localStorage-less Puppeteer browser context, which defaulted to invisible near-white-on-white text). The original fix only special-cased `[data-theme="dark"]`, which fixed the explicit-dark-toggle case but not the OS-preference-only case. **Fix:** hardcoded the label to a fixed dark color (`rgb(32 26 20)`) instead of the theme variable, matching the same theme-independent-chip convention already used by `badge-glow` and the bento/gallery overlay text elsewhere on the site (fixed colors for any UI element that sits on top of a photo, since photos don't change with the page theme). **Lesson: theme-variable text color paired with a hardcoded (non-variable) background is only safe if every code path that can set the theme is covered — `[data-theme="dark"]` alone misses the `prefers-color-scheme` fallback path, which a naive "toggle dark mode and look" QA pass won't catch since toggling always sets the attribute explicitly. Test the *zero-attribute* default state in an isolated browser context, not just the explicit toggle states.**

**Watermark defect discovered and fixed (site-wide, not scoped to this task):** while sourcing new photos, the fetch-then-Read verification step revealed that `plus.unsplash.com/premium_photo-*` URLs render with a tiled "Unsplash+" watermark when accessed without a license — invisible to every check this project relies on (HTTP-200 sweep, dedup grep, alt-text review), since the watermark only shows up on the actual decoded pixels. All ~15 `premium_photo-` URLs already live across the site (14 in `categories.html`, 1 testimonial avatar in `index.html`) were confirmed watermarked by spot-checking two of them, then all 15 were replaced with re-sourced, fetch-then-Read-verified `images.unsplash.com/photo-*` (standard, non-Plus) equivalents matching each original subject/alt-text as closely as possible, with two intentional near-substitutions where a truly clean match for the literal subject couldn't be found without a defect (a rolled-towels photo standing in for "laundry detergent bottle," and a 4-candle photo standing in for the described metal-holder single candle) — alt text was rewritten in both cases to accurately describe what the replacement image actually shows, rather than leaving a caption that no longer matched. **Lesson for all future templates: `plus.unsplash.com` (Unsplash+/premium) URLs must be treated as never-safe-to-use without a paid license, full stop — not just "verify like any other image." Restrict all sourcing to `images.unsplash.com/photo-` (standard) URLs going forward, and if a `plus.unsplash.com` URL is ever found already in a project, assume it's watermarked and replace it rather than spot-checking a sample and hoping the rest are fine — the failure mode is invisible to every automated check this project already runs.**

**Image sourcing:** 6 new photos (Shop by Craving), 7 new photos (Fresh Picks Gallery), 4 new photos (Home 2 hero strip), plus 15 replacement photos (watermark fix) — 32 total, every one sourced via `WebFetch` on Unsplash search-results pages and verified via the fetch-then-Read technique before use. Rejection rate stayed consistent with prior passes (roughly 1-in-2 to 1-in-3 candidates rejected for legible brand names/logos on product packaging, foreign-currency price tags, mismatched signage, or — newly — the tiled Plus watermark); the household cleaning-product category in particular required double-digit candidate rounds since minimalist product photography on Unsplash is disproportionately branded mockup/demo content (fake placeholder wordmarks like "Spray Bottle mockups" or "cosmétiques.") or real eco-brand product shots (GOGONANO, Heinz, Campbell's). Final sweep: 114 unique image URLs site-wide, zero duplicates, 100% HTTP 200, zero `plus.unsplash.com` references remaining anywhere in the template.

---

## Template 05 — Hardware & Paint Store

- **Brand name:** Anvil & Hue Hardware + Paint Co.
- **Tagline:** "Everything to Build It. Every Shade to Finish It."
- **Business category:** Hardware, tools, paint and electrical/plumbing supply store (Chennai, India — trade + retail, no e-commerce checkout, no dashboard)
- **Primary color:** Warm amber-orange `#EA6A12` (500) — CTAs, gradient-text accent, brand
- **Secondary color:** Muted steel blue `#3A7CA8`/`#2A5C82` — secondary panels, category imagery accents
- **Accent:** Gold `#C99100` — star ratings, colour-of-the-month, badges
- **Neutral / background:** Warm off-white `#FAFAF9` (light), true near-black `#0B0D10` (dark) — a *plain neutral* system (no tinted/brown/kraft base like T1/T2/T3), deliberately closer to a SaaS/DTC-commerce canvas than a "hardware store" cliché
- **Font combination:** Sora (display, geometric extrabold headings) + Inter (body) — a clean modern pairing, structurally distinct from every prior template's type system (T1 serif+script+sans triple, T2 rounded Baloo+Quicksand, T3 angular Space Grotesk+Inter, and distinct from this template's own *first-draft* condensed-industrial Barlow Condensed+Work Sans, replaced mid-build — see redesign note below)
- **Header type:** Logo-left / **right-aligned nav, not centered** (a first for this registry — T1/T2/T3 all center the nav group) — sticky **glass header** (`backdrop-filter: blur(16px)`, translucent bg, hairline border, shadow only appears after scroll). "Home" is a small dropdown (Home 1 / Home 2, click/hover, same mechanism as "Shop") rather than a plain nav link, added after an explicit fix request — see changelog.
- **Navigation style:** Home dropdown, Shop mega-menu (4-column: Tools/Paints/Electrical image-thumbnail categories + a dark "Trade & Contractor Pricing" promo tile, rounded-2xl panel with soft shadow — not full-bleed), Paint Colours, Brands We Stock, About, Contact (Contact and a phone-CTA pill stay directly visible per the master-prompt rule).
- **Hero layout (Home 1):** Editorial two-column hero over a **mesh-gradient + soft blob backdrop** (radial-gradient blend, not a flat texture): eyebrow pill, extrabold headline with a **gradient-text** accent phrase, stat trio below a hairline divider, dual pill CTAs. Right side is a **3-photo bento collage** (one large `col-span-3 row-span-2` tile + two stacked tiles) with **pointer-driven 3D tilt** on each tile and a glass (`backdrop-filter`) floating "Price Match Guarantee" card overlapping the bottom-left corner.
- **Hero layout (Home 2):** Centered, image-free hero — pegboard/pill-tag eyebrow replaced by 4 floating circular icon badges (hammer/paint/bolt/faucet) orbiting the headline at soft rotated angles, gradient-text on the headline's second line, star-rating trust line, no photo at all — the site's second distinct hero mechanic (photography-heavy vs. photography-free), a deliberate "lighter/faster-feeling" alternate homepage.
- **Hero layout (inner pages):** Compact `rounded-[2rem]` **rounded-corner** photo banners (not full-bleed, not diagonal-cut) with a bottom gradient-overlay caption block and breadcrumb — every inner page uses a distinct, verified photo.
- **Section order (Home 1):** Header → Hero (bento collage) → Category bento grid (asymmetric: 1 large 2×2 + 3 varied tiles, dark gradient-overlay captions, not a uniform 4-up grid) → Brand marquee (auto-scroll text wordmarks, pause-on-hover) → Best-Selling Essentials (6-card grid) → Paint Shade Finder teaser (12-chip swatch preview + link) → Why Choose Us (dark section, noise-overlay + glass stat tiles, pointer-tilt) → Paint Calculator + Trade Account split promo → Store Hours & Location (map + hours table with today auto-highlighted + click-to-call) → Testimonials (scroll-snap strip + prev/next) → Gradient CTA banner (rounded, blob backdrop) → Footer.
- **Card style:** `.card-modern` — rounded-3xl, hairline border, hover lifts `-8px` with a soft diffused shadow plus a subtle primary-tinted ring (`box-shadow` glow), not a hard flat card — deliberately soft/DTC rather than flat/industrial.
- **Button style:** `.btn-pill` — fully rounded; primary variant is a gradient fill with a **diagonal shine sweep** on hover (`::before` translateX wipe) plus a colored glow shadow and lift; outline/light/ghost variants for secondary contexts.
- **Image style:** Bento photo collages (Home 1 hero, category grid), rounded-corner compact banners (inner pages), plain catalogue-style product photography on flat/icon-tile backgrounds (Products), pure-CSS colour swatches (no photography) for the paint library, glass-panel floating callouts.
- **Background style:** Plain warm-neutral canvas; **mesh-gradient** (layered radial blends of primary/secondary/accent at low opacity) reserved for hero/404 sections only; a **grain/noise overlay** (`mix-blend-mode: overlay`, SVG turbulence) adds depth to dark sections without a repeating pattern motif (no dot-grid, no pegboard, no diagonal stripes — deliberately avoids every prior template's literal texture device).
- **Animation style:** ScrollReveal.js (`data-reveal="up|left|right|scale"`) as the site-wide scroll-reveal engine; a hand-rolled **pointer-driven 3D tilt** (`data-tilt`, `perspective()/rotateX/rotateY`, gated to `(pointer:fine)` + `prefers-reduced-motion: no-preference`) on hero collage tiles, category bento tiles and dark-section stat cards — a new interaction pattern for this registry; CSS marquee for the brand-logo strip; drag/wheel horizontal scroll-strips (testimonials, deals, milestones); vanilla accordion/tabs/filter/lightbox, no other external animation library.
- **Footer layout:** `.footer-modern` — dark near-black panel with **rounded top corners** (`border-top-left/right-radius: 2.5rem`), not a zigzag/wave/scalloped edge; standard 4-column grid (brand+social / shop / customer care / hours+call CTA).
- **Mobile menu pattern:** **Right-side glass-backdrop slide-in drawer** (`backdrop-filter: blur` scrim + a `rounded-l-3xl` panel translating in from the edge, RTL-aware — docks left and rounds the opposite corner under `dir="rtl"`) — "Home" and "Shop" both render as inline accordions inside the drawer.
- **Special design features:** Dark/light mode toggle + RTL/LTR toggle (localStorage, `dir` attribute), **paint quantity calculator** (wall length/height/coats/openings → litres, `Math.ceil` rounding, unique to this template), **interactive paint colour library** (48 swatches across 6 filterable families, click-to-inspect detail panel, `navigator.clipboard` copy-code button), brand wall with category filter (text-wordmark cards, not logo image files — avoids trademark/sourcing risk while keeping realistic brand names), trade & contractor account form with dynamic add/remove order-line rows and a JS-set `min=today` delivery-date field, store-hours table with today's row auto-highlighted via `Date().getDay()` (a lighter-weight alternative to a live open/closed badge), FAQ live search with proper expand/collapse reconciliation on every keystroke, click-to-call `tel:` links throughout, Google Maps `output=embed` iframe (no API key).
- **Pages built:** index.html (Home 1), home-2.html (Home 2), about.html, products.html (Tools/Electrical/Plumbing aisles with subcategory filter pills), paints.html (colour library + calculator + finishes), brands.html (20-brand filterable wall), contact.html (map/hours/click-to-call + general + trade forms + FAQ preview), faq.html (topic-grouped, live search), 404.html.
- **Libraries used:** Tailwind CSS (CDN, class dark mode), Font Awesome 6 (icons), Google Fonts (Sora/Inter), ScrollReveal 4 (scroll reveal). No Swiper, no AOS, no GSAP, no GLightbox (lightbox is a small vanilla implementation), no CountUp.js (stats are static, matching a "practical, no-frills" brief). No build step; no backend.

**Do not reuse for future templates:** right-aligned (non-centered) desktop nav with a small click-dropdown "Home" switcher, glass/blur sticky header as the *primary* header treatment, mesh-gradient hero backdrop + gradient-text headline combo, 3-tile asymmetric bento photo collage hero (Home 1) / icon-orbited photo-free centered hero (Home 2), pointer-driven 3D tilt (`data-tilt`) micro-interaction, rounded-top-corner dark footer, right-side glass-backdrop slide-in drawer mobile menu, interactive paint-colour-library-with-clipboard-copy component, paint quantity calculator tool, amber-orange/steel-blue/gold/warm-neutral palette, Sora+Inter pairing.

### Redesign pivot — industrial theme scrapped for modern minimal/DTC-commerce
The template was fully built once already (Barlow Condensed + Work Sans, safety-orange/hazard-yellow palette, diagonal clip-path hero, `.crate-card` bolt-corner cards, `.btn-stamp` clipped-corner buttons, pegboard-dot texture, sawtooth-edge footer, roller-shutter mobile menu) before the client rejected the direction outright as "not good, not modern — completely redesign." Rather than iterate on the industrial concept, the whole visual system was replaced end-to-end (confirmed via `AskUserQuestion` against four named directions — modern-minimal/DTC, bold-editorial, warm-retail, dark-premium — before rebuilding, since "modern" is not a self-evident spec and a second wrong guess would have doubled the wasted rebuild cost). Every one of the industrial-era class names (`.btn-stamp`, `.crate-card`, `.pegboard`, `.sawtooth-top`, `.ruler-edge`, `.diagonal-hero`, `.diagonal-panel`, `.hazard-tag`) was deleted from `style.css` and swept for zero remaining references across all HTML files before sign-off — a full rename/reskin in place (keeping the old class names but changing their CSS) was considered and rejected, since the brief's "fresh layout... unique sections" language meant the *structure* needed to change too (diagonal clip-path → rounded bento grid, flat stamp buttons → gradient pill buttons with shine sweep), not just the surface colors. A second follow-up ("still doesn't look modern/premium, ThemeForest-quality") drove a further pass adding mesh-gradient backdrops, glass panels, gradient-text headlines, pointer-tilt micro-interactions and an asymmetric bento hero/category layout on top of the already-rebuilt flat system — recorded as a lesson that "modern minimal" alone can still read as generic without a genuine depth/motion layer on top of rounded-corners-and-soft-shadows.
**Lesson for future templates:** when a client rejects a *finished* visual direction wholesale, don't default to a quick CSS class reskin — confirm the replacement direction explicitly (a 2-minute question) before a full rebuild, since guessing wrong on "modern" a second time costs a second full rebuild. Separately, "modern minimal" and "premium/ThemeForest-quality" are not the same bar — soft shadows and rounded corners alone read as competent-but-generic; premium requires an additional, deliberate layer (gradient mesh, glass, tilt, gradient-text, asymmetric/bento composition) on top of the base system.

### Image sourcing note
All images were sourced via `WebFetch` on Unsplash search-results pages, and the first extraction attempt returned the *short page-slug* photo ID (11-character, from `/photos/<slug>` URLs) rather than the long-form CDN id — every one of these resolved to a 404 when actually requested (`images.unsplash.com/photo-<shortslug>` is not a valid CDN path). A corrected prompt explicitly asking for the long numeric-timestamp+hash id fixed this for all subsequent sourcing, and the initial broken batch was re-resolved and replaced. Separately, mid-build, six `plus.unsplash.com/premium_photo-*` (Unsplash+) URLs were used for a handful of images before recalling T3's registry lesson (above) that Plus URLs render with a paid-tier watermark — caught via a source review before user-facing sign-off (not by visually spot-checking a render) and replaced with `images.unsplash.com/photo-*` standard equivalents, re-verified via a site-wide `curl` HTTP-200 sweep (34 unique URLs, zero `plus.unsplash.com` remaining). **Lesson: when sourcing many images quickly, re-check new batches against both established lessons (short-ID vs. long-ID, Plus-vs-standard domain) rather than assuming the fix from the first batch will hold for the rest — a corrected prompt/process can still regress under time pressure later in the same session.**

---

## Template 06 — Gift & Card Shop

- **Brand name:** Ribbon & Twine Gift & Card Co.
- **Tagline:** "Every Gift, Perfectly Tied." (Home 1) / "What are you celebrating?" (Home 2)
- **Business category:** Local gift and greeting-card shop (Maple Hollow, Ohio) — occasion-based gifting, in-store personalization/engraving, same-day in-person pickup; no e-commerce checkout, no dashboard
- **Primary color:** Plum `#9B2C6F` (500) — CTAs, headline accents, active states
- **Secondary color:** Jade `#1F7A5C` (500) — trust badges, secondary icons/accents
- **Accent:** Blush `#EE97A8` / `#F3B8C2` — decorative confetti, hero blob glows, soft highlight fills
- **Neutral / background:** Warm paper cream `#FEFBF6`→`#F3E8D8` (light), deep aubergine-ink `#1D1220`→`#150C18` (dark) — a plum/aubergine dark mode, deliberately distinct from every prior template's dark palette (T1 choco-brown, T2 indigo-navy, T3 near-black, T5 near-black)
- **Font combination:** Bricolage Grotesque (variable, display headings) + Karla (body) — a 2-font pairing with a distinct structural character (quirky variable-optical-size grotesque) from every prior pairing on the site (T1 serif+script+sans, T2 rounded Baloo+Quicksand, T3 angular Space Grotesk+Inter, T5 Sora+Inter)
- **Header type:** Logo-**centered**, split navigation either side (left group: Home dropdown, Shop by Occasion, Personalization; right group: Gallery, About, FAQ) — a header shape not yet used on this site (T1 = unified single row, T2 = two-level w/ centered-nav, T3 = ?, T5 = right-aligned nav). Above it, a thin dark **utility strip** (same-day-pickup note + phone + address, always visible, not scroll-hiding). Desktop nav gates at `xl` (1280px) per the site-wide lesson.
- **Navigation style:** Only "Home" carries a dropdown (Home — Classic / Home — Card Fan, per the master-prompt's "no other dropdowns" rule), every other link is a plain top-level link; Contact stays reachable via a persistent "Visit / Reserve" CTA button rather than a nav link.
- **Hero layout (Home 1 — `index.html`):** **Die-cut gift-tag photo frame** — a real gift photo clipped into a rounded gift-tag silhouette via CSS `clip-path: path(...)`, complete with a punched string-hole (`.gift-tag-hole`) and a gentle hanging-sway keyframe (`.tag-sway`), floating confetti-piece SVGs, and a floating "2 hrs average pickup wait" stat card overlapping its corner. Text column: badge pill, serif-free extrabold headline, dual CTA, three "same-day pickup / free gift wrap / on-site engraving" trust chips. A border-top stat strip (years in business, gifts personalized yearly, occasion collections, pickup wait) uses vanilla `data-count-to` counters (IntersectionObserver-driven, no CountUp.js).
- **Hero layout (Home 2 — `home-2.html`):** **Fanned greeting-card rack** (`.card-fan`/`.fan-slot`) — a central hero photo flanked by two rotated, color-blocked "card" panels (birthday/anniversary emoji + copy) fanned out at offsetting angles, image **left**/text **right** (mirrored from Home 1). Text column carries a "Quick Occasion Finder" chip row (7 occasions) that deep-links straight into `shop.html?occasion=<slug>`, pre-filtering the catalog on load — a functional hero component, not just decorative.
- **Hero layout (inner pages):** Compact banners, no two identical — photo+gradient overlay (About, Contact, Gallery), solid/gradient plum with a subtle pattern (Shop, Personalization, FAQ).
- **Section order (Home 1):** Header → Hero (gift-tag frame) → Stat strip → Shop by Occasion (7-card image grid, links into `shop.html?occasion=`) → Personalize It (split photo/copy + 2-up "finished example" thumbnails) → Shop-Floor Favorites (6-card product grid) → Why Last-Minute Shoppers Love Us (dark plum/ink icon band) → Notes from the Pickup Counter (testimonials) → Gallery preview (4-tile grid linking to `gallery.html`) → Newsletter/CTA band → Footer.
- **Section order (Home 2):** Header → Hero (card-fan + occasion finder) → Compact personalization banner (icon + photo strip, border-top/bottom, not a full section) → Age/occasion-agnostic product grid (5-card, distinct set from Home 1's 6) → remaining sections (process, testimonials, CTA) reordered and re-composed around the occasion-finder concept rather than repeating Home 1's stat-strip/gallery-preview layout verbatim.
- **Card style:** `.card-ribbon` — rounded-[1.75rem] card with a **folded-paper-ribbon corner** (`::before` CSS triangle in the top corner, color-coded per context via a `.ribbon-plum` modifier, RTL-mirrored corner via `[dir="rtl"]` override) and a small ribbon icon that fades/scales in on hover — a corner-fold motif not used by any prior template (T1 = scalloped edge, T2 = thick-border+tape-pin sticker, T5 = soft-glow lift).
- **Button style:** `.btn-fold` — solid plum pill-ish button whose bottom corner **unfolds a paper-fold triangle on hover** (`::after`, `border-width` grown via transition, RTL-mirrored), plus a plain `.btn-fold-outline` secondary variant — a distinct "paper-fold" button mechanic vs. T1's gradient-glow pill, T2's flat-bottom-shadow press, T5's shine-sweep gradient pill.
- **Image style:** Die-cut gift-tag clip-path hero photo (Home 1, new technique for this site), fanned rotated card-rack collage (Home 2), plain rounded-2xl split-section photography (About/Personalization), uniform `aspect-[4/3]`/`aspect-square` product-grid tiles with `object-cover`, GLightbox gallery grid with gradient-overlay hover zoom (`.gallery-tile`).
- **Background style:** Warm paper-cream base, no repeating site-wide texture pattern (unlike T1's sprinkle-dot or T2's confetti-dot) — texture is reserved for the die-cut hero and inner-page pattern banners only; plum/jade/ink full-bleed color-block sections for contrast bands.
- **Animation style:** **Zero external animation library** — a deliberate departure from every prior template (T1 = AOS, T2 = GSAP+ScrollTrigger, T5 = ScrollReveal.js): scroll-reveal is a hand-rolled `.reveal-tag` class driven by a single `IntersectionObserver` in `main.js`, stat counters use a vanilla `data-count-to` + IntersectionObserver combo (not CountUp.js), and the CSS keyframes are bespoke (`tag-sway` hero hang-sway, `confetti-float` drifting confetti pieces). GLightbox is used for the Gallery page only (all anchors carry explicit `type: image` per the site-wide lesson). The `initHorizontalScrollStrips()` wheel-redirect + drag-to-scroll fix (documented in T2's registry) was built in from day one for any `overflow-x-auto` strip, not added as a later correction. All motion respects `prefers-reduced-motion`.
- **Footer layout:** `.footer-perforated` — dark aubergine-ink panel whose **top edge is a repeating perforation/ticket-tear pattern** (`radial-gradient` circle-punch repeat, theme-aware fill color), evoking a torn price-tag/ticket edge — a new top-edge motif distinct from T1's scallop, T2's wave, T5's rounded corners. Standard 4-column grid (brand+social / shop links / customer care / visit-us address+hours+phone).
- **Mobile menu pattern:** Left-side (`inset-start-0`) slide-in drawer using CSS **logical properties** (`start-0`, `[dir="rtl"] .mobile-drawer { transform: translateX(+105%) }` mirrored automatically rather than via duplicated rtl-scoped markup) with a backdrop scrim; "Home" renders as an inline accordion (Home — Classic / Home — Card Fan) inside the drawer, matching the desktop dropdown's two options exactly.
- **Special design features:** Dark/light mode toggle + RTL/LTR toggle (localStorage, `dir` attribute flip), occasion-based catalog filter on `shop.html` with `?occasion=<slug>` deep-linking (read via `URLSearchParams` on load, shared by both homepages' occasion links), dedicated Personalization/Engraving page (process steps, turnaround, finished-example gallery), GLightbox photo gallery (8 shop-life photos, all `type: image`-tagged), vanilla IntersectionObserver scroll-reveal + count-up system (no third-party animation library — a first for this site), always-visible utility strip (same-day pickup cutoff time, phone, address) instead of a scroll-hiding bar, contact form with in-page validation + store hours/map for in-person pickup.
- **Pages built:** index.html (Home 1), home-2.html (Home 2), about.html, shop.html (occasion-filterable catalog), personalization.html (engraving/customization service), gallery.html (GLightbox photo showcase), faq.html, contact.html, 404.html.
- **Libraries used:** Tailwind CSS (CDN, class dark mode), Google Fonts (Bricolage Grotesque/Karla), GLightbox (gallery lightbox only). No Font Awesome (icons are inline SVG), no AOS/GSAP/ScrollReveal/Swiper/CountUp.js — scroll-reveal, counters and the horizontal-scroll drag-fix are all hand-rolled vanilla JS in `main.js`. No build step; no backend.

**Do not reuse for future templates:** centered-logo split-nav header with an always-visible (non-hiding) utility strip, die-cut gift-tag `clip-path` hero photo frame with a punched string-hole and hang-sway keyframe, fanned rotated card-rack hero collage (Home 2), folded-paper-ribbon-corner card motif (`.card-ribbon`), paper-fold-unfurl button hover mechanic (`.btn-fold`), perforated/ticket-tear footer top edge, logical-property (`start-0`) left-side slide-in drawer, plum/jade/blush/aubergine-ink palette, Bricolage Grotesque + Karla pairing, zero-external-animation-library vanilla IntersectionObserver reveal+counter system as the site's *primary* motion engine, occasion-based `?occasion=` deep-linking pattern between a homepage chip finder and a filterable catalog page.

### Build interruption and recovery note
This template's initial build ran across two background-agent sessions that both terminated early on an unrelated platform session-limit error (not a code defect) — once right after `index.html`/`style.css`/`main.js` were finished, and again after all eight remaining pages were drafted but before the 30 sourced product/gallery/contact photos had been substituted for their `TOKEN:<slug>` placeholders or this registry entry was written. Recovery was done directly in the primary session rather than re-spawning agents: every `TOKEN:` placeholder across all HTML files was inventoried via `grep`, the 15 already-verified URLs from the completed image-sourcing sub-task were substituted via a token→URL `sed` map, and the remaining 16 tokens (`product-*`, `gallery-*`, `contact-counter`) were freshly sourced and visually verified one-by-one using the same Unsplash-search → `WebFetch`-raw-image → `Read`-the-saved-file technique established by prior templates, rejecting several strong-sounding candidates that failed on closer visual inspection (a UK card-rack with visible `£1.99` price tags, two racks with German/Chinese card text, several candle/ribbon photos carrying real third-party brand labels like "Paddywax"/"Boy Smells"/a Hong Kong tourist-shop storefront/a Paris bookshop with French signage, a hair-salon counter with a visible "SumUp" POS logo, and a shop-front photo with a visible "DoorDash" bag logo) before landing on clean replacements — with three captions (`product-cardrack`, `product-photoframe`, `product-candle`, and one gallery caption) rewritten to accurately describe the substituted photo rather than left describing the original, unfulfilled request. **Lesson: when a multi-page build spans an interrupted session, `TOKEN:<slug>` placeholders (rather than partially-applied real URLs) made the incomplete state trivially auditable via a single `grep` — every remaining gap was explicit and machine-findable rather than requiring a visual page-by-page diff.** A full site-wide sweep after recovery confirmed 31 unique `images.unsplash.com` photo IDs (zero `plus.unsplash.com`, zero duplicates), all returning HTTP 200, zero leftover `TOKEN:` references, and zero lorem-ipsum/placeholder text.

### Full-site redesign pass — richer content, zero repeated images anywhere in the template
A follow-up request asked for every page to be redesigned with more content, modern layouts and unique sections, under a much stricter constraint than the initial build: **no photo may be reused anywhere else in the entire template — not across pages, and not twice on the same page** (the initial build had reused the same ~13 product/occasion photos across `index.html`, `home-2.html` and `shop.html` 2–3× each, and `shop.html`'s own 21-card grid had reused several photos across sibling products in the same occasion).

**Canonical-owner reassignment:** rather than re-sourcing the entire site from scratch, every already-verified photo was assigned to exactly one page/section as its permanent home (documented in a scratch ledger before editing began, to prevent re-collisions): the 13 occasion/product photos moved to `shop.html` as a curated **featured** subset; the 3 personalization photos stayed on `personalization.html`; the 9 gallery photos stayed on `gallery.html`; the 3 About photos and 1 Contact photo were untouched. Every page that had been borrowing one of these photos for a "preview" or "teaser" section had that section either (a) rebuilt as an icon/typography-driven component with no photo at all, or (b) given a freshly sourced, page-exclusive photo — never a photo already spoken for elsewhere.

**Policy adopted to keep 9 pages' worth of "richer, unique" content achievable without an unbounded photo-sourcing marathon:** real photography is reserved for hero sections and a small number of genuinely photo-appropriate feature moments per page (About's team/community story, Personalization's technique examples, Gallery's whole reason for existing); every category-navigation, quick-pick, trend, or teaser component that doesn't need a specific photographed subject was rebuilt as a **no-photo icon tile** (`.occasion-tile`, new component: colored icon roundel + heading + description, used for Home 1's occasion grid and several "favorite/best-seller" quick-picks) or a **no-photo product row** (`.product-row`, new component: icon + name + price in a compact dashed-border row, used for the 11 of `shop.html`'s 21 catalog items that don't carry a dedicated photo). This is a deliberate design choice, not a shortcut: alternating photo-heavy and icon/typography-only sections is a pacing technique already validated on sibling templates in this registry, and it's the only way "zero repeats, 9 pages, more content" is achievable without sourcing 60+ additional images.

**New sourcing (16 photos), each verified via the established WebFetch-raw-image → Read-saved-file technique, each assigned to exactly one page/section:** `index.html` gained 2 (an amber candle/wood-tray still life for "Shop-Floor Favorites," a kraft-paper-and-twine gift for the Personalization spotlight — chosen specifically because the twine bow ties directly into the "Ribbon & Twine" brand name); `home-2.html` gained 1 (a Dutch-tulip-shelf shot for a new "Seasonal Spotlight" section); `about.html` gained 1 (a small-town main-street-at-dusk shot for a new "Community & Downtown" section); `personalization.html` gained 2 (a cross-stitched monogram ornament, a purple watercolor hand-lettered card — the hand-lettering photo's palette happens to match the site's plum brand color); `gallery.html` gained 4 (cozy shelved-ceramics interior, hands tying a raffia bow, an intricate laser-cut wood mandala, a Valentine's plush-heart window display). `shop.html`, `faq.html`, `contact.html`'s photo counts were left as-is (contact/faq got content-only additions; shop.html's "more content" came from restructuring its existing 13 photos into a smaller featured set plus 11 new icon-only rows, not new photography).

**Rejected candidates during this pass** (same discipline as the original build): a CGI/3D-rendered gift-box mockup (broke the site's all-real-photography convention), a chocolate box with legible French branding + a maker's logo on the lid, a canvas tote with a visible "NEIRAMI" brand tag, two candle photos with real third-party brand labels ("Paddywax," "Boy Smells"), a European storefront with legible partial signage (wrong region for an Ohio shop), an old-fashioned Asian typesetting-shop photo (wrong craft/region for a boutique engraving studio), and a CNC-engraving-machine photo that 404'd on the CDN.

**Verification performed before sign-off:** a `grep`-based sweep confirmed zero `TOKEN:` leftovers, zero `plus.unsplash.com` references, and — critically — zero photo IDs appearing in more than one `<img>` tag anywhere across all 9 HTML files (the initial naive count flagged 13 "duplicates," which on inspection were all the harmless GLightbox `href`-equals-`img src` pattern, i.e. a single photo linking to an enlarged version of itself — not a second, different use of the same photo); all 41 unique photo URLs re-verified at HTTP 200; every page's `<div>`/`<section>`/`<article>`/`<a>` tag counts re-balanced after editing; `main.js`'s syntax re-checked with `node -c`; every new CSS class (`.occasion-tile`, `.occasion-tile-icon`, `.occasion-tile-count`, `.trend-rank`, `.product-row`, `.product-row-icon`, `.chip-block`) confirmed defined exactly where used. **Lesson: a naive `grep -c` count of a photo ID across a page will over-report duplicates on any page using GLightbox, since every lightbox-enabled tile legitimately references its own photo twice (`href` + `img src`) — filter to `<img src=` occurrences only when auditing for genuine image reuse.**

### shop.html follow-up — broken sidebar filter, full photo catalog, 3×3 pagination
A direct follow-up flagged three problems with the redesigned `shop.html`: the sidebar occasion filter was unreliable ("not working properly"), the mixed photo-card / icon-only-row layout read as inconsistent, and the user wanted every product photographed, a 3×3 (9-per-page) grid, and pagination.

**Root cause of the flaky sidebar filter:** the occasion-filter chip wrapper carried `data-scroll-strip`, which attaches the site's generic horizontal-drag-scroll handler (`initHorizontalScrollStrips()` — wheel-redirect + `setPointerCapture` drag-to-scroll, built for wide content carousels like testimonials/best-sellers). That handler calls `strip.setPointerCapture(e.pointerId)` on every `pointerdown` anywhere inside the strip, including presses that land on a child `<button>`. Capturing the pointer on the parent, combined with any sub-pixel movement between press and release (extremely common on trackpads/touch), is a well-documented way for a scrollable container to swallow taps on its own interactive children. **Fix:** removed `data-scroll-strip` from both the occasion-filter wrapper (`shop.html`) and the gallery-filter wrapper (`gallery.html`, same latent bug, fixed proactively) — neither needs the custom mouse-drag assist: on desktop the sidebar is `lg:flex-col lg:overflow-visible` (nothing to scroll), and on mobile/tablet native touch/trackpad scrolling on a plain `overflow-x-auto` row works with zero JS. **Lesson: never attach a drag-to-scroll/pointer-capture handler to a container whose children are themselves clickable controls (filter chips, tab buttons) — reserve `data-scroll-strip` for passive content strips (cards, testimonials, images) with no interactive elements inside the scrollable area.**

**Full photography, zero remaining icon-rows:** the 11 catalog items that had been shipped as no-photo `.product-row` entries (a deliberate space-saving compromise from the prior redesign pass) were each given a freshly sourced, page-and-site-exclusive photo and converted to the same `.card-ribbon` treatment as the other 10 — `shop.html` now shows all 21 products as uniform photo cards, none reused within the page or anywhere else on the site (verified: 52 unique photos site-wide by `<img src>` count, zero duplicates, all HTTP 200). Two products' copy was quietly adjusted to match what the sourced photo actually shows rather than the original placeholder description, per the site's standing policy of never leaving a caption that misdescribes its image: "Birthday Wishes Card Trio" now describes a single elegant foil-lettered card (the available photography showed one card, not three), and "New Baby Card & Onesie Set" was renamed "Soft Knit Baby Blanket & Card Set" to match a sourced pastel-blanket photo instead of a baby-clothing shot (every onesie/baby-clothing candidate found carried a visible commercial brand tag — Carter's, generic infant-wear labels — and was rejected on sight per the site's no-visible-third-party-branding rule).

**Pagination (3×3, `main.js` §9, extended not replaced):** `[data-shop-grid]` carries `data-page-size="9"`; `applyFilters(resetPage)` now computes the full filtered/matching set first, slices it to the current page's 9-item window, and only then toggles `.hidden` per card (previously it toggled visibility directly off the category/personalize match with no windowing at all). Changing category or the personalize-only checkbox resets to page 1 (`resetPage` defaults to `true`); clicking a pagination control calls `applyFilters(false)` to repaginate without resetting the active filter. `renderPagination()` builds Prev/Number/Next buttons into the pre-existing empty `<nav data-shop-pagination>`, hiding itself entirely when a filtered set fits on one page — which is every single-occasion view, since each of the 7 categories holds exactly 3 items (well under the 9-per-page threshold); pagination only ever appears on "All Occasions" (21 items → 3 pages of 9/9/3), exactly mirroring the "single category always fits on one page, pagination only appears for the unfiltered view" pattern already validated on a sibling template's `menu.html`. The live `data-shop-count` label was upgraded from a flat "showing N gifts" to a proper "Showing 10–18 of 21 gifts" range so users can see their position across pages, not just a total.

**New sourcing (11 photos)** for the birthday/anniversary/wedding/new-baby/holiday/sympathy/graduation icon-rows being converted to real photography, verified via the same WebFetch-raw-image → Read-saved-file technique and cross-checked against every ID already in the site-wide ledger before use: confetti-filled balloon + pink foil "Happy Birthday" balloon; a black card with gold-foil "Happy Birthday to You" lettering on a purple ground (the purple happens to match the site's own plum brand color); a mauve rose-and-eucalyptus bouquet; a blank card in a kraft envelope beside a sunflower; a wedding ring resting on an invitation with dried flowers; a wood keepsake box laser-engraved "Olivia & Jordan · 12/12/26" under a tree-and-heart cutout (an exact, almost eerie match for the "engraved with the couple's names & date" copy already on the page); a pastel rainbow crocheted baby blanket; a pile of vintage-style Christmas cards; a jute tote bag monogrammed "O.J.L.B."; a gentle floral "just for you" card; and gold glitter "CONGRATS" lettering with curling ribbon and star confetti.

**Rejected candidates this pass:** a 3D-rendered/CGI gift-box pair (broke the all-real-photography convention), a rose bouquet in a box bearing a small florist logo tag, three onesie/baby-clothing photos with a visible "Carter's" tag or similar, and a black-and-white crocheted-bootie photo (tonally inconsistent with the site's all-color photography).

### gallery.html complete redesign — bento/masonry layout, hover captions, one new HD photo
A direct follow-up asked for `gallery.html` specifically to be completely redesigned with a modern, responsive layout, using unique HD images with zero repeats anywhere in the template.

**Layout:** replaced the plain uniform `sm:grid-cols-2 lg:grid-cols-3` grid (every tile the same `aspect-[4/3]` square, no captions, click straight to lightbox with no visual hierarchy) with a new `.gallery-bento` component — a dense CSS grid (`grid-auto-flow: dense`, `grid-template-columns: repeat(2/3/4, 1fr)` at mobile/tablet/desktop, fixed `grid-auto-rows`) where three tiles carry a `.gallery-tile-lg` modifier (`grid-column: span 2; grid-row: span 2`) to read as large "feature" photos breaking up the rhythm of an otherwise-uniform grid — a genuinely different structural pattern from every other grid on the site (shop.html's uniform 3-col product grid, index.html's icon-tile grid), and not previously used anywhere in this template.
- **Hover/focus captions (new interaction):** every tile now shows a category tag + title on hover/focus, sliding up over a bottom-anchored gradient scrim (`.gallery-tile-caption`), plus a small frosted-glass magnifying-glass affordance icon (`.gallery-tile-zoom`) that fades in independently — neither existed before (the old tiles were bare `<img>` with only a generic darkening overlay on hover, no text, no zoom cue). `:focus-within` is paired with every `:hover` rule so the reveal also works for keyboard users tabbing to a tile, not just mouse hover.
- **Markup simplification:** collapsed the old three-level structure (`<div data-gallery-tile><a class="glightbox"><img></a></div>`) into a single `<a data-gallery-tile class="glightbox gallery-tile">` carrying the filter, lightbox and visual-tile concerns on one element — fewer wrapper nodes, same `[data-gallery-tile]` / `.glightbox` JS hooks, no behavior change required in `main.js`'s existing selectors.
- **Filter bar richness:** each filter chip now shows a live category icon + a photo-count badge (`.chip-count`, the same component already used on `shop.html`'s sidebar), and a new `data-gallery-count` label below the chips reports "Showing N of 14 photos" — both are populated by an extended `applyGalleryFilter()` in `main.js` §10 (previously the gallery filter only toggled `.hidden` with no count or empty-state feedback at all). A `data-gallery-empty` message appears if a filter ever yields zero tiles, mirroring the pattern already used on `shop.html`.
- **HD source sizing:** every tile now requests two distinct resolutions from the same Unsplash asset — a lighter `w=1000`/`w=1200` size for the on-page `<img src>` (large tiles get the bigger crop) and a separate, higher `w=1800&q=85` request for the GLightbox `href` so the enlarged lightbox view is genuinely higher-resolution than the grid thumbnail, not just the same file stretched — a first for this template's lightbox implementation.

**New photo (1), verified via the established WebFetch-raw-image → Read-saved-file technique and checked against the full site-wide ledger before use:** a moody, warm-lit shot of wrapped gift boxes stacked on reclaimed-wood shelving against an exposed brick wall with string lights and trailing greenery — sourced specifically for one of the three large bento tiles ("The Gift Wall"). Portrait-oriented in its native crop, which is precisely why it was chosen for a `row-span-2` tile rather than a standard square — the redesign's asymmetric layout made a strong portrait shot usable for the first time on this page (the prior uniform-grid design could only accept photos that cropped well to a flat 4:3, which had ruled out several strong candidates found in earlier sourcing passes).

**Final count and verification:** `gallery.html` now holds 14 unique photos (13 kept from the prior pass + 1 new), all confirmed unused anywhere else on the site; the four filter-chip counts (In-Store 6, Wrapping & Ribbon 4, Personalization 2, Seasonal 2) were verified against the actual `data-category` attributes in the markup rather than hand-counted; site-wide sweep after this pass: 53 unique photos across all 9 pages, zero duplicates (by `<img src>`, excluding the harmless GLightbox `href`-references-its-own-`src` pattern), all HTTP 200. `index.html`'s "Inside the Shop" stat teaser (`data-count-to`) was updated from 13 to 14 to stay in sync.

### gallery.html — empty-grid fix (masonry), then a spotlight+uniform-grid rebuild, then a 4-photos-per-category floor
Three quick successive follow-ups to the bento redesign above, each triggered by a fresh user request rather than a bug found independently.

1. **Empty-cell fix:** the bento's `grid-auto-flow: dense` packing of 3 large (2×2 = 4-cell) tiles + 11 standard (1-cell) tiles totals 23 grid cells, which isn't evenly divisible by 2, 3, or 4 (the responsive column counts) — so exactly one blank cell was left over at every breakpoint. Fixed by dropping the dense/spanning grid for a CSS multi-column masonry layout (`column-count`, `break-inside: avoid`), which structurally cannot leave an internal hole. A "Share Your Visit" section was added below the grid with 3 icon-cards as new below-the-fold content, per the same request.
2. **4-cards-minimum in "Share Your Visit":** that section was bumped from 3 to 4 icon-cards, restructured from a 3-up row to a `grid grid-cols-2 gap-5` 2×2 layout.
3. **Full gallery-section redesign:** the masonry approach was itself superseded — the grid was rebuilt as a plain uniform `aspect-square` CSS grid (no spanning/dense packing at all) plus a single large tile pulled out into its own `.gallery-spotlight` component above the grid ("The Gift Wall" photo). This is the most robust of the three approaches: a uniform grid with no spanning can only ever leave an incomplete *trailing* row, never an internal gap.
4. **4-photos-per-category floor:** a request that every filterable category show at least 4 photos found Personalization and Seasonal at only 2 each. Sourced 4 new photos (2 per category), each verified via the standard WebFetch-raw-image → Read-saved-file technique and checked against the site-wide ledger:
   - Personalization: close-up of vintage metal letterpress type spelling "LETTER PRESS" (`1699662585308-fcb113a0a4ba`, "Set in Type"); blush envelopes stamped with a gold wax seal (`1646568779353-b9d2b903b3e1`, "Sealed With Wax").
   - Seasonal: stacked red/silver metallic-wrapped Christmas gifts with ribbon bows amid pine garland (`1765524436234-b1653bd18359`, "Holiday Wrap Wall"); overhead pile of pastel plastic Easter eggs (`1681664951333-e91936d730c8`, "Spring Egg Hunt") — chosen specifically to diversify the Seasonal category beyond its existing autumn/Valentine's pair with a third (winter) and fourth (spring) season.
   - One rejected candidate: a Christmas-market-stall photo (wreath, string lights, shelves of souvenir mugs) was passed over for reading too much like a European Christmas-market stall rather than a US gift shop, and risked unreadable foreign text on the mug designs.

**Final count and verification:** `gallery.html` now holds 18 unique photos (In-Store 6, Wrapping & Ribbon 4, Personalization 4, Seasonal 4) — every category at or above the 4-card floor. Filter-chip counts, the hero stat row's photo count, and the `data-gallery-count` "Showing N of N" label were all updated 14→18 to match. Verified: `div`/`section`/`a` tags balanced, `main.js` syntax valid (no JS changes were needed — the filter logic only depends on `[data-gallery-tile]`/`data-category`), zero duplicate `<img src>` photo IDs site-wide, all 4 new image URLs return HTTP 200. `index.html`'s "Inside the Shop" Photos stat (`data-count-to`) updated 14→18 to stay in sync.

### gallery.html — In-Store category bumped from 6 to 9 photos
A direct follow-up asked for 3 more cards specifically in the In-Store section. Sourcing hit an unusually high rejection rate this pass — worth noting for future sourcing on this template: 12 candidates were pulled and viewed before 3 passed. Rejected, with reasons: a game/toy shop with a visible "SAIGONOPOLY" brand name and Vietnamese framed maps; a large corporate department-store interior (track lighting, "Home Accents & Textiles" wayfinding sign, EXIT sign) that read as too big/sterile for this boutique's aesthetic; a shelf of gift boxes with a Vietnamese "LƯU Ý" safety-notice sign in frame (same photoshoot as an existing in-use photo — that whole ID cluster is now avoided for future sourcing); a "BREW TEA Co"-branded café/tea-shop counter scene; a Diptyque-branded candle display with French scent names; an East-Asian curio shop with Chinese porcelain and jade figurines; a Danish "NicNac" shelf display with a "2990.-" kroner price tag and a Danish flag figurine; a repeating "F"-logo branded gift-box pattern; a British greeting-card rack with "£1.99"/"£2.99" price tags; a rack of German-language greeting cards ("LIEBE", "HEUTE IST DEIN TAG"); a Portuguese-signed ("CAIXA") bookstore interior; and a wall of "HAY"-branded tote bags.

**3 new photos, verified via the standard WebFetch-raw-image → Read-saved-file technique and checked against the site-wide ledger:**
- Rustic wooden wall shelf of amber/brown stoneware pottery beside a single lit wall sconce (`1780246031877-9bfe3be0a5b7`, "The Pottery Shelf").
- A cluster of woven wicker/rattan wall baskets in warm neutral tones (`1682126381847-44d2817f524d`, "Woven Wall Display").
- Close-up of a brass entrance bell mounted on a wooden door frame (`1661885965704-99fc49eb60df`, "The Front Door Bell").

**Final count and verification:** `gallery.html` now holds 21 unique photos (In-Store 9, Wrapping & Ribbon 4, Personalization 4, Seasonal 4). Filter-chip counts, the hero stat row's photo count, and the `data-gallery-count` label updated 18→21. Verified: `div`/`section`/`a` tags balanced (36/36, 5/5, 61/61), `main.js` syntax valid, zero duplicate `<img src>` photo IDs site-wide, all 3 new image URLs return HTTP 200. `index.html`'s "Inside the Shop" Photos stat updated 18→21 to stay in sync.

### index.html — hero rebuilt as a full-bleed photo banner
Direct follow-up: "redesign index page hero section, use full banner image and make some best layout design use hd images." The prior hero (two-column: text left, die-cut `.gift-tag-frame` photo right — Template 06's signature hero device, still used unchanged on `home-2.html`, `shop.html`, and `404.html`, so its CSS was left in place) is replaced on `index.html` only with a full-bleed `.hero-banner` — a single HD photo fills the entire section edge-to-edge, headline/CTAs sit on top of it, and a second HD photo floats as a bordered polaroid-style card in the bottom-right corner on `lg+` screens for depth. This is a genuinely different hero composition from anything else in this template (2-col split vs. full-bleed with floating inset), and distinct in mood from Template 01 Bakery's full-bleed hero (dual blob shapes + floating stats card at the bottom edge) — this one uses a left-to-right + bottom scrim gradient over the photo itself, an inset floating photo card (not a stats card) in the opposite corner, and a bottom-center scroll-cue chevron linking to `#stats`.

**New CSS (`assets/css/style.css`, after the confetti-piece block):** `.hero-banner` (`position:relative; isolation:isolate; overflow:hidden`), `.hero-banner-media`/`.hero-banner-media img` (absolutely-positioned full-cover background image), `.hero-banner-scrim` (dual linear-gradient — top-to-bottom AND left-to-right — so the left-aligned text block sits over the darkest corner while the photo's right two-thirds stays visible), `.hero-banner .btn-fold-outline` / `.hero-banner .chip` (descendant-scoped light-on-dark overrides so the existing outline-button and chip components read correctly over a photo without touching their shared base definitions used elsewhere), `.hero-float-card` (bordered polaroid card, reuses the existing `.tag-sway` animation), `.hero-scroll-cue` (new small bounce keyframe).

**Images used — both already-established index.html hero photos, no net-new page ownership:**
- Background banner: a new photo, whitewashed rustic wood plank surface with a kraft-wrapped gift tied in red/white twine, pinecones and winter berries in the bottom-left corner (`1637870104313-093e0f0b3718`) — chosen specifically for its built-in negative space (the right two-thirds of the frame is empty plank) which is what makes a text-over-photo hero legible without an opaque overlay killing the image.
- Floating inset card: the flat-lay of wrapped presents that was the *entire* hero photo in the old 2-column design (`1713496736683-ffb12c754c27`) — repurposed in place rather than retired, so no new page took ownership and the site-wide ledger only gained one truly new photo for this pass.

**Sourcing note for future passes:** wide/panoramic "shop interior" and "ribbon wall" searches had a very high rejection rate here too (Turkish "KASA" antique shop, Brazilian Bahia wish-ribbons with Portuguese text, a personalized word-search wrapping paper with a branded "KNOT WITH LOVE" ribbon spool, sewing-thread walls mistaken for ribbon) before a wood-plank flat-lay search produced a clean, on-brand, negative-space-friendly result on the first usable hit — flat-lay/overhead product shots continue to outperform wide environmental shop-interior shots for hit rate on this template.

**Verification:** visually confirmed via headless-Chrome screenshots at 1440×900 and 390×900 (an initial raw `chrome --headless --screenshot` pass falsely showed text overflowing off-screen on mobile — traced to the CLI screenshot firing before Tailwind's CDN JIT had finished injecting its compiled stylesheet, not a real bug; a proper Puppeteer run with `waitUntil: networkidle0` showed the header, hamburger menu, and all hero text wrapping and fitting correctly at 390px, confirming the false alarm). `div`/`section`/`a` tags balanced, `main.js` syntax unchanged/valid (no JS touched — the hero has no interactive JS hooks), zero duplicate `<img src>` photo IDs site-wide, both image URLs return HTTP 200.

---

## Template 08 — Music Instrument Store

- **Brand name:** Cedar & Brass Music Co.
- **Tagline:** "Instruments with Soul, Since 1978." (Home 1) / "Where Every Musician Begins." (Home 2)
- **Business category:** Family-owned (three-generation) music instrument store — guitars, keyboards/pianos, percussion; beginner rent-to-own program; in-house repair services and instrument trade-ins; physical storefront (Redbridge, North Carolina). No e-commerce checkout, no dashboard.
- **Primary color:** Mahogany Wine Red `#8C2F39` (600), full 50–900 ramp (`#F6E9EA` → `#4A1620`) — CTAs, headline accents, active states.
- **Secondary color:** Antique Brass Gold `#B8892B` (500), full 50–900 ramp (`#FBF3E2` → `#453110`) — badges, EQ-bar decorations, footer top edge, star ratings, dashed card borders, tuning-peg card accent.
- **Accent:** Deep Teal `#1F5C57`/`#194B47`/`#123834` — used sparingly and consistently (not rotating): "Rent or Buy" badges, one contrasting solid-teal inner-page banner (`instruments.html`), a handful of icon accents. A fixed 3-hue system, deliberately not a rotating multi-hue system like T2.
- **Neutral / background:** Warm parchment/ivory `#FBF3E7` (light), espresso-walnut `#1B120C`→`#241A12` gradient (dark). A separate always-warm-off-white **fixed** token (`--ink-fixed:#F3E9DC`) is used exclusively for light text on *permanently*-dark surfaces (footer, dark icon bands, hero overlays on photos) — kept structurally distinct from the adaptive `--text`/`--bg` tokens from the start, specifically to avoid the dark-mode token-collision bug documented in Template 02's registry entry (a token redefined per-theme cannot safely double as both "the page's adaptive background" and "light text sitting on a surface that's dark in both themes").
- **Font combination:** DM Serif Display (display serif — headlines, hero text, section titles) + Mulish (body/UI text) — a 2-font pairing structurally distinct from every prior template's pairing on this site.
- **Header type:** Single sticky row with an unusual flex order — **[1] nav links** (packed to content, not `flex-1`) → **[2] logo** (shrink-0, right-of-center) → **[3] controls** (theme/RTL toggles + "Book a Repair" CTA, shrink-0, true right edge) on desktop (`xl`, 1280px+); below `xl`, order becomes hamburger (left) → centered logo → theme-toggle-only (right), with RTL toggle and the CTA moving into the mobile panel. **Shrinks on scroll** past 80px (`.is-scrolled` class transitions header padding/logo font-size down) — never hides, a mechanic distinct from every prior template's header behavior (hide-on-scroll, permanent-glass, floating-capsule, three-tier-marquee, smart-hide-utility-bar). Desktop nav gates at `xl` (1280px) per the site-wide lesson.
- **Navigation style:** 7 links — Home (dropdown: Home 1/Home 2), About, Instruments, Brands, Rentals, Repair & Trade-In, Contact — FAQ reachable via footer + mobile panel only. Only "Home" carries a dropdown; click always calls `open()` (never toggles), closes only via mouseleave/outside-click/focusout/Escape, per the T1/T2 click-vs-hover lesson. `justify-between` on the header row (not a `dir`-scoped auto-margin) for bidi-safe edge-pinning. Skip-link hidden via `transform:translateY(-100%)`, never a large negative `left` offset.
- **Hero layout (Home 1):** "Equalizer-edge split hero" — text left (eyebrow badge, DM Serif headline with one brass-gold accent phrase, paragraph, dual `.btn-pluck` CTAs, 3 stat pill-chips with no dividers), image right (large rounded-2xl guitar photo with a vertical strip of 6–8 animated CSS equalizer bars beside it, looping `scaleY` pulse in brass-gold, freezes under reduced-motion) plus a floating "What's it worth?" trade-in-estimator teaser card overlapping the photo's bottom-left corner (a functional teaser linking to `repair-tradein.html#trade-in-estimator`, not a stats-counter or product-spotlight card).
- **Hero layout (Home 2):** "Stage spotlight hero" — full-bleed dark stage-photo background with a radial spotlight-cone overlay plus a slow-rotating `conic-gradient` spotlight-sweep keyframe (freezes under reduced-motion), centered uppercase tracked DM Serif headline, star-rating trust line, dual CTAs, a bottom-anchored drum-kit photo with an elliptical `radial-gradient` stage-floor shadow, and a horizontally-scrollable strip of circular "stage-floor marker" category badges beneath the hero. No split columns, no equalizer bars, no floating widget card — deliberately unlike Home 1.
- **Hero layout (inner pages):** Compact banners, no two identical — solid mahogany gradient + wood-grain (About), solid teal gradient + wood-grain (Instruments), solid brass gradient + wood-grain (Brands), photo + dark gradient overlay (Rentals, Repair & Trade-In), mahogany→brass diagonal gradient + wood-grain (Contact), flat teal-500 + wood-grain (FAQ).
- **Section order (Home 1):** Header → Hero (equalizer split) → Shop by Category (3 `.card-string` cards) → Featured Instruments (6-card grid, mixed photo/icon-tile cards, price + Buy/Rent badge) → Beginner Rental Program teaser (image+copy split, 3-step chip row) → Brands marquee (auto-scroll text wordmarks) → Why Musicians Choose Us (dark mahogany-gradient icon band, 6 items, `--ink-fixed` text) → Repair & Trade-In teaser (split: repair checklist left, embedded *functional* mini trade-in estimator right) → Testimonials (CSS multi-column masonry wall, star ratings + instrument-icon badges) → Visit Us preview (compact map-styled tile + live open/closed status strip) → Final CTA (solid mahogany rounded band) → Footer.
- **Section order (Home 2):** Header → Hero (stage spotlight, category-badge strip attached) → Instrument Spotlight (hand-rolled prev/next carousel, spec-plate: photo + name + spec table + price, 4 slides) → Rental Plans comparison (3-column pricing cards) → Repair Services (icon-only checklist grid, no photos — deliberate pacing break) → Meet the Luthier (single bio card: photo + credentials + quote) → Brands marquee (shared component) → In-Store Life (bento photo grid + hand-rolled lightbox, 4 photos) → Testimonials (single large spotlight quote + decorative CSS audio-waveform bars) → Visit Us (full split panel: larger map embed + directions/parking copy) → Final CTA (full-bleed photo band + dark gradient overlay) → Footer.
- **Card style:** `.card-string` — rounded-2xl, 2px dashed brass-gold border that intensifies (border-color + `box-shadow` color-pulse ring) on hover, plus a small circular "tuning peg" accent dot positioned with the **logical property `inset-inline-end`** (auto-mirrors under `dir="rtl"` with zero override, applying T06's logical-properties lesson). Flex-column with `.card-cta-row{margin-top:auto}` so price/CTA rows stay baseline-aligned across a row of unequal-content cards.
- **Button style:** `.btn-pluck` — rounded-lg (not pill, not chunky-flat-shadow), solid fill; on hover a `::before` circle scales `0 → 2.2` from `transform-origin:center` as a radial fill-reveal (deliberately center-out, not directional, so it needs no RTL-mirroring override unlike T3/T5/T6's directional button mechanics); on `:active`, a ~150ms `rotate(0 → -2deg → 1.5deg → 0)` "plucked string settling" keyframe. `.btn-pluck-outline` and `.btn-pluck-brass` variants share the same radial mechanic.
- **Image style:** Close-crop/macro instrument photography (soundhole, fretboard, body-curve details) favored over full-instrument-with-headstock shots specifically to avoid real trademarked headstock logos (Taylor/Fender/Roland/REMO were all encountered and rejected during sourcing); mixed photo-card and icon-tile product cards on the same grid as a deliberate pacing technique (validated by T6's registry precedent) rather than forcing a photo for every catalog item in a vertical where "clean, logo-free instrument photography" has an unusually high rejection rate; bento-grid lightbox gallery (Home 2); hand-rolled spec-plate carousel (Home 2).
- **Background style:** Warm parchment base with a very subtle (~5% opacity) inline-SVG wavy wood-grain texture (`.wood-grain`, repeating sine-wave `<path>` lines tiled via `background-repeat`) on light-mode sections only — texture removed entirely in dark mode for readability. A genuinely different texture concept from every prior template's dot-sprinkle/confetti-dot/graph-paper-lines/mesh-gradient-noise (wavy lines, not dots or straight grids).
- **Animation style:** Zero external JS animation/carousel/lightbox libraries. `IntersectionObserver` + native **Web Animations API** (`element.animate()`) scroll reveals (`data-reveal="up|left|right|scale"`) — a WAAPI-driven code path distinct from T3/T6's IntersectionObserver-plus-CSS-class-toggle approach. Hand-rolled `requestAnimationFrame` stat counters (`data-count-to`). `initHorizontalScrollStrips()` (wheel-redirect + drag-to-scroll via pointer events, never attached to filter/tab containers, per T6's `setPointerCapture` lesson) on the About timeline and Home 2 category-badge strip. Hand-rolled Home 2 instrument-spotlight carousel and hand-rolled lightbox (click thumbnail → modal, prev/next, Escape-to-close, focus-trap). CSS `@keyframes` equalizer-bar pulse (hero, footer top edge, 404) and conic-gradient spotlight sweep (Home 2 hero), both reduced-motion-safe.
- **Footer layout:** `.footer-eq` — dark espresso-walnut panel whose **top edge is a row of ~40 vertical bars of varying heights in brass-gold** (`nth-child`-driven CSS heights, `aria-hidden`), evoking an audio equalizer meter — a top-edge motif distinct from every prior template's scallop/wave/diagonal-stripe/rounded-corner/perforation. Standard 4-column grid (brand+social+founding blurb / Shop / Customer Care / Visit Us with address+hours+phone).
- **Mobile menu pattern:** "Top-down push panel" — an **in-document-flow** panel (`display:grid; grid-template-rows:0fr → 1fr` transition) directly beneath the header that pushes page content down, not a `position:fixed` overlay/drawer/sheet. Because it's never transformed/translated, it needs **no RTL slide-direction handling at all**, sidestepping the whole class of RTL slide-direction bugs documented in T1/T2/T6's changelogs. Contains: nav links with Home as an inline `<details>` accordion, a 2×2 grid of large tappable category tiles (Guitars/Keyboards/Percussion/Rentals), a theme+RTL toggle row, and the "Book a Repair" CTA. Respects `prefers-reduced-motion` (instant toggle, no height transition).
- **Special design features:** Dark/light mode + RTL/LTR toggles (localStorage, inline early `<head>` script to prevent flash-of-wrong-theme, applied consistently across all 10 pages); a real **client-side Trade-In Value Estimator** (type/age/condition/brand-known inputs → computed price *range*, clearly labeled as an estimate, embedded in both a homepage mini-widget and the full `repair-tradein.html#trade-in-estimator` version via a shared `[data-trade-estimator]` JS module supporting multiple instances per page); a **Repair Booking form** with an instrument-condition checkbox checklist and a `FileReader`-based photo-preview uploader (clearly labeled client-side-only, no actual upload); category tabs on `instruments.html` (All/Guitars/Keyboards/Percussion, hash-aware, shared with header/footer/mobile deep-links like `instruments.html#guitars`); a filterable brand wall of **text-wordmark cards** (`brands.html`, invented brand names, "Authorized Dealer" badges — no scraped/real logo images, per T5's trademark-safety lesson); a rent-to-own visual credit explainer (progress bars showing rental-payment-to-purchase-credit accumulation); a live **Open Now/Closed** status badge and **today's-row auto-highlight** on the hours table (`contact.html`, computed from `new Date().getDay()` against a stored weekly-hours object); a full contact form with required/email/phone/min-max-length validation and a live character counter; an FAQ page with 4 topic-grouped accordion clusters and live client-side search; an on-brand 404 ("This Note's Off-Key") with animated equalizer bars and a quick-link grid + site search.
- **Pages built:** `index.html` (Home 1), `home-2.html` (Home 2), `about.html`, `instruments.html`, `brands.html`, `rentals.html`, `repair-tradein.html`, `contact.html`, `faq.html`, `404.html`.
- **Libraries used:** Tailwind CSS (CDN, `[data-theme="dark"]` selector strategy), Font Awesome 6 (icons only), Google Fonts (DM Serif Display / Mulish). Zero JS libraries of any kind — no AOS/GSAP/ScrollReveal/Swiper/Lenis/GLightbox/CountUp/Typed.js. All scroll reveals, counters, carousels, lightbox, horizontal-scroll drag, tabs, accordions, form validation, FileReader preview, and trade-in math are hand-rolled vanilla JS in one `assets/js/main.js`. No build step; no backend.

**Do not reuse for future templates:** nav-links → logo → controls asymmetric header flex order with shrink-on-scroll (never-hide) behavior, mahogany/brass/teal 3-fixed-hue palette, DM Serif Display + Mulish pairing, equalizer-edge split hero with a vertical animated-bar strip beside the hero photo (Home 1), stage-spotlight hero with conic-gradient sweep + bottom-anchored instrument photo + elliptical stage-floor shadow (Home 2), `.card-string` dashed-border + logical-property tuning-peg-dot component, `.btn-pluck` center-out radial-reveal + "plucked-string-settle" rotate-keyframe button mechanic, `.footer-eq` equalizer-meter-bar top edge, in-document-flow "top-down push panel" mobile menu (grid-template-rows transition, no transform), wavy inline-SVG wood-grain background texture, WAAPI-driven (`element.animate()`) IntersectionObserver reveal engine as a site's primary motion system, client-side trade-in value estimator with a shared multi-instance JS module, FileReader-based repair-booking photo-preview uploader, hand-rolled instrument-spotlight spec-plate carousel (Home 2), rent-to-own progress-bar credit explainer.

### Home Page 1 (`index.html`) vs. Home Page 2 (`home-2.html`)

Same tokens (palette/type/header/footer/buttons/cards), deliberately different hero mechanic, section order, and component choices — no section on Home 2 reuses Home 1's exact layout for the same content type.

- **Hero mechanic:** Home 1 is a two-column split (text vs. photo+equalizer-bars+floating widget); Home 2 is a full-bleed single-column centered composition (dark stage photo + spotlight overlays + bottom-anchored second instrument photo). These are structurally unrelated hero devices on the same site, not palette-only variants of one hero.
- **Featured-product device:** Home 1 uses a static 6-card grid (mixed photo/icon-tile) inline in the page flow; Home 2 uses a single hand-rolled prev/next "spec-plate" carousel (photo + name + spec table + price) — a functionally different interaction, not just a different visual skin on the same grid.
- **Testimonial device:** Home 1 is a CSS multi-column masonry wall of many small staggered quote cards with star ratings + instrument-icon badges; Home 2 is a single large spotlight quote with a decorative CSS audio-waveform bar — the site's two distinct testimonial treatments.
- **Repair/rental content:** Home 1 folds repair into a single "teaser" split panel with an embedded mini trade-in estimator; Home 2 gives rentals a full 3-column pricing-style comparison and repairs a dedicated icon-only checklist grid — structurally different components covering the same subject matter without repeating layouts.
- **Visit Us:** Home 1 uses a compact 2-tile strip (styled map tile + info card); Home 2 uses a full split panel with a real Google Maps iframe embed and directions/parking copy — a deliberately larger, more detailed treatment.
- **Final CTA:** Home 1 is a solid rounded mahogany band; Home 2 is a full-bleed photo band with a dark gradient overlay — the site's two distinct final-CTA treatments (solid-color vs. photographic).
- **New page-exclusive component:** Home 2 also introduces the circular stage-floor category-badge strip (attached to the hero), the "Meet the Luthier" bio card, and the hand-rolled lightbox-driven "In-Store Life" photo grid — none of which appear on Home 1.

### Revision pass — header layout, hero/reveal robustness fixes, back-to-top, Home 1 card redesign
A direct follow-up requested a conventional logo-left header, a better-looking `index.html` hero, and (in a second message) a site-wide back-to-top button plus a redesign of every `index.html` card type so no two card groups on the page share a silhouette. Site-wide, applied to all 10 pages via a scripted find/replace (not hand-edited per file, to guarantee identical results):

1. **Header reflowed to logo-left:** the original launch header used an unusual `nav → logo → controls` flex order (logo sitting right-of-center, between the nav group and the icon/CTA cluster) — a deliberate uniqueness choice at build time, but flagged as confusing in review. Rebuilt as a conventional two-group `justify-between` row: **[logo + nav together, left]** vs. **[toggles + CTA + hamburger, right]**, logo now flush left on every breakpoint including mobile (previously mobile centered the logo between hamburger and a single icon). The `.is-scrolled` shrink-on-scroll behavior, the Home 1/Home 2 dropdown, and the `xl` (1280px) desktop-nav gate were all preserved unchanged. Also added a plain-CSS `@media` belt-and-suspenders pair (`.header-desktop-only` / `.header-mobile-only`, `display:none!important`) alongside the existing Tailwind `hidden xl:flex` / `xl:hidden` utility pairs, as defensive redundancy against any Tailwind-CDN runtime-class-generation edge case — harmless if never needed, cheap insurance if it is.
2. **Two real bugs found and fixed during visual QA (headless-Chrome screenshot review, see tooling note below):**
   - The in-flow mobile push-panel (`#mobile-panel`, `grid-template-rows:0fr→1fr`) left a hairline sliver of its closed content (the "Home" accordion row) visibly bleeding in under the header at rest. Root cause: a `0fr` grid track doesn't reliably reach a true zero rendered height across engines when its content has any intrinsic line-height. Fixed by pairing the height transition with a `visibility` toggle that's delayed on the *closing* edge only (`transition: grid-template-rows .35s ease, visibility 0s linear .35s` at rest vs. `0s linear 0s` when `.is-open`) — content stays visible through the collapse animation, then fully leaves the render tree, so a fractional leftover row height can never paint.
   - The hero's image column (`grid lg:grid-cols-2` → single column below `lg`) had no `min-w-0` on the flex/grid items wrapping the `<img>`; a flex/grid item's default `min-width:auto` lets a replaced element's intrinsic size force the whole shared implicit grid track wider than the viewport, which cascaded into the header's own icons appearing to be pushed off-screen and the H1 failing to wrap on narrow widths — despite the header itself being unrelated to the hero. Fixed by adding `min-w-0` to the hero's left/right grid children and the image's flex wrapper. **Lesson for future templates: any flex/grid item that contains an `<img>` (or other replaced element) needs an explicit `min-w-0` unless the item is also given a hard `width`/`max-width` — the default `min-width:auto` floor is sized from the image's intrinsic pixels, not from its `w-full`/percentage CSS width, and the resulting overflow can visually manifest on a completely unrelated sibling (here, the sticky header) rather than on the offending element itself.**
   - Separately (not overflow-related): `initReveal()`'s `IntersectionObserver` used `threshold:0.15` + a `-40px` bottom `rootMargin`, so content already sitting in the initial viewport at load (e.g. the third "Shop by Category" card, at 1440×~1000) could sit at just-under-threshold intersection and never receive a first callback, leaving it permanently at the base `[data-reveal]{opacity:0}` state. Fixed by checking each `[data-reveal]` element's `getBoundingClientRect()` against the viewport at init time and snapping anything already visible straight to its revealed end-state (`duration:1`, no stagger delay) instead of routing it through the observer at all — scroll-triggered animation is now reserved for content that's genuinely below the fold at load, which is both more correct UX (nothing "waits" to appear if the user never scrolled past it) and eliminates the class of flakiness entirely rather than just tuning the threshold numbers.
   - A third, independent bug surfaced while testing the Visit Us card's live status pill: `initHoursStatus()` opened with `if (!rows.length) return;` against `[data-hours-day]` (the *hours-table* row-highlighter), which unconditionally skipped the rest of the function — including the unrelated `[data-open-status]` Open Now/Closed Now badge logic — on every page that has the badge but not the full hours table (`index.html`; `contact.html` has both and was never affected). The badge had silently rendered as an empty `&nbsp;` pill since initial launch. Fixed by making the hours-table block conditional internally instead of an early return that aborts the whole function.
3. **Back-to-top button (new, all 10 pages):** a fixed circular `#back-to-top` button (`inset-inline-end` positioned, so it mirrors to the opposite corner under `dir="rtl"` automatically), brass-on-hover mahogany fill, fades/lifts in past `window.scrollY > 500` via a dedicated `initBackToTop()` IIFE-scoped function, smooth-scrolls to top (instant under `prefers-reduced-motion`). Inserted via the same scripted edit across all 10 pages rather than by hand, to guarantee byte-identical markup/behavior everywhere.
4. **`index.html` card redesign — four new components, one per card group, so no two card types on the page (or elsewhere in the template — every other page still uses `.card-string` verbatim) share a silhouette:**
   - `.card-showcase` (Shop by Category, 3 cards) — full-bleed photo, bottom gradient scrim, a circular brass "medallion" icon badge top-corner, headline+copy overlaid on the photo, a hover-revealed CTA line. Replaces the previous image-top/white-content-below `.card-string` treatment for this section.
   - `.card-catalog` (Featured Instruments, 6 cards) — horizontal list-row card (small square thumbnail or icon-tile left, copy+price+CTA right), solid hairline border (not dashed), `.price` in the display serif. A commerce/catalog-row silhouette, deliberately not a vertical stack.
   - `.card-quote` (Testimonials, 6 cards in the existing CSS-columns masonry) — borderless, tinted-surface "speech bubble" with a giant serif watermark quotation mark and a small triangular pointer at the bottom corner (`clip-path`, RTL-mirrored via a `[dir="rtl"]` override). The only fully borderless card on the page.
   - `.card-visit` (Visit Us info panel, 1 card) — solid mahogany gradient-fill dark panel (vs. the light/tinted surfaces of every other card on the page), a translucent `.icon-dot` badge, and a `box-shadow` ring added specifically around the live open/closed status pill so it stays legible even when its JS-set fill color is close to the card's own background gradient.
   - The shared mobile-menu quick-category tiles (2×2 grid inside `#mobile-panel`, identical across all 10 pages' mobile panels) were deliberately left on `.card-string` — they're a repeated navigation utility, not page content, so redesigning them page-by-page would fragment the mobile nav rather than serve the "unique card per content type" goal.

**Tooling note for future revision passes on this template:** Chrome's legacy `--headless --screenshot --window-size=W,H` CLI flags were found to be unreliable in this environment — requested widths were silently ignored in favor of a fixed effective viewport, and runs could silently attach to an unrelated already-running Chrome process instead of launching an isolated one (visible only as an "Opening in existing browser session" stderr line, easy to miss). Both the mobile-panel bleed and the hero overflow bugs above were only reliably reproduced (and only reliably fixed) after switching to driving Chrome via the DevTools Protocol directly (`--headless=new --remote-debugging-port` + `Emulation.setDeviceMetricsOverride` for exact width/height/mobile flags + `Page.captureScreenshot`), with an explicit `--user-data-dir` per run to guarantee a fresh, isolated instance. **Lesson: don't trust `--window-size` readings from this Chrome CLI path at face value — cross-check with an in-page `window.innerWidth` readout (or switch to CDP `Emulation.setDeviceMetricsOverride`) before concluding a layout bug is width-dependent or width-independent.**

### Second revision pass — Shop by Category and Testimonials redesigned again, Featured Instruments image padding fixed
Two direct follow-ups landed back-to-back and both concerned `index.html`'s card treatments from the pass above. `.card-showcase`, `.card-quote`, and their supporting CSS were fully deleted (not left as dead code) and replaced; `.card-catalog` was kept but reworked.

1. **Shop by Category — replaced the interactive tabbed switcher with `.card-frame`, a static 3-card grid:** the first redesign pass (documented above) had already gone through a full-bleed-photo-overlay version and then an interactive vertical-tab/photo-switcher version; this pass replaced the switcher with a third, simpler treatment per direct request: a bordered card with the photo on top (`aspect-ratio:4/3`), a circular brass "badge" medallion straddling the photo/body seam (`bottom:-1.25rem`, a `border:3px solid var(--surface)` ring so it reads as cut into the photo edge rather than just floating on top), and heading/copy/CTA-link below. No JS — this reverts the section from an interactive component back to a plain content grid, a deliberate simplification. `initCategorySwitcher()` and its `[data-cat-switcher]`/`[data-cat-tab]`/`[data-cat-panel]` markup were removed outright rather than left unused.
2. **Featured Instruments — `.card-catalog` reworked so the thumbnail sits flush with no padding on the image side:** previously the whole card had uniform `padding:.9rem`, so the 6.5rem square thumbnail floated inset with a visible margin on all four sides. Now `.card-catalog` itself carries `overflow:hidden` and zero padding, the thumb is `width:8rem` with `align-self:stretch` (no fixed height — it stretches to match the row's content height via the parent's default flex `align-items:stretch`) and sits flush against the card's start edge, and only `.card-catalog .body` (the text/price side) carries its own `padding:1rem 1.25rem`. Because the outer card still owns the `border-radius` + `overflow:hidden`, the flush thumbnail's outer corners still pick up the card's own rounding for free — no separate radius needed on the thumb. `.body` also gained `display:flex;flex-direction:column` directly in CSS (previously relied on a bare Tailwind `flex-1` utility in the markup with no explicit column direction, which meant the price/CTA row's `margin-top:auto` baseline-alignment trick wasn't actually guaranteed to work — a latent bug from the first pass, fixed here as a side effect).
3. **Testimonials — replaced the horizontal drag-scroll "ticket-stub" strip with `.testimonial-carousel`, a single-card prev/next + dot-indicator spotlight:** per direct request to redesign the section again. The ticket-stub cards (dashed perforation, punched-notch corners, mahogany stub) are gone entirely, along with their CSS. The new component is one centered bordered card (not a strip of many cards) showing one review at a time — big serif watermark quote-mark icon, `DM Serif Display` quote text, circular icon avatar, name, star rating — with a `prev`/`dots`/`next` control row beneath. New JS module `initTestimonialCarousel()` (`[data-testimonial-carousel]` root, `[data-testimonial-slide]` × 6 toggled via the `hidden` attribute exactly like the pattern already established for FAQ/instrument category tabs elsewhere on the site, `[data-testimonial-dot]` × 6, `[data-testimonial-prev]`/`[data-testimonial-next]`) — verified via CDP by scripting an actual dot click and confirming both the active dot state and the displayed quote/name/avatar updated correctly.
4. **Verification:** all three changes reconfirmed via CDP screenshots (not the unreliable `--window-size` CLI path) at desktop and mobile widths, in dark mode, and in `dir="rtl"` (thumbnail flush-edge correctly flips to the opposite side of the card; prev/next chevrons use the existing `.rtl-flip` utility). `node -c` clean, div-tag balance unchanged/clean on `index.html`, zero leftover references to `card-showcase`/`card-quote`/`card-ticket`/`category-switcher`/`category-tab`/`category-panel` anywhere in the HTML, CSS, or JS.

**Do not reuse for future templates (supersedes the equivalent bullets from the pass above for these three components):** `.card-frame` circular-badge-straddling-photo-seam card (Shop by Category), flush-edge-thumbnail `.card-catalog` with padding reserved for the text side only (Featured Instruments), `.testimonial-carousel` single-spotlight prev/next+dot review carousel (Testimonials). The interactive tabbed category switcher and the ticket-stub perforated testimonial strip were both built, verified, and then explicitly discarded in favor of these — still worth avoiding in future templates even though they no longer appear on this one, since they were fully designed and screenshotted before being replaced.

### Third revision pass — Testimonials made a responsive multi-card track, Final CTA differentiated from Visit Us, Shop by Category → flush-image cards
Two more direct follow-ups. The first flagged that the single-spotlight testimonial carousel from the pass above should show multiple cards at once ("2 or 3 based on screen width") and that the Final CTA band read as a near-duplicate of the Visit Us panel directly above it (both were solid mahogany-filled rounded blocks with centered/near-identical copy and a two-button row). The second, sent mid-turn while the first was still being verified, asked to redesign Shop by Category yet again or remove it — resolved via `AskUserQuestion` rather than guessing, since deleting a homepage section outright is a real content-loss decision distinct from a pure visual redesign; the answer was to redesign it using the same flush-image treatment just established on Featured Instruments.

1. **Testimonials — `.testimonial-carousel` (single centered spotlight card) replaced with a responsive multi-card scroll-snap track:** `.testimonial-slide`/`.testimonial-dot`/`.testimonial-dots`/the single-card-at-a-time `show(i)` JS logic are gone. Each of the 6 reviews is now its own compact bordered `.testimonial-card` (quote icon, `DM Serif Display` text, avatar+name+stars footer pinned via `margin-top:auto`) inside a `.testimonial-card-slide` flex-basis wrapper: `flex:0 0 calc(50% - .5rem)` below `lg` (2 cards visible) and `flex:0 0 calc(33.333% - 1rem)` at `lg`+ (3 visible) — literally "2 or 3 cards based on screen width," not a per-breakpoint JS recalculation. The track reuses the site's existing `.scroll-strip` class (native `scroll-snap-type:x`, drag/wheel support already wired site-wide via `initHorizontalScrollStrips()`) rather than inventing new pagination math. `initTestimonialCarousel()` was rewritten from slide/dot toggling to a plain `track.scrollBy({left: track.clientWidth * dir})` on prev/next click — one page-width jump per click, no discrete page-count/dot-index bookkeeping needed since the number of cards-per-page already varies by breakpoint. Prev/next buttons moved from below the card (centered, part of the single-card layout) to a small control pair beside the section heading (heading left, arrows right, `flex justify-between` header row) — a layout position that only makes sense once the track is a multi-item row rather than one full-width card. Verified via CDP: exactly 3 cards visible at 1440px, exactly 2 at 820px and 390px, and a scripted click on the "next" button moved `track.scrollLeft` from 0 to 1255px (confirmed via `Runtime.evaluate`, not just visually).
2. **Final CTA — new `.cta-band` component, deliberately not matching the Visit Us panel it sits directly beneath:** previously both sections used the same visual language (solid mahogany fill, rounded corners, centered-ish copy, white text, two-button row), which is exactly the kind of adjacent-section sameness the master prompt's "never repeat the same... component style" rule is meant to catch — it had slipped through because the two components were built in different passes without a side-by-side comparison. `.cta-band` flips three variables at once so it can't read as a reskin: **color** (solid brass-gold `var(--brass-500)` fill with dark `var(--mahogany-900)` text, not a mahogany/dark-gradient fill with light text), **shape** (asymmetric `flex` row — copy+buttons on one side, a decorative `.eq-bars` cluster recolored dark-on-brass on the other, via `md:flex-row` — not a centered single-column block), and **motion** (the equalizer-bar pulse animation, reusing the hero/footer's existing `.eq-bars` component in a third context, vs. Visit Us's static gradient). This also keeps Home 1's final CTA distinct from Home 2's full-bleed-photo final CTA (documented in the original Template 08 registry entry) — the site now has three genuinely different final-CTA treatments: Home 1 solid-brass asymmetric-with-eq-bars, Home 2 full-bleed-photo-with-overlay, and Visit Us's own two-column gradient panel (which functions as a secondary CTA, not the page's final one).
3. **Shop by Category — `.card-frame` (circular badge straddling the photo seam) replaced with `.card-flush`:** per the `AskUserQuestion` answer, matches Featured Instruments' "flush image, no padding" principle but in a **vertical** orientation (photo flush to the card's top/left/right edges, `aspect-ratio:4/3`, text panel padded below) rather than Featured Instruments' **horizontal** flush-start row — same underlying idea applied on a different axis so the two sections read as a coherent pair without being visually identical. The floating circular medallion badge is gone; a small static icon chip (`.card-flush-icon`, same circular-chip visual language as the testimonial avatars, for site-wide consistency) sits inline above the heading inside the padded body instead of overlapping the photo.
4. **Verification:** `node -c` clean; `index.html` div-tag balance 148/148; zero remaining references to `card-showcase`/`card-quote`/`card-ticket`/`category-switcher`/`category-tab`/`category-panel`/`card-frame`/`testimonial-slide`/`testimonial-dot` anywhere in HTML/CSS/JS (each confirmed via a post-edit `grep -c` sweep); all three components re-screenshotted via CDP at desktop/tablet/mobile widths.

**Do not reuse for future templates (supersedes the equivalent Testimonials/Final-CTA/Shop-by-Category bullets from both passes above):** responsive 2-up/3-up `.testimonial-card-slide` scroll-snap track with a `clientWidth`-based `scrollBy` prev/next (Testimonials), solid-brass asymmetric `.cta-band` with a recolored `.eq-bars` decoration as a final CTA (Home 1), vertical flush-top `.card-flush` (Shop by Category) — paired with Featured Instruments' horizontal flush-start `.card-catalog` as a deliberate matched-but-not-identical set. The single-spotlight `.testimonial-carousel` and the circular-badge `.card-frame` were both fully built and verified before being superseded — avoid them in future templates too.

### Fourth revision pass — Shop by Category removed, Final CTA redesigned again as a dark "spotlight glow" band
Two more direct follow-ups. First: remove the Shop by Category section from `index.html` entirely (not redesign it again) — category browsing remains reachable via the main nav's Instruments link and the mobile menu's quick-category tiles, so nothing was orphaned. Second, separately: redesign the brass `.cta-band` from the pass above with a more modern layout.

1. **Shop by Category removed:** the whole `<section>` (heading + 3 `.card-flush` cards) was deleted from `index.html`, and `.card-flush`/`.card-flush-media`/`.card-flush-body`/`.card-flush-icon`/`.card-flush-link` were deleted from `style.css` after confirming via `grep` that no other page referenced them (Featured Instruments' visually-related `.card-catalog` was left untouched — it's a separate component). Home 1 now runs Hero → Featured Instruments directly; the page's `<section>` count dropped from 10 to 9. Left the surrounding sections' backgrounds as-is (Hero and Featured Instruments both already sit on the plain page background with no explicit `background` override) rather than rebalancing the color-alternation rhythm, since only removal was requested, not a rhythm pass.
2. **Final CTA — `.cta-band` rebuilt a second time, from a flat brass asymmetric band to a dark centered "spotlight glow" band:** the brass-flat/asymmetric-split version (previous pass) is gone. The new version is a full-width dark card (`linear-gradient(180deg, var(--mahogany-900), var(--espresso-1))`) with a blurred radial "glow orb" (`::before`, `radial-gradient(circle, rgba(184,137,43,.4), transparent 68%)`, positioned to bleed off the top edge) sitting behind a centered eyebrow pill, oversized serif headline, copy, a centered button pair, and — new — a **trust-chip strip** below a hairline divider (`Since 1978 · 30+ Brands Carried · Family Owned · 5-Star Rated by Regulars`, reusing stats already established in the hero rather than inventing new claims). This is the third distinct Final-CTA treatment tried in this template's revision history (flat-brass-asymmetric-with-eq-bars → this dark-glow-centered version) and remains clearly distinct from Visit Us's diagonal two-tone gradient split panel directly above it (centered vs. split, radial-glow-on-dark vs. flat diagonal gradient, a trust-chip footer row vs. none) and from Home 2's full-bleed-photo final CTA. The decorative `.eq-bars` motif was dropped from this component entirely — it's still used in the hero, footer top edge, and 404 page, so the site doesn't lose the equalizer motif overall, it's just no longer doing double duty as this section's decoration.
3. **Verification:** `node -c` clean; `index.html` div/section balance 138/138 and 9/9; `grep` confirmed zero remaining `card-flush`/`Shop by Category` references anywhere in HTML or CSS; the new CTA band screenshotted via CDP at desktop, mobile, and in dark+RTL together (confirmed the button order and stat-chip order both mirror correctly under `dir="rtl"`, and contrast holds in dark mode since the component's colors were already theme-token-free/fixed-dark by design, not adaptive).

**Do not reuse for future templates:** dark radial "spotlight glow" `.cta-band` (blurred `::before` glow orb behind a centered eyebrow+headline+buttons+trust-chip-strip stack) as a final CTA treatment.

### Fifth revision pass — `instruments.html` completely redesigned
A direct request to completely redesign the catalog page, followed mid-turn by a request to source real photos for every card, redesign the card again, and add more sections. The original page (flat teal-gradient hero banner, plain `btn-pluck`/`btn-pluck-outline` filter pills, `.card-string` product cards mixed photo/icon-circle, one big image+text intro banner per category tab) is gone entirely.

1. **Hero — `.instrument-hero-collage`:** the only inner-page hero on the site with **no colour band at all** — text sits directly on the plain page background (About/Brands/Contact use solid or gradient colour bands, Rentals/Repair use full-bleed photo+overlay, FAQ uses flat teal). Right side is a tilted 3-photo "gallery wall" collage (`.instrument-hero-photo.p1/.p2/.p3`, each independently rotated ±3–6°, staggered z-index, bordered like a print). Below it, a plain icon-stat strip (`9+ In Stock Today · Every One Set Up by Hand · Rent or Buy, Your Choice · Trusted Since 1978`) inside a hairline-bordered band — new content, not present before.
2. **Category filter — `.instrument-filter-tab`:** replaced the plain `btn-pluck`/`btn-pluck-outline` pill toggle with a purpose-built chip component (icon + label + a live count badge per category). `initCategoryTabs()` was changed from swapping `btn-pluck`/`btn-pluck-outline` classes to toggling a generic `.is-active` class, since the filter is no longer built from the shared button component. **Bug found and fixed during this pass:** the hero's 3 category chips were also wired as functional tab triggers (`data-tab-btn="guitars"` etc., alongside their `href="#guitars"` fallback) so clicking them from elsewhere on the page actually switches the grid — but because `document.querySelectorAll('[data-tab-btn]')` returns elements in DOM order and the hero comes before the filter bar, `tabs[0]` silently became the hero's "Guitars" chip instead of the filter bar's "All" button, making the page default to the Guitars tab on load instead of All. Fixed by hardcoding the fallback default to `'all'` instead of deriving it from `tabs[0]` in both the no-hash-present and hash-doesn't-match-any-tab branches. **Lesson: never derive a "first/default" value from DOM order (`tabs[0]`) for a `[data-*]`-selected element set once more than one functional area of the page can contain elements carrying that same data-attribute — a later, unrelated addition elsewhere in the DOM can silently change which element is "first."**
3. **Cards — `.card-product` (built and then rebuilt once more in the same pass):** v1 was a rotated corner-ribbon badge + 2-column icon-bullet spec grid + dashed footer divider. Per direct follow-up this was replaced with v2: a flat pill badge overlapping the photo's top corner, hover-zoom on the photo itself, spec facts as small rounded pill **chips** (no icons), and a circular arrow icon-button CTA (`.card-product-cta`) instead of a text link — a cleaner "modern shop card" language distinct from `.card-string` (dashed border, pill badge, bullet list, text-link CTA) and `.card-catalog` (flush horizontal row, text-link CTA).
4. **Real photography for 7 of 9 products (up from 4 of 9):** sourced and visually verified (WebFetch raw CDN URL → `Read` the saved file) new photos for the electric guitar, ukulele, 61-key synth, 88-key weighted piano, and 49-key compact keyboard — all previously shown as flat icon tiles. Rejected candidates during sourcing, all real branding visible on close inspection: a Cordoba-branded ukulele, a "Chao—"-branded ukulele, a CASIO-branded keyboard, a YAMAHA-branded keyboard. **Compact Cajon Box Drum and Millrace Conga Pair deliberately kept as clean icon-fallback tiles** — cajon and true conga photography on Unsplash was either non-existent, mismatched (djembes, bongos, frame drums misfiled under those search terms), or dominated by a person's portrait rather than the instrument; per the site's standing content-accuracy policy, a clean icon tile was judged better than forcing a wrong or cluttered photo onto those two cards. **A real bug was introduced and caught during this pass:** an automated regex-based find/replace script (meant to swap 5 products' icon-fallback markup for `<img>` tags, keyed by matching "nearest following product-name heading") had a non-greedy wildcard that could skip over an unrelated product's card entirely when two products shared byte-identical icon-fallback markup (e.g. two guitar-icon/mahogany-100 fallbacks, three keyboard-icon/teal-500 fallbacks) — it correctly replaced each product's *first* occurrence but then, searching for that same product's *second* occurrence later in the file, matched through and consumed several unrelated cards in between, producing a cascading off-by-one: Ukulele got Cedar Bend's photo, 61-Key got Ukulele's, 88-Key got 61-Key's, 49-Key got 88-Key's, and Cajon wrongly received 49-Key's photo instead of staying an icon tile — while every one of those products' *second* (category-panel) occurrence was left completely untouched. Diagnosed by generating a full media-block → product-heading mapping (`awk` script pairing each `card-product-media` line with the next `font-display text-lg mb-1` heading) and comparing it against the intended assignment table, then corrected with 10 precise line-number-targeted replacements (re-verified line contents immediately before writing, since the bug's root cause was exactly this kind of ambiguous-match risk). **Lesson: a regex keyed on "this content, then eventually that anchor text" is unsafe once the same content block can legitimately repeat more than once with *different* other blocks of the same shape in between — either scope the match to guarantee adjacency (require the literal fixed markup between content and anchor, not a wildcard) or do the substitution via an explicit line-number/content diagnostic pass instead of a blind global regex.**
5. **Two new content sections:** "Staff Picks" (`.card-spotlight`, two large 2-column editorial cards — photo one side, copy+price+CTA the other — breaking the grid's rhythm on purpose) and "How Buying Works" (`.buying-steps`/`.buying-step-num`, three numbered circles connected by a horizontal line on desktop, a new step-flow pattern distinct from the pill-chip rental steps used on `index.html` and the plain icon band used elsewhere). Plus a rent/trade-in cross-sell (two solid-color link tiles, mahogany and teal) and a lightly restyled closing "Not Sure What You Need?" CTA.
6. **Verification:** `node -c` clean; `instruments.html` div balance 153/153, section count 4→7; zero remaining `card-product-ribbon`/broken-attribute references (`grep`-swept after the bug fix); full media→heading mapping re-verified correct for all 18 card instances (9 products × 2 panels each) after the fix; screenshotted via CDP at desktop and in dark+RTL together (header nav, hero collage tilt, filter-chip order, and badge position all confirmed mirroring correctly).

### Sixth revision pass — `brands.html` completely redesigned
A direct request to fully redesign the brand directory page (the last page still running its original launch-day layout: flat brass gradient+wood-grain hero, plain `btn-pluck`/`btn-pluck-outline` filter pills, `.card-string` brand tiles, one closing paragraph). The original page is gone entirely except its content (all 12 real brand names/categories/descriptions, kept verbatim) and its shared header/footer.

1. **Hero — `.brand-hero-frame`, a "shop placard" mechanic (new, third distinct inner-page hero device):** the site already had (a) solid/gradient colour-band + wood-grain banners (About, Instruments' predecessor, the old Brands, Contact, FAQ) and (b) `instruments.html`'s "no colour band at all, tilted photo collage" hero — so neither was reused. `brands.html` now sits on the plain page background (like Instruments) but composes it completely differently: a single centered bordered panel (`.brand-hero-frame`) with a brass top accent bar and four corner "rivets" (`.brand-hero-rivet`, positioned with logical `inset-block-*`/`inset-inline-*` properties so they mirror under RTL with zero overrides) holding the breadcrumb/eyebrow/H1/copy — a "museum placard" silhouette, not a split-column and not a photo collage. Three large low-opacity FontAwesome instrument-icon "ghosts" (`.brand-hero-ghost`, guitar/drum/keyboard, `opacity:.05–.08`, positioned with logical `inset-inline-*`) are scattered behind the panel on the plain background for a "wall of instruments" texture that needs no photography and no real/fake logos.
2. **Filter chips — `.brand-filter-chip`, a new component (not `.btn-pluck`, not Instruments' `.instrument-filter-tab`):** the original page used `btn-pluck`/`btn-pluck-outline` for its filter row, but the shared `initBrandFilter()` in `main.js` only ever toggled an `.is-active` class (never swapped those two button classes), so the old "active" filter state was visually inert. The new chip is a squared (`border-radius:.5rem`, not a 999px pill) tag with a small brass dot instead of an icon and no count badge — deliberately not a reskin of Instruments' pill+icon+count-badge chip. `.is-active` is now a real, fully-styled state (solid mahogany fill, brass dot). The `data-brand-filter`/`data-brand-category` attribute contract and `initBrandFilter()` itself were left completely untouched — the "All Brands" chip carries `is-active` + `aria-pressed="true"` in the initial HTML, matching the task's requirement that this filter (unlike Instruments' hash-aware tabs) has no JS-driven default-activation.
3. **Cards — `.card-plaque` (new, replaces `.card-string` for this page only):** an "engraved brass plaque" — centred content, an inset brass hairline (`box-shadow: inset 0 0 0 1px rgba(184,137,43,.22)`) instead of `.card-string`'s dashed border, four small corner rivets (logical inset properties again, no RTL override needed), a circular category-icon medallion, and an uppercase tracked "Authorized Dealer" rule (two short hairlines flanking the text) replacing the old solid-teal pill badge. Per the task's trademark-safety requirement (already logged from an earlier pass), brand cards stay text-wordmark only — no invented or scraped logo imagery.
4. **Two new sections added, plus the old one-paragraph "Why These Brands" folded into a fuller treatment — four new/rebuilt sections total:**
   - **Categories at a Glance** (`.brand-stat-band`) — a flat solid `var(--mahogany-900)` band (no gradient, no radial glow orb — deliberately distinct from `.cta-band`'s dark-glow treatment and `.visit-panel`'s diagonal gradient) with a 4-column stat grid (`.brand-stat`, hairline `border-inline-start` dividers) showing 3/3/3/3 brand counts per category via the existing `data-count-to` counter engine (no JS changes needed).
   - **Our Vetting Standard** — merges the old page's sole closing paragraph ("We Don't Carry Everything — On Purpose") with four new criteria rows (`.vet-row`: Playability Tested In-Store, Warranty Support We Can Stand Behind, Spare Parts We Can Actually Get, Honest Consistent Pricing). Deliberately built as **plain icon+text rows separated by the existing `.dashed-rule`-style dashed divider, with no numbering and no connecting line** — a third distinct step/list shape alongside Home 1's pill-chip rental steps and Instruments' numbered-circle+connector-line `.buying-steps`.
   - **Brand Spotlight** (`.brand-feature`) — a large-format feature on Millbrook Strings' "small Appalachian workshop" story, with a real, content-verified photo (see sourcing note below). Built as a layered/overlapping "shingled" composition (`.brand-feature-panel` physically overlaps the photo's edge via a negative `margin-inline-start`, with an explicit `[dir="rtl"]` override so the overlap correctly flips to the opposite edge under RTL rather than just mirroring via logical properties alone, since the two grid columns themselves don't reorder) — deliberately not a plain 50/50 side-by-side grid, since that's already `instruments.html`'s Staff Picks `.card-spotlight` silhouette.
   - **Ask About a Brand We Don't Carry** (`.brand-ask-cta`) — a light bordered panel with a brass `border-inline-start` accent bar (mirrors automatically under RTL) and a single `.btn-pluck` linking to `contact.html`, deliberately lighter-weight than the dark stat band above it.
5. **Image sourcing:** one new photo for the Brand Spotlight — `photo-1696876622217-11327c831549` (a close-up of an acoustic guitar's soundhole/fretboard/wood grain, no visible brand text or logo, no price tag). Found via `WebFetch` on an Unsplash search-results page (which only returns short page-slug ids), resolved to the long-form CDN id via a `WebFetch` on the individual photo page, then visually verified with the `WebFetch`-raw-CDN-URL → `Read`-the-saved-file technique before use. Confirmed via `grep -rl` against every `images.unsplash.com/photo-…` id already in the project that this id is not used anywhere else on the site.
6. **Verification:** `node -c assets/js/main.js` clean (no JS was changed — `initBrandFilter()` and its `data-brand-filter`/`data-brand-category` contract are untouched). `brands.html` tag balance: 80/80 `<div>`, 6/6 `<section>`, 42/42 `<a>`. Zero `lorem`/TODO/placeholder text. Every `href` resolves to a real shipped file (`index.html`, `home-2.html`, `about.html`, `instruments.html`, `brands.html`, `rentals.html`, `repair-tradein.html`, `contact.html`, `faq.html`) or a same-page/mail/tel link. `grep` confirmed zero collisions between the new class names (`brand-hero-*`, `brand-filter-chip`, `card-plaque*`, `brand-stat-*`, `vet-row`/`vet-icon`, `brand-feature*`, `brand-ask-cta`) and any pre-existing class in `style.css` or another page. Screenshotted via CDP (fresh `--headless=new --remote-debugging-port` + `--user-data-dir` instance, `Emulation.setDeviceMetricsOverride` for exact widths — the legacy `--window-size` CLI flag was confirmed unreliable again before switching) at desktop (1440px, full page), mobile (390px), and dark+RTL together (1440px, full page): confirmed the hero panel's rivets and top accent bar render correctly, filter chips and grid order reverse correctly under RTL, the vetting-standard icon circles sit on the correct (inline-end) side, and — the one component that couldn't rely on logical properties alone — the Brand Spotlight panel's photo-edge overlap and the Ask CTA's accent bar both correctly flip sides under `dir="rtl"` via their explicit overrides. Stat-counter animation confirmed settling to the correct final values (3/3/3/3) on both passes.

**Do not reuse for future templates:** `.brand-hero-frame` bordered "shop placard" hero panel with corner rivets + scattered low-opacity icon-ghost watermarks (no colour band, no photo), `.brand-filter-chip` squared colour-dot toggle (as opposed to a pill+icon+count chip), `.card-plaque` inset-hairline "engraved plaque" card with corner rivets and an uppercase double-hairline-flanked dealer rule, flat solid-fill (no gradient, no glow) `.brand-stat-band` stat strip with hairline column dividers, plain-icon-row-with-dashed-divider (`.vet-row`, no numbers, no connector line) as a vetting/criteria list shape, `.brand-feature` layered/overlapping "shingled" photo+panel spotlight card (photo-edge overlap requiring an explicit `[dir="rtl"]` margin-flip override, not just logical properties), `.brand-ask-cta` bordered panel with a single `border-inline-start` accent bar as a mini-CTA.

**Do not reuse for future templates:** colour-band-free hero with a tilted 3-photo gallery-wall collage (Instruments), icon+label+count `.instrument-filter-tab` chip filter, `.card-product` modern shop card (hover-zoom photo, flat corner badge, pill spec chips, circular arrow CTA button), `.card-spotlight` 2-column editorial "staff picks" card, `.buying-steps` numbered-circle-with-connecting-line step-flow.


---

## Template 10 — Print & Copy Shop

- **Brand name:** Sharpline Print & Copy Co.
- **Tagline:** "Precision Printing, Same-Day Ready"
- **Business category:** Neighborhood print & copy shop (documents, business cards, banners & signs, photo prints) with an online customer dashboard for upload, spec selection, job tracking and reorder.
- **Primary color:** Press Blue `#2454FF` (600), full ramp `#eef2ff` → `#142c7c`
- **Secondary color:** Toner Magenta `#E0218A` (500), full ramp `#fdeef7` → `#9c1260`
- **Accent color:** Highlighter Yellow `#FFC400` (500) — a literal CMYK-inspired 3-hue brand system (Cyan-register blue, Magenta, Yellow), not used by any prior template on this site.
- **Neutral / background:** Paper White `#FAFAF7` (light) / Ink Black `#14161C` (dark), with a warm off-white "paper" tint rather than pure white.
- **Font combination:** Anton (condensed poster-display, headlines/stamps) + IBM Plex Sans (body/UI) + IBM Plex Mono (job codes, prices, tracking numbers, turnaround pill) — a display+sans+mono triple structurally distinct from every prior pairing on this site (T1 serif+script+sans, T2 rounded Baloo+Quicksand, T3 angular Space Grotesk+Inter, T5 Sora+Inter, T6 Bricolage Grotesque+Karla, T7 Unbounded+Manrope, T8 DM Serif Display+Mulish). The mono face is functional, not decorative — it renders every job ID and price site-wide and in the dashboard.
- **Header type:** Logo-left, right-aligned nav (not centered, not split) — sticky glass-on-scroll header with a thin dark utility strip above it (turnaround-time mono pill + hours + phone, always visible) and a live "Today's turnaround: 24 hrs" mono status readout baked into the desktop nav row — a functional micro-feature not present on any prior template. Desktop nav/CTA gate at xl (1280px) per the site-wide lesson from T1; hamburger drawer below that. Dashboard pages do not reuse this header — they get a dedicated left sidebar + topbar app shell (see Dashboard below), a header shape unused elsewhere on the site.
- **Navigation style:** Single flex row, 7 links (Home, Home 2, Services, Pricing, Gallery, About, Contact) all directly visible, no generic "Pages" dropdown. Services page uses a secondary sticky in-page sub-nav (anchor links to Documents/Business Cards/Banners/Photo Prints) — a second-level nav pattern unique to that page.
- **Hero layout (Home 1 — index.html):** Full-bleed photo hero (offset press photo) with dark-to-brand-blue diagonal gradient, halftone-dot overlay, decorative crop-mark corners, Typed.js cycling headline ("PRECISION PRINTING FOR YOUR BUSINESS CARDS. / BANNERS & SIGNS. / BULK DOCUMENTS. / PHOTO PRINTS."), dual CTAs, and an inline stat trio (CountUp: jobs printed, avg turnaround, rating) sitting directly in the hero beneath a divider — not a floating overlapping card.
- **Hero layout (Home 2 — home-2.html):** Split-screen, text left / offset two-image collage right (large digital-press photo + smaller Pantone-swatch photo overlapping its corner with crop marks), no inline stats in the hero — stats instead live in a separate full-width dark band directly below. Structurally and compositionally distinct from Home 1.
- **Hero layout (inner pages):** Compact (not full-height) banner with breadcrumb over a themed photo + dark overlay, a different photo per page (About = press-operator portrait, Services = paper-cutter machine, Pricing = Pantone swatch book, Gallery = photo-prints table, Contact = storefront exterior, 404 = halftone pattern only, no photo).
- **Section order (Home 1):** Header → Hero (full-bleed + inline stats) → Trust strip (4-icon band) → Services grid (4 ticket-cards) → How It Works (4-step icon row w/ connecting dashed line) → Job Tracker teaser (dark halftone split panel with a live mock order-tracking ticket card) → Gallery preview (horizontal scroll-snap strip) → Testimonials (Swiper carousel, 3-up) → Pricing teaser (3 ticket-cards) → FAQ (accordion) → CTA banner (centered gradient) → Footer.
- **Section order (Home 2):** Header → Hero (split-screen collage) → Stats band (dark, full-width, CountUp) → What We Print (icon-chip horizontal strip) → Featured Work (asymmetric CSS-grid editorial collage, hover-reveal captions, no lightbox) → Our Process (vertical zig-zag alternating image/text, 3 steps) → Why Sharpline (3-col icon reasons grid) → Testimonials (custom stacked-card switcher with prev/next + dots, no Swiper, no spotlight-avatar pattern) → CTA close (image-left / dark-panel-right split). Completely different composition, order and testimonial mechanism from Home 1.
- **Card style:** .ticket-card — "receipt/ticket" card: near-square corners (10px radius, deliberately less rounded than typical), a small CSS "paperclip" .ticket-clip accent floating on the top-right corner, and a .ticket-tear dashed divider (with punch-hole circles at each end) separating the description area from the price/CTA baseline. Price is always rendered in .price-tag (IBM Plex Mono, bold). A new card language not used by any prior template (T1 cake-card, T2 rounded playful, T3 grocery, T5 shop, T6 gift, T7 gym stat card, T8 card-product/card-spotlight).
- **Button style:** Rectangular, 6px radius (not pill, not sharp square) with a signature hard offset shadow (4px 4px 0 accent-color) that grows and lifts on hover (translate(-2px,-2px)) — evokes a stamped/registration-mark print aesthetic. Primary = blue w/ yellow offset shadow, Secondary = magenta w/ black offset shadow. Not used by any prior template (T1 pill+glow, T5/T6/T7/T8 flat or soft-shadow fills).
- **Image style:** Full-bleed hero photography; horizontal scroll-snap strip (Home 1 gallery preview); asymmetric CSS-grid editorial collage with hover-caption overlays (Home 2 featured work, distinct from any lightbox pattern); Pinterest-style CSS-columns masonry with category filtering + GLightbox (dedicated Gallery page); offset two-photo collage with crop-mark corners (Home 2 hero, About story grid).
- **Background style:** Warm paper-white base with a subtle halftone dot-pattern (radial-gradient, brand-blue dots at low opacity) used on dark sections (Job Tracker teaser, 404) and hero overlays — a CMYK-print-process visual motif distinct from every prior template's texture (T1 sprinkle dots, T2 none noted, T3 none noted, T6 none noted).
- **Animation style:** GSAP + ScrollTrigger fade/slide reveals site-wide (direction-aware) — a fresh primary animation library choice for this site (T1 used AOS). Typed.js cycling hero headline (Home 1 only). Custom vanilla count-up counters (IntersectionObserver + easing, no CountUp.js dependency). GLightbox on the Gallery page. A signature "printing progress bar" micro-animation (.print-progress, animated CMYK gradient sweep) used on every job/order card across the site and dashboard to visualize print status — a motif unique to this template's subject matter. Smart-scroll header (hide on scroll-down, show on scroll-up).
- **Footer layout:** .torn-top — dark ink-black panel whose top edge is a jagged torn-paper silhouette (clip-path polygon zigzag), evoking a hand-torn sheet of paper — a top-edge motif distinct from every prior template (T1 scallop, T2 wave, T3 diagonal-stripe, T5 rounded corners, T6 perforated ticket-tear, T7 chevron track-line, T8 EQ equalizer bars). Standard 4-column grid (brand+social / Explore / My Account / Visit the Shop with hours+phone+email).
- **Mobile menu pattern:** Right-side slide-in drawer (RTL-aware: docks and slides from the inline-end side via CSS logical properties, correctly flipping for dir="rtl" — learned from T1's documented RTL drawer bug) with stacked links, theme+direction toggle buttons, account link and CTA button.
- **Dashboard (customer area — new page type for this site):** Dedicated left sidebar + topbar app shell (JS-injected, distinct from the public marketing header/footer entirely — dashboard pages render no public header, no torn-paper footer). Sidebar nav: Overview, New Print Job, My Orders, Back to website, Log out. Overview page: gradient welcome banner + 4 stat cards (active jobs, ready for pickup, total orders, total spent) + recent-orders ticket-card list + 2 quick-action cards. New Print Job page: drag-and-drop upload zone (native File API, metadata captured — name/size only, no real backend) + spec form (service, paper/size, color, sides, finishing, copies with a stepper control, Standard/Rush turnaround radio cards) with a live price estimate recalculated on every field change, sticky order-summary sidebar. My Orders page: status-filter chip bar (Received/Processing/Printing/Ready/Picked Up) + ticket-card rows each with a print-progress bar and a one-click Reorder button that clones the original job's file-on-record and specs into a new order with status "Received" — no re-upload required, satisfying the brief's core requirement. Order Details page: horizontal 4-step status timeline (Received → Processing → Printing → Ready), full spec table, reorder button. All order data is mocked via localStorage (sharpline-orders), seeded with 4 realistic in-progress jobs on first load, with a lightweight time-based auto-progression so statuses visibly advance across return visits.
- **Special design features:** Dark/light mode toggle (data-theme attribute + localStorage, system-preference fallback, anti-flash inline head script on every page); RTL/LTR direction toggle (dir attribute + localStorage, CSS logical properties throughout — ps-*, pe-*, start-*, end-* — rather than Tailwind rtl: variants); mock authentication (localStorage sharpline-auth, login/register forms redirect into the dashboard, guest auto-provisioning so the dashboard demo is explorable without forcing a login wall); service-preselect deep-linking (dashboard-new-order.html?service=business-cards pre-fills the order form from Services/Pricing CTAs); sticky in-page sub-nav on Services; tabbed spec/pricing/FAQ panel on Service Details; password-visibility toggle; quantity stepper control; generic data-validate form-validation micro-framework (required/email/phone/min/max) reused across Contact, Login, Register and New Print Job forms.
- **Pages built:** index.html (Home 1), home-2.html (Home 2), about.html, services.html, service-details.html (Document Printing deep-dive), pricing.html, gallery.html, contact.html, login.html, register.html, dashboard.html, dashboard-new-order.html, dashboard-orders.html, dashboard-order-details.html, 404.html.
- **Libraries used:** Tailwind CSS (CDN, utility classes only — dark mode and RTL handled via custom CSS variables/attributes, not Tailwind's dark:/rtl: variants, for cross-version reliability), Google Fonts (Anton / IBM Plex Sans / IBM Plex Mono), GSAP 3.12 + ScrollTrigger (scroll reveals), Typed.js 2.1 (Home 1 hero only), Swiper 11 (Home 1 testimonials only), GLightbox (Gallery page only). No CountUp.js (custom vanilla implementation instead). No build step; no backend — dashboard "database" is localStorage.

**Do not reuse for future templates:** CMYK-inspired 3-hue blue/magenta/yellow palette; Anton+IBM Plex Sans+IBM Plex Mono display/body/mono triple; utility-strip-plus-live-turnaround-pill header; .ticket-card receipt/ticket card language (paperclip corner + dashed tear-line + mono price tag); hard-offset-shadow "stamped" button style; .torn-top jagged clip-path paper-tear footer edge; halftone dot-pattern background texture; .print-progress animated CMYK-gradient status bar; sidebar+topbar dashboard app shell pattern; horizontal zig-zag process timeline paired with an asymmetric hover-caption editorial grid (Home 2); custom stacked-card testimonial switcher with dot/arrow controls (Home 2).


### Card-system changelog (applies to all pages in this template)
Post-launch revision — noted here so future templates don't repeat the same mistake: the initial build leaned on `.ticket-card` (receipt/paperclip/tear-line) as a catch-all for nearly every boxed content block on the site — service cards, pricing, testimonials, FAQ items, dashboard stats, contact info, spec option groups — which read as monotonous despite the card itself being a good signature component. Split into five purpose-built components, each reserved for one semantic role, with `.ticket-card` narrowed back to only literal order/job-ticket contexts (Services grid, dashboard order rows, order-summary sidebars):
- `.price-plate` — pricing content only (top CMYK gradient bar + icon badge, no paperclip/tear). Used on the Home 1 pricing teaser and all four `pricing.html` tables.
- `.quote-card` — testimonials only (decorative serif quote glyph + blue accent rail). Used on Home 1's Swiper slides and Home 2's stacked-card switcher.
- `.stat-tile` — flat metric panels (dashboard Overview stat row).
- `.info-panel` (+ `.info-panel-cta` hover variant) — plain bordered content boxes for things that aren't a product/price/quote/stat: Contact sidebar, Service Details' spec-option groups, dashboard Order Details' spec/info lists, dashboard Overview's two quick-action links (left accent-bar variant).
- `.accordion-shell` / `.accordion-row` — FAQ redesigned from four separate `.ticket-card` boxes into one flat divided list with a rotating chevron, matching common modern FAQ patterns instead of stacking more cards.

Also redesigned the Home 1 "Recent Work" gallery preview from a plain horizontal scroll-snap image strip into a fixed-shape **bento grid** (`.bento-tile` / `.bento-cta`) — one large featured tile, three supporting tiles, and a dark "View Full Gallery" CTA tile baked directly into the grid. This is a third distinct gallery treatment alongside Home 2's asymmetric hover-caption editorial grid and the dedicated Gallery page's Pinterest masonry + lightbox — no two of the site's three gallery-style layouts repeat the same grid mechanic or card shape.

**Do not reuse for future templates (additions):** `.price-plate` top-gradient-bar pricing card; `.quote-card` serif-glyph + accent-rail testimonial card; fixed-shape bento grid with an embedded dark CTA tile as a homepage gallery preview.

### Pricing teaser + FAQ redesign (Home 1)
Post-launch revision: the pricing teaser (`.price-plate` 3-card grid) and FAQ (`.accordion-shell`) on `index.html` were replaced with two more distinctive, on-brand designs:
- **Pricing teaser -> printed receipt mockup** (`.receipt-card`): a single centered, slightly rotated white "paper" receipt with a mono-font itemized price list, dashed section dividers, a CSS barcode, and a jagged torn-off bottom edge (`clip-path` zigzag, same technique family as the footer's `.torn-top` but applied to a small card's bottom edge instead of a full-width top edge). Deliberately stays paper-white/ink-black in both light and dark theme, like a photographed object. `.price-plate` remains in use on `pricing.html`'s four service tables.
- **FAQ -> chat-bubble Q&A** (`.chat-bubble-q` / `.chat-bubble-a` / `.chat-avatar-*`): a messaging-style conversation layout, customer questions right-aligned in grey bubbles, shop answers left-aligned in blue bubbles with a printer-icon avatar, all visible by default (no accordion/JS dependency — better scanability and one less interactive dependency). The now-unused `.accordion-shell` CSS and its JS wiring (`data-accordion`) were removed from the codebase since this was their only usage site-wide.

**Do not reuse for future templates (additions):** printed-receipt pricing mockup with barcode + torn edge; chat-bubble/messaging-style FAQ layout.

### Round 2 revisions — Home 2, Services, Pricing, Gallery redesigns + theme/responsive hardening
- **Home 2 hero** changed from a two-image offset collage to a single large framed image with a floating `.ticket-card` stat overlay ("4.9/5 — 600+ local reviews") anchored to its bottom-left corner, crop-marks retained. Still structurally distinct from Home 1's full-bleed background hero.
- **Home 2 Featured Work** replaced with an interactive **category showcase**: a left-hand list of 4 category buttons (reusing the site's generic `data-tabs` mechanism) swaps a large image + caption on the right. A third distinct gallery-style treatment alongside Home 1's bento grid and the Gallery page's masonry.
- **Services** gained three new sections: an "At a Glance" comparison table (responsive column-hiding below `sm`/`md` instead of forcing horizontal scroll), a "Turnaround & Delivery" 3-card band (`.info-panel` with colour-coded left borders), and a "Bulk & Custom Orders" split CTA.
- **Pricing** was fully rebuilt: tabbed per-service pricing (`data-tabs` + `.price-plate`) replaces four stacked full-width tables, plus new "Add-ons & Finishing" unified table, a dark "Volume Discounts" band, and a plain-list Pricing FAQ (no card, no accordion, no chat-bubble — a fourth distinct FAQ presentation on the site).
- **Gallery** rebuilt as a fixed 4-column (4×2 per page) grid instead of Pinterest masonry, with counts fixed to 4/8/12 per category (Documents 12, Business Cards 8, Banners & Signs 8, Photo Prints 4 = 32 total) and real pagination (prev/next + numbered pages, per-filter page state) built with vanilla JS. Added a stats band and a "Client Spotlight" case-study section.
- **Theme colour pass**: introduced theme-aware tint variables (`--tint-blue`, `--tint-magenta`, `--tint-yellow`) to replace fixed light-50-shade icon-badge backgrounds that looked like stray bright chips on dark cards in dark mode (affected `.plate-icon`, home-2 showcase icons, services turnaround icons, dashboard-new-order's selected-turnaround state). Backgrounds now shift automatically with the theme instead of staying a fixed light tint.
- **Responsiveness**: audited all 15 pages × 7 breakpoints (320–1440px) for horizontal overflow. Found and fixed a real bug — the Services "At a Glance" table's `min-w-[560px]` forced genuine page-level horizontal scroll on narrow viewports despite its `overflow-x-auto` wrapper (confirmed via actual scroll-position/visual-shift testing, not just DOM measurement). Fixed at the source with responsive column-hiding (and the matching Pricing add-ons table got the same treatment after a text-clipping issue turned up). Also added a global defensive `html, body { overflow-x: hidden }` safety net, and a hard 4-second fallback timer on the count-up stat animation (mirroring the existing scroll-reveal safety net) so a stat can never stay stuck at "0" if it never crosses the IntersectionObserver threshold.

**Do not reuse for future templates (additions):** framed-single-image hero with a corner-overlapping stat card (Home 2); left-list/right-image-swap "category showcase" as a gallery preview mechanic; responsive data-table pattern that hides secondary columns below a breakpoint instead of forcing horizontal scroll.

### Round 3 revisions — palette swap to Indigo/Amber, button redesign, nav breakpoint fix
- **Palette swap**: replaced the CMYK-inspired Press Blue/Toner Magenta/Highlighter Yellow palette with **Indigo `#4338CA` (primary) + Amber `#F59E0B` (secondary) + Rose `#FB7185` (accent)**, per explicit user direction. Token names (`--c-blue-*`, `--c-magenta-*`, `--c-yellow-*`) were kept to avoid a site-wide variable rename; only values changed. Swept and fixed several hardcoded (non-variable) rgba/hex references to the old palette that a variable-only swap would have missed: focus-ring box-shadows on `login.html`/`register.html`/`contact.html`/`dashboard-new-order.html`, hero gradient overlays on `index.html`/`login.html`/`register.html`, the favicon SVG data-URI fill color (all 15 pages), and the dashboard status-badge dark-mode colors. Also swapped the semantic assignment of the "processing" vs "printing" job-status colors (processing → amber/in-progress, printing → rose/active) since the old token-to-status mapping no longer made intuitive sense under the new hues.
- **Button redesign**: replaced the hard-offset "stamp" shadow (`4px 4px 0 var(--accent)`) with a clean elevated style — solid fill, a soft colour-tinted blurred shadow, and a gentle lift on hover. `.btn-secondary` (amber) switched to dark ink text by default (better contrast on a light/warm amber fill than white).
- **Nav breakpoint fix**: the desktop nav/CTA controls were gated at Tailwind's `xl` (1280px), so any browser window between 1024–1279px — a very common laptop/desktop width — showed the mobile hamburger drawer instead of the real desktop navigation, which read as a bug ("mobile menu popup" appearing in normal desktop use). Measured that the full header (logo + 6 nav links + controls, now fewer than the original 7 thanks to the Home 1/Home 2 dropdown) fits at 1024px with zero overflow, so the gate was lowered to `lg` (1024px) site-wide.

**Do not reuse for future templates (additions):** Indigo/Amber/Rose palette; soft-elevated-shadow button style (no hard offset).

### Round 4 revisions — image dedup, mobile menu, RTL bug, lightbox bug, more content
- **Image duplication audit**: found the same photo reused across 3-6 unrelated pages in many cases (e.g. one document-stack photo appeared on index/home-2/services/service-details/gallery). Re-sourced ~19 fresh, verified real Unsplash images and reassigned so no photo appears on more than one page except where index.html's homepage cards intentionally preview gallery.html's own archive (a deliberate, justified overlap).
- **Mobile menu now full-width** (`w-full` instead of `w-[85%] max-w-sm`) per explicit request.
- **RTL/LTR toggle bug**: flipping `dir` changed the mobile drawer's (and dashboard sidebar's) closed-position `transform` value, and since both had an active CSS transition, the hidden panel visibly slid across the entire screen before settling back off-canvas — looked exactly like an unwanted "mobile menu popup" appearing in desktop/web view. Fixed by briefly adding a `.dir-switching { transition: none !important }` class around the `dir` attribute change (removed two animation frames later), so the hidden position updates instantly instead of animating through view.
- **Nav breakpoint bug**: desktop nav/controls were gated at `xl` (1280px) even though the full header (logo + 6 links + controls) fits cleanly at `lg` (1024px) with zero overflow — any window between 1024–1279px (very common laptop width) was incorrectly showing the mobile hamburger drawer. Lowered the gate to `lg` site-wide.
- **Testimonial Swiper equal-height bug** (Home 1): a Tailwind `h-auto` class on `.swiper-slide` was overriding Swiper's own `height:100%` equal-height mechanism. Removed the override and added a `.testimonial-swiper .quote-card { min-height: 230px }` safety net so all slides read as visually identical regardless of testimonial length.
- **Bento gallery preview lazy-load flash** (Home 1): the 4 secondary tiles used `loading="lazy"`, which on mobile showed a visible gray placeholder flash since users scroll into this "Recent Work" section quickly. Switched to eager loading for this small, high-value image set.
- **GLightbox blank-slide bug** (Gallery): clicking any gallery image opened a lightbox with the caption showing but the image area blank. Root cause — GLightbox auto-detects content type from the URL's file extension, and Unsplash CDN URLs have none, so it silently failed to render an `<img>`. Fixed by adding `data-type="image"` to all 32 gallery lightbox links.
- **Gallery content expansion**: replaced the single testimonial with a broader page: added "From File to Finished Print" (3-step process with real photos), "More Recent Wins" (3 quick case-result stats), and swapped the standalone testimonial for a "Paper & Stock We Use" materials section (4 real stock-texture photos + descriptions) for more substantive, on-topic content per explicit request.
- **About/Contact expansion**: About gained "What We Stand For" (4 borderless value props) and "Inside the Press Room" (3-photo equipment showcase); Contact gained a "Contact by department" 3-panel section (colour-coded left borders) and a native `<details>`/`<summary>` FAQ (zero-JS, a fresh accordion mechanic distinct from the chat-bubble and plain-list FAQs used elsewhere on the site).

**Do not reuse for future templates (additions):** native `<details>`/`<summary>` FAQ pattern; `.dir-switching` transition-freeze technique for direction toggles on off-canvas panels.
